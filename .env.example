# Copy to `.env` (git-ignored) and fill in. Variable NAMES only here, never real values.

# CourtListener API token (https://www.courtlistener.com/help/api/rest/ -> profile).
# Used only for the research adapter's CourtListener requests. Oyez needs no token.
COURTLISTENER_TOKEN=
