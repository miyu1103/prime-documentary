import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT320_CAPTIONS, SHORT320_TOTAL_SEC} from './short320_timing';

/**
 * short320 — A muster drill is engineering made of people: it converts several thousand strangers into countable groups standing next to the boats. Nearly two thousand aboard had never once walked to theirs - legally, ordinarily, by video.
 * Episode PD-2026-080-concordia.  Type ?.
 *
 * AVOIDS: States that the video arrangement was legal, ordinary and industry-wide before saying what it cost, so the Short does not read as an accusation. No charge, no trial, no named person, no claim that the crew failed - the passengers' own testimony about the crew is in the third Short, not contradicted here.
 * LEAVES FOR THE LONG-FORM: What happens to a video briefing in a corridor after the ship loses all power in ninety seconds?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-080-concordia.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short320/short320_01.png', kind: 'image', motion: 'parallax', fast: true},   // close-up of an orange lifejacket being fastened
  {line: 'L1', id: 'p02', src: 'shorts/short320/short320_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a rack of folded orange lifejackets in a deck locker
  {line: 'L2', id: 'p03', src: 'shorts/short320/short320_03.png', kind: 'image', motion: 'pushin', fast: true},   // an abandon-ship drill on an open deck in brilliant dayligh
  {line: 'L2', id: 'p04', src: 'shorts/short320/short320_04.png', kind: 'image', motion: 'parallax'},   // a cruise ship stairwell of polished brass and glass
  {line: 'L3', id: 'p05', src: 'shorts/short320/short320_05.png', kind: 'image', motion: 'kenburns'},   // a safety briefing video playing on a cabin television
  {line: 'L3', id: 'p06', src: 'shorts/short320/short320_06.png', kind: 'image', motion: 'pushin'},   // a cabin bunk with an orange lifejacket laid out
  {line: 'L4', id: 'p07', src: 'shorts/short320/short320_07.png', kind: 'image', motion: 'parallax'},   // a very large white cruise ship at a Mediterranean quay
  {line: 'L4', id: 'p08', src: 'shorts/short320/short320_08.png', kind: 'image', motion: 'kenburns'},   // a gangway at a quay in brilliant daylight
  {line: 'L5', id: 'p09', src: 'shorts/short320/short320_09.png', kind: 'image', motion: 'pushin'},   // a blank muster list posted in a frame on a bulkhead
  {line: 'L6', id: 'p10', src: 'shorts/short320/short320_10.png', kind: 'image', motion: 'parallax'},   // a cruise ship corridor of cabin doors receding
  {line: 'L6', id: 'p11', src: 'shorts/short320/short320_11.png', kind: 'image', motion: 'kenburns'},   // a cruise ship reception atrium in daylight
  {line: 'L7', id: 'p12', src: 'shorts/short320/short320_12.png', kind: 'image', motion: 'pushin'},   // a crowded cruise ship theatre in the evening
  {line: 'L8', id: 'p13', src: 'shorts/short320/short320_13.png', kind: 'image', motion: 'parallax'},   // a wide carpeted cruise ship lounge in the evening
  {line: 'L8', id: 'p14', src: 'shorts/short320/short320_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a cruise ship dining room in the evening, every chair set
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT320_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT320: ShortData = {
  shortId: 'short320',
  episodeId: 'PD-2026-080-concordia',
  durationSec: SHORT320_TOTAL_SEC,
  narrationSrc: 'shorts/short320/audio/short320_final_mix_v002_en_us.mp3',
  captions: SHORT320_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short320/short320_ctathumb.jpg',
  ctaLongTitle: "Sixty-Nine Minutes to Give the Order.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short320.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short320/short320_kin_never_walked.webm', atSec: 38.99, durSec: 2.2, phrase: "NEVER ONCE WALKED"},
    {src: 'shorts/short320/short320_kin_rehearsal.webm', atSec: 51.33, durSec: 2.4, phrase: "THE REHEARSAL WAS NOT"},
  ],
};
