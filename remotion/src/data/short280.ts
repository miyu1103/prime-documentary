import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT280_CAPTIONS, SHORT280_TOTAL_SEC} from './short280_timing';

/**
 * short280 — One long steel rod became two shorter ones, four inches apart. On the drawing it looks like the same thing.
 * Episode PD-2026-069-hyatt.  Type ?.
 *
 * AVOIDS: No casualty figure and no victim named; no claim about whose hand drew it - the script says nobody has established that; no engineering conclusion the film does not state.
 * LEAVES FOR THE LONG-FORM: On the drawing it looks like the same thing. What did the change actually do?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-069-hyatt.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short280/short280_01.png', kind: 'image', motion: 'parallax', fast: true},   // H001 approved episode plate
  {line: 'L1', id: 'p02', src: 'shorts/short280/short280_02.png', kind: 'image', motion: 'kenburns', fast: true},   // H002 approved episode plate
  {line: 'L2', id: 'p03', src: 'shorts/short280/short280_03.png', kind: 'image', motion: 'pushin', fast: true},   // H003 approved episode plate
  {line: 'L3', id: 'p04', src: 'shorts/short280/short280_04.png', kind: 'image', motion: 'parallax'},   // H004 approved episode plate
  {line: 'L4', id: 'p05', src: 'shorts/short280/short280_05.png', kind: 'image', motion: 'kenburns'},   // H005 approved episode plate
  {line: 'L4', id: 'p06', src: 'shorts/short280/short280_06.png', kind: 'image', motion: 'pushin'},   // H006 approved episode plate
  {line: 'L5', id: 'p07', src: 'shorts/short280/short280_07.png', kind: 'image', motion: 'parallax'},   // H009 approved episode plate
  {line: 'L5', id: 'p08', src: 'shorts/short280/short280_08.png', kind: 'image', motion: 'kenburns'},   // H011 approved episode plate
  {line: 'L6', id: 'p09', src: 'shorts/short280/short280_09.png', kind: 'image', motion: 'pushin'},   // H016 approved episode plate
  {line: 'L6', id: 'p10', src: 'shorts/short280/short280_10.png', kind: 'image', motion: 'parallax'},   // H021 approved episode plate
  {line: 'L7', id: 'p11', src: 'shorts/short280/short280_11.png', kind: 'image', motion: 'kenburns'},   // H026 approved episode plate
  {line: 'L7', id: 'p12', src: 'shorts/short280/short280_12.png', kind: 'image', motion: 'pushin'},   // H029 approved episode plate
  {line: 'L8', id: 'p13', src: 'shorts/short280/short280_13.png', kind: 'image', motion: 'parallax'},   // H003 approved episode plate
  {line: 'L8', id: 'p14', src: 'shorts/short280/short280_14.png', kind: 'image', motion: 'kenburns'},   // H004 approved episode plate
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT280_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT280: ShortData = {
  shortId: 'short280',
  episodeId: 'PD-2026-069-hyatt',
  durationSec: SHORT280_TOTAL_SEC,
  narrationSrc: 'shorts/short280/audio/short280_final_mix_v002_en_us.mp3',
  captions: SHORT280_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short280/short280_ctathumb.jpg',
  ctaLongTitle: "Non-Delegable",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short280.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short280/short280_kin_four_inches.webm', atSec: 7.41, durSec: 2.0, phrase: "4 INCHES"},
    {src: 'shorts/short280/short280_kin_whose_hand.webm', atSec: 11.07, durSec: 2.2, phrase: "NOBODY KNOWS WHO"},
  ],
};
