import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT309_CAPTIONS, SHORT309_TOTAL_SEC} from './short309_timing';

/**
 * short309 — Every grid keeps a critical-load list of the customers who never get shed. The gas fields that fuelled the power plants were not on it - so the grid's own rescue cut the fuel of the plants it was saving, and the cut fed itself.
 * Episode PD-2026-073-uri.  Type ?.
 *
 * AVOIDS: Blames nobody - the Short closes each step of the loop with the script's own sentence that nobody in the chain did anything wrong at the moment they did it. Does not blame any fuel type: the gas system appears as the load that was cut, not as the culprit. Never says the grid collapsed. No deaths are mentioned. The list mechanism and every step of the loop are verified script sentences.
 * LEAVES FOR THE LONG-FORM: How close did the grid come to going down entirely - and what was the number that measured it?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-073-uri.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short309/short309_01.png', kind: 'image', motion: 'parallax', fast: true},   // a printed list on a plain desk under a lamp, the page enti
  {line: 'L1', id: 'p02', src: 'shorts/short309/short309_02.png', kind: 'image', motion: 'kenburns', fast: true},   // two hands holding a blank ruled page over a desk, no face
  {line: 'L2', id: 'p03', src: 'shorts/short309/short309_03.png', kind: 'image', motion: 'pushin', fast: true},   // a municipal water treatment plant from outside the fence
  {line: 'L2', id: 'p04', src: 'shorts/short309/short309_04.png', kind: 'image', motion: 'parallax'},   // a large blank board on an office wall with pins and string
  {line: 'L3', id: 'p05', src: 'shorts/short309/short309_05.png', kind: 'image', motion: 'kenburns'},   // a gas field of low wellheads across flat land under thin s
  {line: 'L3', id: 'p06', src: 'shorts/short309/short309_06.png', kind: 'image', motion: 'pushin'},   // a wellhead valve tree glazed with ice, close
  {line: 'L4', id: 'p07', src: 'shorts/short309/short309_07.png', kind: 'image', motion: 'parallax'},   // a gas compressor station at night, buildings and stacks, l
  {line: 'L4', id: 'p08', src: 'shorts/short309/short309_08.png', kind: 'image', motion: 'kenburns'},   // the same low gas compressor compound seen from outside, da
  {line: 'L5', id: 'p09', src: 'shorts/short309/short309_09.png', kind: 'image', motion: 'pushin'},   // a gas-fired power station at night standing cold and dorma
  {line: 'L5', id: 'p10', src: 'shorts/short309/short309_10.png', kind: 'image', motion: 'parallax'},   // an industrial disconnect switch on a pole, handle down, sn
  {line: 'L6', id: 'p11', src: 'shorts/short309/short309_11.png', kind: 'image', motion: 'kenburns'},   // a plain dark surface for a composited three-node loop diag
  {line: 'L7', id: 'p12', src: 'shorts/short309/short309_12.png', kind: 'image', motion: 'pushin'},   // a gas wellhead valve tree with rime ice on every surface
  {line: 'L8', id: 'p13', src: 'shorts/short309/short309_13.png', kind: 'image', motion: 'parallax'},   // a figure in high-visibility clothing walking a gas pad in 
  {line: 'L8', id: 'p14', src: 'shorts/short309/short309_14.png', kind: 'image', motion: 'kenburns'},   // the same gas field under a hot flat sky, dry grass
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT309_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT309: ShortData = {
  shortId: 'short309',
  episodeId: 'PD-2026-073-uri',
  durationSec: SHORT309_TOTAL_SEC,
  narrationSrc: 'shorts/short309/audio/short309_final_mix_v002_en_us.mp3',
  captions: SHORT309_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short309/short309_ctathumb.jpg',
  ctaLongTitle: "246 Died in the Texas Freeze. The",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short309.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short309/short309_kin_not_critical.webm', atSec: 14.58, durSec: 2.2, phrase: "NOT IDENTIFIED AS CRITICAL"},
    {src: 'shorts/short309/short309_kin_cut_more.webm', atSec: 33.47, durSec: 2.4, phrase: "CUT MORE"},
  ],
};
