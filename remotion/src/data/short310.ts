import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT310_CAPTIONS, SHORT310_TOTAL_SEC} from './short310_timing';

/**
 * short310 — Everyone on the water did the right thing in the right order - closed the breakers by hand twice, called the tug, warned the channel, stopped the traffic - and the recorder proves it. Sixty-seven seconds still ran out.
 * Episode PD-2026-077-keybridge.  Type ?.
 *
 * AVOIDS: Names no defendant and no company; makes no claim about why the ship failed to recover, which is the contested part. Does not depict the collision, the collapse or the six men beyond the script's own sentence that they were gone. No allegation is spoken at all in this Short.
 * LEAVES FOR THE LONG-FORM: If nobody on that ship made a mistake, what was it that kept switching the power off?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-077-keybridge.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short310/short310_01.png', kind: 'image', motion: 'parallax', fast: true},   // a wall-mounted operations clock in a dim traffic control r
  {line: 'L1', id: 'p02', src: 'shorts/short310/short310_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a gloved hand resting on a radio handset, transmit light o
  {line: 'L2', id: 'p03', src: 'shorts/short310/short310_03.png', kind: 'image', motion: 'pushin', fast: true},   // an empty six-lane bridge deck at night down its centre lin
  {line: 'L2', id: 'p04', src: 'shorts/short310/short310_04.png', kind: 'image', motion: 'parallax'},   // a single orange traffic cone in a pool of work light
  {line: 'L3', id: 'p05', src: 'shorts/short310/short310_05.png', kind: 'image', motion: 'kenburns'},   // a works truck with a rotating amber beacon on a closed lan
  {line: 'L4', id: 'p06', src: 'shorts/short310/short310_06.png', kind: 'image', motion: 'pushin'},   // an electrical breaker panel, one handle in the open positi
  {line: 'L4', id: 'p07', src: 'shorts/short310/short310_07.png', kind: 'image', motion: 'parallax'},   // anonymous gloved hands closing a heavy electrical breaker 
  {line: 'L4', id: 'p08', src: 'shorts/short310/short310_08.png', kind: 'image', motion: 'kenburns'},   // a machinery space in darkness with a single emergency ligh
  {line: 'L5', id: 'p09', src: 'shorts/short310/short310_09.png', kind: 'image', motion: 'pushin'},   // a harbour pilot's hand on a VHF radio, transmit light on
  {line: 'L5', id: 'p10', src: 'shorts/short310/short310_10.png', kind: 'image', motion: 'parallax'},   // a coastguard watch room at night, screens glowing
  {line: 'L6', id: 'p11', src: 'shorts/short310/short310_11.png', kind: 'image', motion: 'kenburns'},   // a tug boat far away across black water at night
  {line: 'L6', id: 'p12', src: 'shorts/short310/short310_12.png', kind: 'image', motion: 'pushin'},   // a patrol car parked across a closed highway ramp at night
  {line: 'L7', id: 'p13', src: 'shorts/short310/short310_13.png', kind: 'image', motion: 'parallax'},   // a voyage data recorder capsule mounted on a ship's deck
  {line: 'L8', id: 'p14', src: 'shorts/short310/short310_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a ship's bridge interior at night, instrument glow only
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT310_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT310: ShortData = {
  shortId: 'short310',
  episodeId: 'PD-2026-077-keybridge',
  durationSec: SHORT310_TOTAL_SEC,
  narrationSrc: 'shorts/short310/audio/short310_final_mix_v002_en_us.mp3',
  captions: SHORT310_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short310/short310_ctathumb.jpg',
  ctaLongTitle: "They Got the Lights Back.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short310.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short310/short310_kin_sixtyseven.webm', atSec: 6.77, durSec: 2.2, phrase: "SIXTY-SEVEN SECONDS"},
    {src: 'shorts/short310/short310_kin_did_not_matter.webm', atSec: 41.22, durSec: 2.4, phrase: "IT DID NOT MATTER"},
  ],
};
