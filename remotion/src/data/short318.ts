import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT318_CAPTIONS, SHORT318_TOTAL_SEC} from './short318_timing';

/**
 * short318 — The greasing interval grew by four hundred per cent over fifteen years, and the check that would have caught the consequences grew alongside it. Wear tracks flight hours. A calendar does not know that.
 * Episode PD-2026-079-alaska261.  Type ?.
 *
 * AVOIDS: States that an extension is not by itself evidence of anything, in the script's own words, and does not assert motive, favouritism or capture for any approval. Names no mechanic, no prosecution, no crew error. No wreckage, no victims.
 * LEAVES FOR THE LONG-FORM: There was a second line of defence, a gauge with one needle. What happened to that one?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-079-alaska261.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short318/short318_01.png', kind: 'image', motion: 'parallax', fast: true},   // a wide aircraft maintenance hangar in brilliant daylight
  {line: 'L1', id: 'p02', src: 'shorts/short318/short318_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a maintenance stand raised to an aircraft tail, work light
  {line: 'L2', id: 'p03', src: 'shorts/short318/short318_03.png', kind: 'image', motion: 'pushin', fast: true},   // a hangar tool control room in bright fluorescent light
  {line: 'L2', id: 'p04', src: 'shorts/short318/short318_04.png', kind: 'image', motion: 'parallax'},   // a machinist's bench with a part clamped in a vice
  {line: 'L3', id: 'p05', src: 'shorts/short318/short318_05.png', kind: 'image', motion: 'kenburns'},   // a hand-drawn shop sketch pinned above a bench
  {line: 'L3', id: 'p06', src: 'shorts/short318/short318_06.png', kind: 'image', motion: 'pushin'},   // the same hangar at night, sodium work lights blazing
  {line: 'L4', id: 'p07', src: 'shorts/short318/short318_07.png', kind: 'image', motion: 'parallax'},   // a three-dimensional render of a threaded screw and nut
  {line: 'L5', id: 'p08', src: 'shorts/short318/short318_08.png', kind: 'image', motion: 'kenburns'},   // two independent thread spirals rendered in contrasting col
  {line: 'L5', id: 'p09', src: 'shorts/short318/short318_09.png', kind: 'image', motion: 'pushin'},   // an airliner cruising in brilliant clear afternoon air
  {line: 'L6', id: 'p10', src: 'shorts/short318/short318_10.png', kind: 'image', motion: 'parallax'},   // an empty conference room with a long table in brilliant li
  {line: 'L7', id: 'p11', src: 'shorts/short318/short318_11.png', kind: 'image', motion: 'kenburns'},   // a large engineering exhibit board on an easel
  {line: 'L7', id: 'p12', src: 'shorts/short318/short318_12.png', kind: 'image', motion: 'pushin'},   // a public hearing room in bright daylight, a raised bench
  {line: 'L8', id: 'p13', src: 'shorts/short318/short318_13.png', kind: 'image', motion: 'parallax'},   // a precision measuring rig on a laboratory bench
  {line: 'L8', id: 'p14', src: 'shorts/short318/short318_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a mechanic seen from behind on a maintenance stand
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT318_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT318: ShortData = {
  shortId: 'short318',
  episodeId: 'PD-2026-079-alaska261',
  durationSec: SHORT318_TOTAL_SEC,
  narrationSrc: 'shorts/short318/audio/short318_final_mix_v002_en_us.mp3',
  captions: SHORT318_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short318/short318_ctathumb.jpg',
  ctaLongTitle: "It Was Measured Once.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short318.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short318/short318_kin_four_hundred.webm', atSec: 21.1, durSec: 2.2, phrase: "FOUR HUNDRED PER CENT"},
    {src: 'shorts/short318/short318_kin_calendar.webm', atSec: 33.18, durSec: 2.4, phrase: "A CALENDAR DOES NOT KNOW"},
  ],
};
