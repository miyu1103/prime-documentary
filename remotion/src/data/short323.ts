import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT323_CAPTIONS, SHORT323_TOTAL_SEC} from './short323_timing';

/**
 * short323 — NIST ran the test nobody had run in that building: the same device, the same arrangement, three materials. Non-retarded foam lit in under fifteen seconds. Fire-retarded foam did not ignite. Bare wood did not ignite.
 * Episode PD-2026-081-station.  Type ?.
 *
 * AVOIDS: Does not say the band caused the deaths or that anyone knew the foam was flammable - the Short is about what the material did, which is what the record establishes. Includes NIST's own contrary bench result, so the finding is not overstated. No charges, no victims.
 * LEAVES FOR THE LONG-FORM: If the foam was only the ignition, what was the building made of?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-081-station.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short323/short323_01.png', kind: 'image', motion: 'parallax', fast: true},   // a sheet of grey convoluted egg-crate acoustic foam
  {line: 'L1', id: 'p02', src: 'shorts/short323/short323_02.png', kind: 'image', motion: 'kenburns', fast: true},   // extreme macro of grey egg-crate acoustic foam
  {line: 'L2', id: 'p03', src: 'shorts/short323/short323_03.png', kind: 'image', motion: 'pushin', fast: true},   // a wall lined with black-painted acoustic foam
  {line: 'L2', id: 'p04', src: 'shorts/short323/short323_04.png', kind: 'image', motion: 'parallax'},   // close-up of the seam where two sheets of acoustic foam mee
  {line: 'L3', id: 'p05', src: 'shorts/short323/short323_05.png', kind: 'image', motion: 'kenburns'},   // acoustic foam glued over plywood panelling
  {line: 'L3', id: 'p06', src: 'shorts/short323/short323_06.png', kind: 'image', motion: 'pushin'},   // a can of spray paint and a sheet of foam on a bench
  {line: 'L4', id: 'p07', src: 'shorts/short323/short323_07.png', kind: 'image', motion: 'parallax'},   // sheets of plywood panelling stacked against a wall
  {line: 'L4', id: 'p08', src: 'shorts/short323/short323_08.png', kind: 'image', motion: 'kenburns'},   // rigid polystyrene insulation board fitted between studs
  {line: 'L5', id: 'p09', src: 'shorts/short323/short323_09.png', kind: 'image', motion: 'pushin'},   // an empty raised performance platform in a small venue
  {line: 'L6', id: 'p10', src: 'shorts/short323/short323_10.png', kind: 'image', motion: 'parallax'},   // a raised gypsum-board ceiling in a small venue
  {line: 'L7', id: 'p11', src: 'shorts/short323/short323_11.png', kind: 'image', motion: 'kenburns'},   // a cluster of stage par-can lights hanging on a bar
  {line: 'L7', id: 'p12', src: 'shorts/short323/short323_12.png', kind: 'image', motion: 'pushin'},   // a ceiling tile held in a gloved hand
  {line: 'L8', id: 'p13', src: 'shorts/short323/short323_13.png', kind: 'image', motion: 'parallax'},   // a roll of carpet flooring standing on end in a room
  {line: 'L8', id: 'p14', src: 'shorts/short323/short323_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // an empty small-venue interior at night
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT323_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT323: ShortData = {
  shortId: 'short323',
  episodeId: 'PD-2026-081-station',
  durationSec: SHORT323_TOTAL_SEC,
  narrationSrc: 'shorts/short323/audio/short323_final_mix_v002_en_us.mp3',
  captions: SHORT323_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short323/short323_ctathumb.jpg',
  ctaLongTitle: "Two Doors on the Outside.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short323.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short323/short323_kin_did_not_ignite.webm', atSec: 19.43, durSec: 2.2, phrase: "DID NOT IGNITE"},
    {src: 'shorts/short323/short323_kin_three_outcomes.webm', atSec: 30.79, durSec: 2.4, phrase: "THREE OUTCOMES"},
  ],
};
