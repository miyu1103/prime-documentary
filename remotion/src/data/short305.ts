import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT305_CAPTIONS, SHORT305_TOTAL_SEC} from './short305_timing';

/**
 * short305 — All sixty-three derailed tank cars were in compliance with their specification, and ninety-four per cent of them opened - compliance describes what was asked of a car on the day it was approved, not how strong it is.
 * Episode PD-2026-072-lacmegantic.  Type ?.
 *
 * AVOIDS: Blames nobody; no person appears in it at all. Does not say the crude was deliberately mislabelled - the cargo is not mentioned. Does not mention the securement, the crew, or the trial. Every figure - sixty-three, seventy-eight per cent, fifty-nine, ninety-four per cent, thirty-seven - is a verified script sentence backed by the FACTS_LEDGER.
 * LEAVES FOR THE LONG-FORM: What was in those cars, and did the shipping papers say what it really was?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-072-lacmegantic.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short305/short305_01.png', kind: 'image', motion: 'parallax', fast: true},   // a section of black tank car steel with a large torn openin
  {line: 'L1', id: 'p02', src: 'shorts/short305/short305_02.png', kind: 'image', motion: 'kenburns', fast: true},   // the flat end of a tank car, punctured, the metal folded in
  {line: 'L2', id: 'p03', src: 'shorts/short305/short305_03.png', kind: 'image', motion: 'pushin', fast: true},   // a very long line of black tank cars from a low three-quart
  {line: 'L2', id: 'p04', src: 'shorts/short305/short305_04.png', kind: 'image', motion: 'parallax'},   // the top of a tank car - walkway, dome cover, handrail - fr
  {line: 'L3', id: 'p05', src: 'shorts/short305/short305_05.png', kind: 'image', motion: 'kenburns'},   // valve fittings sheared off the top of a tank car, lying on
  {line: 'L3', id: 'p06', src: 'shorts/short305/short305_06.png', kind: 'image', motion: 'pushin'},   // a wide flat view of cleared industrial ground with rail tr
  {line: 'L4', id: 'p07', src: 'shorts/short305/short305_07.png', kind: 'image', motion: 'parallax'},   // a single sheet of paper on a clipboard on a desk, a pen be
  {line: 'L4', id: 'p08', src: 'shorts/short305/short305_08.png', kind: 'image', motion: 'kenburns'},   // a plain thick bound document lying closed on a desk
  {line: 'L5', id: 'p09', src: 'shorts/short305/short305_09.png', kind: 'image', motion: 'pushin'},   // a blank diamond-shaped placard bracket on the side of a ta
  {line: 'L6', id: 'p10', src: 'shorts/short305/short305_10.png', kind: 'image', motion: 'parallax'},   // a plain sheet of cream paper filling the frame
  {line: 'L7', id: 'p11', src: 'shorts/short305/short305_11.png', kind: 'image', motion: 'kenburns'},   // a ring binder open on a desk, pages blank
  {line: 'L7', id: 'p12', src: 'shorts/short305/short305_12.png', kind: 'image', motion: 'pushin'},   // close on a knuckle coupler joining two tank cars, slack ch
  {line: 'L8', id: 'p13', src: 'shorts/short305/short305_13.png', kind: 'image', motion: 'parallax'},   // a document storage box on a steel shelf, lid on, label are
  {line: 'L8', id: 'p14', src: 'shorts/short305/short305_14.png', kind: 'image', motion: 'kenburns'},   // a plain matte near-black surface with faint horizontal gra
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT305_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT305: ShortData = {
  shortId: 'short305',
  episodeId: 'PD-2026-072-lacmegantic',
  durationSec: SHORT305_TOTAL_SEC,
  narrationSrc: 'shorts/short305/audio/short305_final_mix_v002_en_us.mp3',
  captions: SHORT305_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short305/short305_ctathumb.jpg',
  ctaLongTitle: "A Town Burned Because Seven Hand",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short305.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short305/short305_kin_ninetyfour.webm', atSec: 31.16, durSec: 2.2, phrase: "NINETY-FOUR PER CENT"},
    {src: 'shorts/short305/short305_kin_not_moved.webm', atSec: 46.5, durSec: 2.4, phrase: "HAD NOT MOVED"},
  ],
};
