import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT296_CAPTIONS, SHORT296_TOTAL_SEC} from './short296_timing';

/**
 * short296 — Nine people on a doormat: the measured geometry of a fifty-metre sloping alley, and why the same headcount is a different kind of place in a narrow passage fed from both ends.
 * Episode PD-2026-074-itaewon.  Type ?.
 *
 * AVOIDS: No victims, no depiction of the crush, no death toll, no blame, no named person. Uses only the published measurements the film itself uses (the PLOS One study's density figures, the width and length figures); gives no gradient figure because none is published, exactly as the film refuses to. Does not say the geometry caused the deaths - it describes the mechanism the study identifies and stops there.
 * LEAVES FOR THE LONG-FORM: Who was supposed to decide, before the evening started, what that street was for on a Saturday night?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-074-itaewon.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short296/short296_01.png', kind: 'image', motion: 'parallax', fast: true},   // the narrowest point at the foot of the alley, both walls i
  {line: 'L1', id: 'p02', src: 'shorts/short296/short296_02.png', kind: 'image', motion: 'kenburns', fast: true},   // the alley's two walls converging, long lens, compressed, e
  {line: 'L2', id: 'p03', src: 'shorts/short296/short296_03.png', kind: 'image', motion: 'pushin', fast: true},   // the alley floor: drain grate, tile edging, a patched squar
  {line: 'L2', id: 'p04', src: 'shorts/short296/short296_04.png', kind: 'image', motion: 'parallax'},   // a builder's tape measure lying open across the alley floor
  {line: 'L3', id: 'p05', src: 'shorts/short296/short296_05.png', kind: 'image', motion: 'kenburns'},   // a wall at arm's length: painted breeze block, conduit, a c
  {line: 'L3', id: 'p06', src: 'shorts/short296/short296_06.png', kind: 'image', motion: 'pushin'},   // the same alley looking down from its top end, empty, morni
  {line: 'L3', id: 'p07', src: 'shorts/short296/short296_07.png', kind: 'image', motion: 'parallax'},   // the granite kerb where the alley meets the main road, worn
  {line: 'L4', id: 'p08', src: 'shorts/short296/short296_08.png', kind: 'image', motion: 'kenburns'},   // the alley's slope read from the side: a stepped kerb risin
  {line: 'L5', id: 'p09', src: 'shorts/short296/short296_09.png', kind: 'image', motion: 'pushin'},   // the same from a high angle, ordinary density, heads and sh
  {line: 'L5', id: 'p10', src: 'shorts/short296/short296_10.png', kind: 'image', motion: 'parallax'},   // a crowd from directly above: heads, shoulders, nothing els
  {line: 'L6', id: 'p11', src: 'shorts/short296/short296_11.png', kind: 'image', motion: 'kenburns'},   // the awning line overhead: PVC awnings and stacked illumina
  {line: 'L6', id: 'p12', src: 'shorts/short296/short296_12.png', kind: 'image', motion: 'pushin'},   // the building line from above: roofscape, condenser units, 
  {line: 'L7', id: 'p13', src: 'shorts/short296/short296_13.png', kind: 'image', motion: 'parallax'},   // the alley wall at arm's length, sodium and LED mixed on a 
  {line: 'L8', id: 'p14', src: 'shorts/short296/short296_14.png', kind: 'image', motion: 'kenburns'},   // steam from a wall vent crossing the empty alley, night
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT296_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT296: ShortData = {
  shortId: 'short296',
  episodeId: 'PD-2026-074-itaewon',
  durationSec: SHORT296_TOTAL_SEC,
  narrationSrc: 'shorts/short296/audio/short296_final_mix_v002_en_us.mp3',
  captions: SHORT296_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short296/short296_ctathumb.jpg',
  ctaLongTitle: "A Street Fifty Metres Long Killed 159",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short296.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short296/short296_kin_doormat.webm', atSec: 0.05, durSec: 2.2, phrase: "NINE PEOPLE ON A DOORMAT"},
    {src: 'shorts/short296/short296_kin_narrow_sloping.webm', atSec: 49.61, durSec: 2.4, phrase: "NARROW SLOPING"},
  ],
};
