import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT300_CAPTIONS, SHORT300_TOTAL_SEC} from './short300_timing';

/**
 * short300 — The hydrants-went-dry story everyone carries is wrong: the pumps never stopped and the power never failed. The pressure died because every burning house became an open tap - the system failed by working.
 * Episode PD-2026-075-lahaina.  Type ?.
 *
 * AVOIDS: Does not blame the water department or any person - the investigation's own findings that the department was seemingly in compliance and that nothing failed carry the short. No victims, no toll, no fire cause, no counterfactual about what more water would have done. All findings quoted are the script's verified sentences.
 * LEAVES FOR THE LONG-FORM: If a town's water system cannot be built to fight the town, what was supposed to stand between the town and the fire?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-075-lahaina.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short300/short300_01.png', kind: 'image', motion: 'parallax', fast: true},   // close on a fire hose coupling with only a thin stream of w
  {line: 'L1', id: 'p02', src: 'shorts/short300/short300_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a municipal fire hydrant at a kerb with a hose already con
  {line: 'L2', id: 'p03', src: 'shorts/short300/short300_03.png', kind: 'image', motion: 'pushin', fast: true},   // a charged hose line lying across scorched black ground
  {line: 'L2', id: 'p04', src: 'shorts/short300/short300_04.png', kind: 'image', motion: 'parallax'},   // an excavated trench in a road surface exposing a water mai
  {line: 'L3', id: 'p05', src: 'shorts/short300/short300_05.png', kind: 'image', motion: 'kenburns'},   // a large municipal water storage tank on a dry hillside
  {line: 'L4', id: 'p06', src: 'shorts/short300/short300_06.png', kind: 'image', motion: 'pushin'},   // an American fire engine standing at a wire farm gate on a 
  {line: 'L5', id: 'p07', src: 'shorts/short300/short300_07.png', kind: 'image', motion: 'parallax'},   // a patch of burned black ground close up under hard sun
  {line: 'L6', id: 'p08', src: 'shorts/short300/short300_08.png', kind: 'image', motion: 'kenburns'},   // a wide of burned black hillside under hard morning sun, no
  {line: 'L6', id: 'p09', src: 'shorts/short300/short300_09.png', kind: 'image', motion: 'pushin'},   // a narrow residential street with cars parked along both si
  {line: 'L7', id: 'p10', src: 'shorts/short300/short300_10.png', kind: 'image', motion: 'parallax'},   // a wide still view of a dry mountain slope above a town, un
  {line: 'L7', id: 'p11', src: 'shorts/short300/short300_11.png', kind: 'image', motion: 'kenburns'},   // a two-lane road running downhill straight toward a flat gr
  {line: 'L7', id: 'p12', src: 'shorts/short300/short300_12.png', kind: 'image', motion: 'pushin'},   // an empty fire station apparatus bay with the roller door u
  {line: 'L8', id: 'p13', src: 'shorts/short300/short300_13.png', kind: 'image', motion: 'parallax'},   // a low lava-rock wall standing alone on a cleared lot
  {line: 'L8', id: 'p14', src: 'shorts/short300/short300_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // the town now: a rebuilt two-lane street under flat overcas
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT300_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT300: ShortData = {
  shortId: 'short300',
  episodeId: 'PD-2026-075-lahaina',
  durationSec: SHORT300_TOTAL_SEC,
  narrationSrc: 'shorts/short300/audio/short300_final_mix_v002_en_us.mp3',
  captions: SHORT300_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short300/short300_ctathumb.jpg',
  ctaLongTitle: "The Sirens Had Never Been Used...",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short300.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short300/short300_kin_uninterrupted.webm', atSec: 18.31, durSec: 2.2, phrase: "UNINTERRUPTED POWER"},
    {src: 'shorts/short300/short300_kin_thousand_holes.webm', atSec: 44.05, durSec: 2.4, phrase: "A THOUSAND HOLES"},
  ],
};
