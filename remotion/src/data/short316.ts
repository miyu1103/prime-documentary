import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT316_CAPTIONS, SHORT316_TOTAL_SEC} from './short316_timing';

/**
 * short316 — The entire safety net was a number in thousandths of an inch read off a needle - and before July 2000, a reading inside the limits was not required to be recorded anywhere. Pass, and the number went nowhere.
 * Episode PD-2026-079-alaska261.  Type ?.
 *
 * AVOIDS: Names no mechanic and says nothing about what the September 1997 check measured, because no number exists. Does not say anyone was prosecuted, does not blame the crew, does not quote the end play figures that belong to other carriers' assemblies. No wreckage, no victims.
 * LEAVES FOR THE LONG-FORM: If nobody could see a number creeping up, what was happening to the threads between the checks?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-079-alaska261.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short316/short316_01.png', kind: 'image', motion: 'parallax', fast: true},   // extreme macro of a dial indicator gauge, the needle at res
  {line: 'L1', id: 'p02', src: 'shorts/short316/short316_02.png', kind: 'image', motion: 'kenburns', fast: true},   // extreme macro of a dial indicator face, needle deflected
  {line: 'L2', id: 'p03', src: 'shorts/short316/short316_03.png', kind: 'image', motion: 'pushin', fast: true},   // a huge steel acme-threaded jackscrew standing vertical
  {line: 'L2', id: 'p04', src: 'shorts/short316/short316_04.png', kind: 'image', motion: 'parallax'},   // an acme nut cut away to show its internal spiral threads
  {line: 'L3', id: 'p05', src: 'shorts/short316/short316_05.png', kind: 'image', motion: 'kenburns'},   // a dial indicator mounted on a bracket with its plunger set
  {line: 'L3', id: 'p06', src: 'shorts/short316/short316_06.png', kind: 'image', motion: 'pushin'},   // a steel restraining fixture clamped to an aircraft structu
  {line: 'L4', id: 'p07', src: 'shorts/short316/short316_07.png', kind: 'image', motion: 'parallax'},   // a torque wrench lying on a steel bench under a hard lamp
  {line: 'L4', id: 'p08', src: 'shorts/short316/short316_08.png', kind: 'image', motion: 'kenburns'},   // extreme macro of a coarse acme thread on a steel screw
  {line: 'L5', id: 'p09', src: 'shorts/short316/short316_09.png', kind: 'image', motion: 'pushin'},   // a blank ruled work card lying on a hangar bench
  {line: 'L6', id: 'p10', src: 'shorts/short316/short316_10.png', kind: 'image', motion: 'parallax'},   // a clipboard of blank ruled sheets hanging on a hangar wall
  {line: 'L7', id: 'p11', src: 'shorts/short316/short316_11.png', kind: 'image', motion: 'kenburns'},   // a wall of pigeonholes holding blank work cards
  {line: 'L7', id: 'p12', src: 'shorts/short316/short316_12.png', kind: 'image', motion: 'pushin'},   // a maintenance records office in brilliant daylight
  {line: 'L8', id: 'p13', src: 'shorts/short316/short316_13.png', kind: 'image', motion: 'parallax'},   // close-up of unmarked binder spines packed on a shelf
  {line: 'L8', id: 'p14', src: 'shorts/short316/short316_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a round wall clock in an airline maintenance office
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT316_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT316: ShortData = {
  shortId: 'short316',
  episodeId: 'PD-2026-079-alaska261',
  durationSec: SHORT316_TOTAL_SEC,
  narrationSrc: 'shorts/short316/audio/short316_final_mix_v002_en_us.mp3',
  captions: SHORT316_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short316/short316_ctathumb.jpg',
  ctaLongTitle: "It Was Measured Once.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short316.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short316/short316_kin_safety_net.webm', atSec: 27.23, durSec: 2.2, phrase: "THE ENTIRE SAFETY NET"},
    {src: 'shorts/short316/short316_kin_nobody_had_to.webm', atSec: 46.28, durSec: 2.4, phrase: "NOBODY HAD TO"},
  ],
};
