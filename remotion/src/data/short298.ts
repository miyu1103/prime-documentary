import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT298_CAPTIONS, SHORT298_TOTAL_SEC} from './short298_timing';

/**
 * short298 — The largest outdoor siren warning system in the world, tested in public every month, with wildfires on its own published list - and in its whole life it had never once been used to warn of a fire.
 * Episode PD-2026-075-lahaina.  Type ?.
 *
 * AVOIDS: Never says sounding the sirens would have saved anyone - it closes on the investigation's own refusal of that counterfactual, quoted verbatim. Names no official; the emergency management administrator, his quote and his resignation are left to the long-form. Does not raise the cause of the fire. All figures (four hundred sirens, finding numbers, eighty-four findings) are the script's own verified sentences.
 * LEAVES FOR THE LONG-FORM: The man who decided not to sound them was asked if he regretted it - his answer was three words. What were they?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-075-lahaina.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short298/short298_01.png', kind: 'image', motion: 'parallax', fast: true},   // a galvanised steel pole carrying a cluster of grey civil-d
  {line: 'L1', id: 'p02', src: 'shorts/short298/short298_02.png', kind: 'image', motion: 'kenburns', fast: true},   // extreme close-up of the cluster of grey painted steel sire
  {line: 'L2', id: 'p03', src: 'shorts/short298/short298_03.png', kind: 'image', motion: 'pushin', fast: true},   // monthly test: a warning siren pole against an empty pale s
  {line: 'L2', id: 'p04', src: 'shorts/short298/short298_04.png', kind: 'image', motion: 'parallax'},   // monthly test: the identical framing of the siren pole unde
  {line: 'L3', id: 'p05', src: 'shorts/short298/short298_05.png', kind: 'image', motion: 'kenburns'},   // the published page: a plain sheet of white paper squared o
  {line: 'L3', id: 'p06', src: 'shorts/short298/short298_06.png', kind: 'image', motion: 'pushin'},   // a large plain wall map of an island group in a municipal o
  {line: 'L4', id: 'p07', src: 'shorts/short298/short298_07.png', kind: 'image', motion: 'parallax'},   // monthly test: the identical framing of the siren pole with
  {line: 'L4', id: 'p08', src: 'shorts/short298/short298_08.png', kind: 'image', motion: 'kenburns'},   // the concrete base of a warning siren pole with a locked gr
  {line: 'L5', id: 'p09', src: 'shorts/short298/short298_09.png', kind: 'image', motion: 'pushin'},   // a warning siren pole standing beside a two-lane coastal ro
  {line: 'L6', id: 'p10', src: 'shorts/short298/short298_10.png', kind: 'image', motion: 'parallax'},   // four warning siren poles standing in a line along a coasta
  {line: 'L6', id: 'p11', src: 'shorts/short298/short298_11.png', kind: 'image', motion: 'kenburns'},   // a plain matte near-black surface with faint horizontal gra
  {line: 'L7', id: 'p12', src: 'shorts/short298/short298_12.png', kind: 'image', motion: 'pushin'},   // the same galvanised siren pole and horn cluster, framed as
  {line: 'L8', id: 'p13', src: 'shorts/short298/short298_13.png', kind: 'image', motion: 'parallax'},   // a thick bound document lying open on a desk
  {line: 'L8', id: 'p14', src: 'shorts/short298/short298_14.png', kind: 'image', motion: 'kenburns'},   // a plain municipal meeting room with a long table and stack
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT298_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT298: ShortData = {
  shortId: 'short298',
  episodeId: 'PD-2026-075-lahaina',
  durationSec: SHORT298_TOTAL_SEC,
  narrationSrc: 'shorts/short298/audio/short298_final_mix_v002_en_us.mp3',
  captions: SHORT298_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short298/short298_ctathumb.jpg',
  ctaLongTitle: "The Sirens Had Never Been Used for a",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short298.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short298/short298_kin_one_siren.webm', atSec: 27.82, durSec: 2.2, phrase: "ONE OPERABLE"},
    {src: 'shorts/short298/short298_kin_never_once.webm', atSec: 40.42, durSec: 2.4, phrase: "NOT THAT DAY EVER"},
  ],
};
