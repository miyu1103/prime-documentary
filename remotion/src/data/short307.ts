import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT307_CAPTIONS, SHORT307_TOTAL_SEC} from './short307_timing';

/**
 * short307 — A recommendation is a document; a standard is a rule with a mechanism behind it. The 2011 winterization recommendation stayed a document for ten years, and when the weather came back, machines failed above their own rated design temperature.
 * Episode PD-2026-073-uri.  Type ?.
 *
 * AVOIDS: Does not say winterization was legally required before 2021 - the whole Short is about the fact that it was not. Names no individual, company or fuel type; the units that failed are machines, not plants of any kind. Does not mention the deaths. The eleven-years figure and the design-temperature finding are the script's verified sentences.
 * LEAVES FOR THE LONG-FORM: Why does Texas answer to nobody outside its borders on this - and what did that cost on the night the order was given?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-073-uri.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short307/short307_01.png', kind: 'image', motion: 'parallax', fast: true},   // a thick bound report lying closed on a desk, cover entirel
  {line: 'L1', id: 'p02', src: 'shorts/short307/short307_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a single sheet of paper on a desk, blank, held flat
  {line: 'L2', id: 'p03', src: 'shorts/short307/short307_03.png', kind: 'image', motion: 'pushin', fast: true},   // a Texas street under thin snow at dawn, ten years older in
  {line: 'L2', id: 'p04', src: 'shorts/short307/short307_04.png', kind: 'image', motion: 'parallax'},   // steam and rime on the pipework of a power station in freez
  {line: 'L3', id: 'p05', src: 'shorts/short307/short307_05.png', kind: 'image', motion: 'kenburns'},   // a plain federal-style hearing room, rows of chairs and a l
  {line: 'L3', id: 'p06', src: 'shorts/short307/short307_06.png', kind: 'image', motion: 'pushin'},   // pipe lagging being wrapped around an outdoor pipe run, glo
  {line: 'L4', id: 'p07', src: 'shorts/short307/short307_07.png', kind: 'image', motion: 'parallax'},   // a row of ring binders on a steel shelf, spines blank
  {line: 'L4', id: 'p08', src: 'shorts/short307/short307_08.png', kind: 'image', motion: 'kenburns'},   // a training room with a projector, blank screen and rows of
  {line: 'L5', id: 'p09', src: 'shorts/short307/short307_09.png', kind: 'image', motion: 'pushin'},   // an empty chair pushed back from a meeting table in a plain
  {line: 'L6', id: 'p10', src: 'shorts/short307/short307_10.png', kind: 'image', motion: 'parallax'},   // a straight road across flat land in ordinary weather, rece
  {line: 'L7', id: 'p11', src: 'shorts/short307/short307_11.png', kind: 'image', motion: 'kenburns'},   // a manufacturer's blank metal data plate riveted to a machi
  {line: 'L7', id: 'p12', src: 'shorts/short307/short307_12.png', kind: 'image', motion: 'pushin'},   // an outdoor valve assembly on a plant site, no ice, ordinar
  {line: 'L8', id: 'p13', src: 'shorts/short307/short307_13.png', kind: 'image', motion: 'parallax'},   // new pipe insulation on outdoor plant pipework, clean and r
  {line: 'L8', id: 'p14', src: 'shorts/short307/short307_14.png', kind: 'image', motion: 'kenburns'},   // new heat-trace cable and cladding on a wellhead, clean
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT307_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT307: ShortData = {
  shortId: 'short307',
  episodeId: 'PD-2026-073-uri',
  durationSec: SHORT307_TOTAL_SEC,
  narrationSrc: 'shorts/short307/audio/short307_final_mix_v002_en_us.mp3',
  captions: SHORT307_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short307/short307_ctathumb.jpg',
  ctaLongTitle: "246 Died in the Texas Freeze. The",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short307.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short307/short307_kin_declined.webm', atSec: 28.1, durSec: 2.2, phrase: "DECLINED TO ACT"},
    {src: 'shorts/short307/short307_kin_eleven_years.webm', atSec: 55.08, durSec: 2.4, phrase: "ELEVEN YEARS"},
  ],
};
