import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT327_CAPTIONS, SHORT327_TOTAL_SEC} from './short327_timing';

/**
 * short327 — The Board listed four things it could find no reasonable explanation for. The third: a crewmember's performance rated partly on willingness to work overtime. The fourth: the workloads went up after the grounding.
 * Episode PD-2026-082-valdez.  Type ?.
 *
 * AVOIDS: States the Board's word foreseeable with the Board's own meaning - the category of accident was visible from where the decision was made, not that anyone predicted this grounding. Does not say Exxon was uniquely bad, does not state a probable cause, says nothing about the master.
 * LEAVES FOR THE LONG-FORM: Nineteen years later a second institution found the same shape from different evidence. What did it write down?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-082-valdez.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short327/short327_01.png', kind: 'image', motion: 'parallax', fast: true},   // three officers' caps hanging in a row
  {line: 'L1', id: 'p02', src: 'shorts/short327/short327_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a brass ship's bell polished bright
  {line: 'L2', id: 'p03', src: 'shorts/short327/short327_03.png', kind: 'image', motion: 'pushin', fast: true},   // a terminal control room in daylight, mimic board
  {line: 'L2', id: 'p04', src: 'shorts/short327/short327_04.png', kind: 'image', motion: 'parallax'},   // a small ship's cabin desk with a blank notepad
  {line: 'L3', id: 'p05', src: 'shorts/short327/short327_05.png', kind: 'image', motion: 'kenburns'},   // a ship's bridge at dawn with brilliant orange light
  {line: 'L4', id: 'p06', src: 'shorts/short327/short327_06.png', kind: 'image', motion: 'pushin'},   // a ship's bridge at midday in brilliant grey light
  {line: 'L4', id: 'p07', src: 'shorts/short327/short327_07.png', kind: 'image', motion: 'parallax'},   // an engine room control station with banks of panels
  {line: 'L5', id: 'p08', src: 'shorts/short327/short327_08.png', kind: 'image', motion: 'kenburns'},   // a ship's mess room with fixed tables
  {line: 'L6', id: 'p09', src: 'shorts/short327/short327_09.png', kind: 'image', motion: 'pushin'},   // a ship's galley at night, stainless steel
  {line: 'L6', id: 'p10', src: 'shorts/short327/short327_10.png', kind: 'image', motion: 'parallax'},   // a valve wheel on a tanker deck at night
  {line: 'L7', id: 'p11', src: 'shorts/short327/short327_11.png', kind: 'image', motion: 'kenburns'},   // a steel cabin door with a name-card holder
  {line: 'L7', id: 'p12', src: 'shorts/short327/short327_12.png', kind: 'image', motion: 'pushin'},   // a ship's cabin interior, a bunk turned back
  {line: 'L8', id: 'p13', src: 'shorts/short327/short327_13.png', kind: 'image', motion: 'parallax'},   // a gangway from a quay to a tanker's deck
  {line: 'L8', id: 'p14', src: 'shorts/short327/short327_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a wide Alaskan sound at dusk, spruce shore
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT327_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT327: ShortData = {
  shortId: 'short327',
  episodeId: 'PD-2026-082-valdez',
  durationSec: SHORT327_TOTAL_SEC,
  narrationSrc: 'shorts/short327/audio/short327_final_mix_v002_en_us.mp3',
  captions: SHORT327_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short327/short327_ctathumb.jpg',
  ctaLongTitle: "The Number on the Paper Had Not Moved.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short327.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short327/short327_kin_foreseeable.webm', atSec: 17.12, durSec: 2.2, phrase: "FORESEEABLE"},
    {src: 'shorts/short327/short327_kin_went_up_after.webm', atSec: 48.45, durSec: 2.4, phrase: "WENT UP AFTER"},
  ],
};
