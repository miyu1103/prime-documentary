import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT299_CAPTIONS, SHORT299_TOTAL_SEC} from './short299_timing';

/**
 * short299 — The first evacuation order naming a Lahaina neighbourhood went out at sixteen minutes past four - naming a shelter that had closed two hours earlier, sent to cellphones in a region where the cellular network had been destroyed before noon.
 * Episode PD-2026-075-lahaina.  Type ?.
 *
 * AVOIDS: Does not blame any named person or the county in the film's own voice - the county's own defence (fourteen alerts sent, all twenty-one towers failed, its suit against the carriers is left to the long-form) is spoken in its position sentence. No counterfactual about what an earlier or different alert would have done. No fire cause. No victims, no toll.
 * LEAVES FOR THE LONG-FORM: What was the warning system the state had actually built for this - and why had it never been used?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-075-lahaina.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short299/short299_01.png', kind: 'image', motion: 'parallax', fast: true},   // a hand holding an ordinary smartphone at waist height
  {line: 'L1', id: 'p02', src: 'shorts/short299/short299_02.png', kind: 'image', motion: 'kenburns', fast: true},   // close on a smartphone held in one hand, screen dark and bl
  {line: 'L2', id: 'p03', src: 'shorts/short299/short299_03.png', kind: 'image', motion: 'pushin', fast: true},   // the shelter door: closed double doors of a municipal hall 
  {line: 'L2', id: 'p04', src: 'shorts/short299/short299_04.png', kind: 'image', motion: 'parallax'},   // the interior of a municipal recreation hall with folded co
  {line: 'L3', id: 'p05', src: 'shorts/short299/short299_05.png', kind: 'image', motion: 'kenburns'},   // the clock ground: unpainted concrete wall shot straight on
  {line: 'L4', id: 'p06', src: 'shorts/short299/short299_06.png', kind: 'image', motion: 'pushin'},   // a lattice communications mast on a ridge with smoke crossi
  {line: 'L4', id: 'p07', src: 'shorts/short299/short299_07.png', kind: 'image', motion: 'parallax'},   // thick grey-brown smoke rolling low across a road surface
  {line: 'L5', id: 'p08', src: 'shorts/short299/short299_08.png', kind: 'image', motion: 'kenburns'},   // a rack of storage servers behind a mesh door in a plain ma
  {line: 'L5', id: 'p09', src: 'shorts/short299/short299_09.png', kind: 'image', motion: 'pushin'},   // an emergency operations workstation seen over a shoulder, 
  {line: 'L6', id: 'p10', src: 'shorts/short299/short299_10.png', kind: 'image', motion: 'parallax'},   // an emergency operations workstation seen over a shoulder, 
  {line: 'L6', id: 'p11', src: 'shorts/short299/short299_11.png', kind: 'image', motion: 'kenburns'},   // a plain municipal operations room with rows of desks
  {line: 'L7', id: 'p12', src: 'shorts/short299/short299_12.png', kind: 'image', motion: 'pushin'},   // the town on an ordinary bright weekday, low-rise Pacific s
  {line: 'L8', id: 'p13', src: 'shorts/short299/short299_13.png', kind: 'image', motion: 'parallax'},   // a small mains-powered radio standing on a kitchen counter
  {line: 'L8', id: 'p14', src: 'shorts/short299/short299_14.png', kind: 'image', motion: 'kenburns'},   // a plain matte near-black surface with faint horizontal gra
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT299_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT299: ShortData = {
  shortId: 'short299',
  episodeId: 'PD-2026-075-lahaina',
  durationSec: SHORT299_TOTAL_SEC,
  narrationSrc: 'shorts/short299/audio/short299_final_mix_v002_en_us.mp3',
  captions: SHORT299_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short299/short299_ctathumb.jpg',
  ctaLongTitle: "The Sirens Had Never Been Used for a",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short299.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short299/short299_kin_shelter_closed.webm', atSec: 12.89, durSec: 2.2, phrase: "ALREADY CLOSED"},
    {src: 'shorts/short299/short299_kin_network_died.webm', atSec: 50.15, durSec: 2.4, phrase: "ALREADY DIED"},
  ],
};
