import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT295_CAPTIONS, SHORT295_TOTAL_SEC} from './short295_timing';

/**
 * short295 — Four hours before anything happened, a caller standing in the alley used the words crushed to death and asked for somebody to come and control it. Eleven calls came; four were attended; the question is what happens to a warning after somebody makes it.
 * Episode PD-2026-074-itaewon.  Type ?.
 *
 * AVOIDS: No depiction of the crush, no victims, no toll, no bodies, no CPR, no memorials. Does not say the police did nothing - states that officers were sent and dispersed crowds, and gives the four-of-eleven split with its attribution (Al Jazeera) spoken aloud. Does not name or characterise any caller beyond the published translations the film itself uses.
 * LEAVES FOR THE LONG-FORM: Where were the officers instead that night - and why did the official answer take three years?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-074-itaewon.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short295/short295_01.png', kind: 'image', motion: 'parallax', fast: true},   // somebody on a phone at the kerb from behind: shoulder, ear
  {line: 'L1', id: 'p02', src: 'shorts/short295/short295_02.png', kind: 'image', motion: 'kenburns', fast: true},   // hands holding phones up above head height, faces out of fr
  {line: 'L2', id: 'p03', src: 'shorts/short295/short295_03.png', kind: 'image', motion: 'pushin', fast: true},   // the mouth of the alley seen from across the main road, tra
  {line: 'L2', id: 'p04', src: 'shorts/short295/short295_04.png', kind: 'image', motion: 'parallax'},   // a main-road pavement in the evening, a crowd flowing past,
  {line: 'L3', id: 'p05', src: 'shorts/short295/short295_05.png', kind: 'image', motion: 'kenburns'},   // the alley from the bottom looking up, ordinary evening, ba
  {line: 'L3', id: 'p06', src: 'shorts/short295/short295_06.png', kind: 'image', motion: 'pushin'},   // the alley ordinarily busy, mid-evening, a dozen people wal
  {line: 'L4', id: 'p07', src: 'shorts/short295/short295_07.png', kind: 'image', motion: 'parallax'},   // backs of heads under a run of vertical sign boxes
  {line: 'L4', id: 'p08', src: 'shorts/short295/short295_08.png', kind: 'image', motion: 'kenburns'},   // an office window at night, blinds half open, lit from with
  {line: 'L5', id: 'p09', src: 'shorts/short295/short295_09.png', kind: 'image', motion: 'pushin'},   // a high-visibility vest at distance in a crowd, back turned
  {line: 'L5', id: 'p10', src: 'shorts/short295/short295_10.png', kind: 'image', motion: 'parallax'},   // two high-visibility figures at the far end of a street
  {line: 'L6', id: 'p11', src: 'shorts/short295/short295_11.png', kind: 'image', motion: 'kenburns'},   // a hand holding a two-way radio, no marking on it
  {line: 'L7', id: 'p12', src: 'shorts/short295/short295_12.png', kind: 'image', motion: 'pushin'},   // the back of a high-visibility figure at a junction, night
  {line: 'L8', id: 'p13', src: 'shorts/short295/short295_13.png', kind: 'image', motion: 'parallax'},   // the alley in rain at night, empty, reflections doubling th
  {line: 'L8', id: 'p14', src: 'shorts/short295/short295_14.png', kind: 'image', motion: 'kenburns'},   // the alley at dusk, empty, sign boxes coming on, all letter
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT295_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT295: ShortData = {
  shortId: 'short295',
  episodeId: 'PD-2026-074-itaewon',
  durationSec: SHORT295_TOTAL_SEC,
  narrationSrc: 'shorts/short295/audio/short295_final_mix_v002_en_us.mp3',
  captions: SHORT295_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short295/short295_ctathumb.jpg',
  ctaLongTitle: "A Street Fifty Metres Long Killed 159",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short295.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short295/short295_kin_eleven_reports.webm', atSec: 25.46, durSec: 2.2, phrase: "ELEVEN REPORTS"},
    {src: 'shorts/short295/short295_kin_no_action.webm', atSec: 39.82, durSec: 2.4, phrase: "NO ACTION"},
  ],
};
