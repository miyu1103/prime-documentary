import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT319_CAPTIONS, SHORT319_TOTAL_SEC} from './short319_timing';

/**
 * short319 — The investigators ran the alternative timeline themselves: signal at 22:39, boats at thirteen degrees of heel, everyone off before the ship reached thirty. Their own report says all of them could have reached safety.
 * Episode PD-2026-080-concordia.  Type ?.
 *
 * AVOIDS: Names nobody, touches no charge or trial, and does not say the crew failed - the modelling is the investigators' own work and is spoken as theirs. Does not say the passengers could not have been saved; the whole Short is the opposite finding. Uses only the 4,229/3,206/1,023 figures.
 * LEAVES FOR THE LONG-FORM: The order came sixty-nine minutes after the rock. What used up the sixty-nine minutes?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-080-concordia.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short319/short319_01.png', kind: 'image', motion: 'parallax', fast: true},   // extreme macro of a brass ship's clinometer
  {line: 'L1', id: 'p02', src: 'shorts/short319/short319_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a brass clinometer on a bulkhead, the pendulum settled off
  {line: 'L2', id: 'p03', src: 'shorts/short319/short319_03.png', kind: 'image', motion: 'pushin', fast: true},   // a ship's bridge at night photographed square on
  {line: 'L2', id: 'p04', src: 'shorts/short319/short319_04.png', kind: 'image', motion: 'parallax'},   // a paper navigation chart spread on a lit chart table
  {line: 'L3', id: 'p05', src: 'shorts/short319/short319_05.png', kind: 'image', motion: 'kenburns'},   // an Italian harbour breakwater at night, a green light
  {line: 'L3', id: 'p06', src: 'shorts/short319/short319_06.png', kind: 'image', motion: 'pushin'},   // a wide Mediterranean coastline at night
  {line: 'L4', id: 'p07', src: 'shorts/short319/short319_07.png', kind: 'image', motion: 'parallax'},   // a muster station marked out on an open cruise ship deck
  {line: 'L4', id: 'p08', src: 'shorts/short319/short319_08.png', kind: 'image', motion: 'kenburns'},   // a muster station sign board mounted on a bulkhead
  {line: 'L5', id: 'p09', src: 'shorts/short319/short319_09.png', kind: 'image', motion: 'pushin'},   // an orange enclosed lifeboat hanging level in its davits
  {line: 'L5', id: 'p10', src: 'shorts/short319/short319_10.png', kind: 'image', motion: 'parallax'},   // a lifeboat davit mechanism in close-up
  {line: 'L6', id: 'p11', src: 'shorts/short319/short319_11.png', kind: 'image', motion: 'kenburns'},   // rows of passengers in orange lifejackets standing at a sta
  {line: 'L7', id: 'p12', src: 'shorts/short319/short319_12.png', kind: 'image', motion: 'pushin'},   // a cruise ship promenade deck in brilliant daylight
  {line: 'L8', id: 'p13', src: 'shorts/short319/short319_13.png', kind: 'image', motion: 'parallax'},   // a calm Mediterranean sea at dusk, an orange sky
  {line: 'L8', id: 'p14', src: 'shorts/short319/short319_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // an orange lifejacket hanging alone on an open door
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT319_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT319: ShortData = {
  shortId: 'short319',
  episodeId: 'PD-2026-080-concordia',
  durationSec: SHORT319_TOTAL_SEC,
  narrationSrc: 'shorts/short319/audio/short319_final_mix_v002_en_us.mp3',
  captions: SHORT319_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short319/short319_ctathumb.jpg',
  ctaLongTitle: "Sixty-Nine Minutes to Give the Order.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short319.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short319/short319_kin_all_of_them.webm', atSec: 37.7, durSec: 2.2, phrase: "ALL OF THEM COULD HAVE"},
    {src: 'shorts/short319/short319_kin_thirtytwo.webm', atSec: 46.7, durSec: 2.3, phrase: "THIRTY-TWO INSTEAD DIED"},
  ],
};
