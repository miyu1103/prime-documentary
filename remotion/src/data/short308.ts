import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT308_CAPTIONS, SHORT308_TOTAL_SEC} from './short308_timing';

/**
 * short308 — The largest manually controlled load shedding in American history was ordered at twenty past one in the morning, it was correct, and it was not enough: the frequency sat below the line for four minutes and twenty-three seconds against a nine-minute threshold.
 * Episode PD-2026-073-uri.  Type ?.
 *
 * AVOIDS: Never says the grid collapsed - the Short's spine is the script's own distinction: load was shed so that the failure would not happen, and the margin is stated as the measured figure against the measured threshold. Names no individual; the operator is an office, not a person. No fuel type is mentioned. No deaths are mentioned.
 * LEAVES FOR THE LONG-FORM: The customers cut that night included the gas fields that fuelled the plants being saved. What happens when a grid's rescue feeds its emergency?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-073-uri.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short308/short308_01.png', kind: 'image', motion: 'parallax', fast: true},   // a utility control room at night, banks of screens on a cur
  {line: 'L1', id: 'p02', src: 'shorts/short308/short308_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a hand resting beside a desk telephone on a control-room c
  {line: 'L2', id: 'p03', src: 'shorts/short308/short308_03.png', kind: 'image', motion: 'pushin', fast: true},   // a utility control room at night with several operators at 
  {line: 'L2', id: 'p04', src: 'shorts/short308/short308_04.png', kind: 'image', motion: 'parallax'},   // a desk telephone handset lifted, a hand and a forearm
  {line: 'L3', id: 'p05', src: 'shorts/short308/short308_05.png', kind: 'image', motion: 'kenburns'},   // a plain analogue wall clock in an operations room, hands n
  {line: 'L4', id: 'p06', src: 'shorts/short308/short308_06.png', kind: 'image', motion: 'pushin'},   // a wall of unlabelled line-graph displays in a dim control 
  {line: 'L5', id: 'p07', src: 'shorts/short308/short308_07.png', kind: 'image', motion: 'parallax'},   // one screen filling the frame, a single line descending
  {line: 'L5', id: 'p08', src: 'shorts/short308/short308_08.png', kind: 'image', motion: 'kenburns'},   // a single line drawn across a dark screen with a second lin
  {line: 'L6', id: 'p09', src: 'shorts/short308/short308_09.png', kind: 'image', motion: 'pushin'},   // a suburban street at night in which the near half is lit a
  {line: 'L6', id: 'p10', src: 'shorts/short308/short308_10.png', kind: 'image', motion: 'parallax'},   // a dead traffic signal over an empty snow-dusted intersecti
  {line: 'L7', id: 'p11', src: 'shorts/short308/short308_11.png', kind: 'image', motion: 'kenburns'},   // a substation seen through a chain-link fence, transformers
  {line: 'L7', id: 'p12', src: 'shorts/short308/short308_12.png', kind: 'image', motion: 'pushin'},   // a close layered view through an electrical substation, ice
  {line: 'L8', id: 'p13', src: 'shorts/short308/short308_13.png', kind: 'image', motion: 'parallax'},   // a single screen in a dark room showing an unlabelled desce
  {line: 'L8', id: 'p14', src: 'shorts/short308/short308_14.png', kind: 'image', motion: 'kenburns'},   // a plain near-black surface with faint horizontal grain
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT308_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT308: ShortData = {
  shortId: 'short308',
  episodeId: 'PD-2026-073-uri',
  durationSec: SHORT308_TOTAL_SEC,
  narrationSrc: 'shorts/short308/audio/short308_final_mix_v002_en_us.mp3',
  captions: SHORT308_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short308/short308_ctathumb.jpg',
  ctaLongTitle: "246 Died in the Texas Freeze. The",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short308.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short308/short308_kin_four_twentythree.webm', atSec: 32.11, durSec: 2.2, phrase: "FOUR MINUTES TWENTY-THREE SECONDS"},
    {src: 'shorts/short308/short308_kin_the_margin.webm', atSec: 50.58, durSec: 2.4, phrase: "FOUR MINUTES THIRTY-SEVEN SECONDS"},
  ],
};
