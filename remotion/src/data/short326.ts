import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT326_CAPTIONS, SHORT326_TOTAL_SEC} from './short326_timing';

/**
 * short326 — A lookout is a person whose whole job is to look at the water and say what is on it. On this ship the position routinely went unattended - because taking it away costs nothing, almost every time.
 * Episode PD-2026-082-valdez.  Type ?.
 *
 * AVOIDS: Blames no crew member; the Board's word routinely is quoted as its own. Does not state a probable cause and does not say the grounding was caused by the lookout being away. Says nothing about the master or alcohol.
 * LEAVES FOR THE LONG-FORM: The company had its own rule about two officers on the bridge. What did the Board say about relying on it?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-082-valdez.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short326/short326_01.png', kind: 'image', motion: 'parallax', fast: true},   // a 1989-era ship's bridge photographed square on
  {line: 'L1', id: 'p02', src: 'shorts/short326/short326_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a helm and rudder angle indicator on a 1989 bridge
  {line: 'L2', id: 'p03', src: 'shorts/short326/short326_03.png', kind: 'image', motion: 'pushin', fast: true},   // a ship's bridge at night with the windows dark
  {line: 'L2', id: 'p04', src: 'shorts/short326/short326_04.png', kind: 'image', motion: 'parallax'},   // a radar display glowing bright green
  {line: 'L3', id: 'p05', src: 'shorts/short326/short326_05.png', kind: 'image', motion: 'kenburns'},   // a narrow ship's alleyway with cabin doors
  {line: 'L3', id: 'p06', src: 'shorts/short326/short326_06.png', kind: 'image', motion: 'pushin'},   // a steel stairway aboard ship going down
  {line: 'L4', id: 'p07', src: 'shorts/short326/short326_07.png', kind: 'image', motion: 'parallax'},   // a ship's bow rail at night with the lookout position empty
  {line: 'L4', id: 'p08', src: 'shorts/short326/short326_08.png', kind: 'image', motion: 'kenburns'},   // the same bow rail with a figure standing at it
  {line: 'L5', id: 'p09', src: 'shorts/short326/short326_09.png', kind: 'image', motion: 'pushin'},   // a watch schedule chalked on a blackboard
  {line: 'L6', id: 'p10', src: 'shorts/short326/short326_10.png', kind: 'image', motion: 'parallax'},   // a pair of binoculars resting on a bridge console
  {line: 'L7', id: 'p11', src: 'shorts/short326/short326_11.png', kind: 'image', motion: 'kenburns'},   // a ship's bridge wing at night, an open door
  {line: 'L7', id: 'p12', src: 'shorts/short326/short326_12.png', kind: 'image', motion: 'pushin'},   // floating glacial ice on dark water at night
  {line: 'L8', id: 'p13', src: 'shorts/short326/short326_13.png', kind: 'image', motion: 'parallax'},   // a navigation buoy with a flashing light
  {line: 'L8', id: 'p14', src: 'shorts/short326/short326_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // coastal Alaska at night, snow on spruce
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT326_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT326: ShortData = {
  shortId: 'short326',
  episodeId: 'PD-2026-082-valdez',
  durationSec: SHORT326_TOTAL_SEC,
  narrationSrc: 'shorts/short326/audio/short326_final_mix_v002_en_us.mp3',
  captions: SHORT326_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short326/short326_ctathumb.jpg',
  ctaLongTitle: "The Number on the Paper Had Not Moved.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short326.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short326/short326_kin_routinely.webm', atSec: 29.44, durSec: 1.47, phrase: "ROUTINELY. NOT ONCE."},
    {src: 'shorts/short326/short326_kin_first_taken.webm', atSec: 44.64, durSec: 2.4, phrase: "THE FIRST THING TAKEN AWAY"},
  ],
};
