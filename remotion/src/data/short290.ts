import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT290_CAPTIONS, SHORT290_TOTAL_SEC} from './short290_timing';

/**
 * short290 — He navigated to the wrong house with his own GPS, steered by a car that belonged to the family he was about to raid.
 * Episode PD-2026-070-wronghouse.  Type ?.
 *
 * AVOIDS: No child, no injury, no depiction of the entry. Does not say the agent lied or destroyed evidence: the GPS sentence is the Supreme Court's, and the film's own refusal to explain it is kept in.
 * LEAVES FOR THE LONG-FORM: Everything he saw confirmed he was at the right house. What was he looking at?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-070-wronghouse.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short290/short290_01.png', kind: 'image', motion: 'parallax', fast: true},   // W083 approved episode plate
  {line: 'L1', id: 'p02', src: 'shorts/short290/short290_02.png', kind: 'image', motion: 'kenburns', fast: true},   // W084 approved episode plate
  {line: 'L2', id: 'p03', src: 'shorts/short290/short290_03.png', kind: 'image', motion: 'pushin', fast: true},   // W085 approved episode plate
  {line: 'L2', id: 'p04', src: 'shorts/short290/short290_04.png', kind: 'image', motion: 'parallax'},   // W086 approved episode plate
  {line: 'L3', id: 'p05', src: 'shorts/short290/short290_05.png', kind: 'image', motion: 'kenburns'},   // W087 approved episode plate
  {line: 'L3', id: 'p06', src: 'shorts/short290/short290_06.png', kind: 'image', motion: 'pushin'},   // W088 approved episode plate
  {line: 'L4', id: 'p07', src: 'shorts/short290/short290_07.png', kind: 'image', motion: 'parallax'},   // W089 approved episode plate
  {line: 'L4', id: 'p08', src: 'shorts/short290/short290_08.png', kind: 'image', motion: 'kenburns'},   // W090 approved episode plate
  {line: 'L5', id: 'p09', src: 'shorts/short290/short290_09.png', kind: 'image', motion: 'pushin'},   // W091 approved episode plate
  {line: 'L6', id: 'p10', src: 'shorts/short290/short290_10.png', kind: 'image', motion: 'parallax'},   // W092 approved episode plate
  {line: 'L6', id: 'p11', src: 'shorts/short290/short290_11.png', kind: 'image', motion: 'kenburns'},   // W093 approved episode plate
  {line: 'L7', id: 'p12', src: 'shorts/short290/short290_12.png', kind: 'image', motion: 'pushin'},   // W019 approved episode plate
  {line: 'L8', id: 'p13', src: 'shorts/short290/short290_13.png', kind: 'image', motion: 'parallax'},   // W083 approved episode plate
  {line: 'L8', id: 'p14', src: 'shorts/short290/short290_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // W084 approved episode plate
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT290_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT290: ShortData = {
  shortId: 'short290',
  episodeId: 'PD-2026-070-wronghouse',
  durationSec: SHORT290_TOTAL_SEC,
  narrationSrc: 'shorts/short290/audio/short290_final_mix_v002_en_us.mp3',
  captions: SHORT290_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short290/short290_ctathumb.jpg',
  ctaLongTitle: "No Rule To Break",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short290.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short290/short290_kin_his_own.webm', atSec: 9.53, durSec: 2.2, phrase: "HIS OWN GPS"},
    {src: 'shorts/short290/short290_kin_their_car.webm', atSec: 39.18, durSec: 2.2, phrase: "THEIR CAR"},
  ],
};
