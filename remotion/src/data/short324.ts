import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT324_CAPTIONS, SHORT324_TOTAL_SEC} from './short324_timing';

/**
 * short324 — An occupant limit is arithmetic tied to the exits - a statement about the doors. The number was raised from 253 to 317 by removing tables and chairs, which genuinely creates floor space. The doors did not change.
 * Episode PD-2026-081-station.  Type ?.
 *
 * AVOIDS: Does not say the limit was illegal, that the fire department was negligent, or that anyone broke a rule - the mechanism is described as reasonable, in the script's own words, before its limitation is named. No charges, no convictions, no occupant figure stated as established fact.
 * LEAVES FOR THE LONG-FORM: If the constraint that kills is the door, what did the door on this building actually measure?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-081-station.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short324/short324_01.png', kind: 'image', motion: 'parallax', fast: true},   // a wall-mounted occupancy placard frame beside a door
  {line: 'L1', id: 'p02', src: 'shorts/short324/short324_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a blank ruled municipal form lying on a desk
  {line: 'L2', id: 'p03', src: 'shorts/short324/short324_03.png', kind: 'image', motion: 'pushin', fast: true},   // an architect's scale rule and pencil on a light table
  {line: 'L2', id: 'p04', src: 'shorts/short324/short324_04.png', kind: 'image', motion: 'parallax'},   // a tape measure and a folded floor plan lying together
  {line: 'L3', id: 'p05', src: 'shorts/short324/short324_05.png', kind: 'image', motion: 'kenburns'},   // a close-up of a floor plan on a lit drafting table
  {line: 'L4', id: 'p06', src: 'shorts/short324/short324_06.png', kind: 'image', motion: 'pushin'},   // a typewritten memorandum lying on a desk
  {line: 'L4', id: 'p07', src: 'shorts/short324/short324_07.png', kind: 'image', motion: 'parallax'},   // a small-town fire department office in daylight
  {line: 'L5', id: 'p08', src: 'shorts/short324/short324_08.png', kind: 'image', motion: 'kenburns'},   // an empty lounge area of a small club with the tables in pl
  {line: 'L5', id: 'p09', src: 'shorts/short324/short324_09.png', kind: 'image', motion: 'pushin'},   // the same lounge area with tables and chairs removed
  {line: 'L6', id: 'p10', src: 'shorts/short324/short324_10.png', kind: 'image', motion: 'parallax'},   // a stack of tables and chairs pushed against a wall
  {line: 'L7', id: 'p11', src: 'shorts/short324/short324_11.png', kind: 'image', motion: 'kenburns'},   // a floor plan with coloured density shading
  {line: 'L7', id: 'p12', src: 'shorts/short324/short324_12.png', kind: 'image', motion: 'pushin'},   // a municipal records office in brilliant daylight
  {line: 'L8', id: 'p13', src: 'shorts/short324/short324_13.png', kind: 'image', motion: 'parallax'},   // a side exit door on the outside of a wood-shingled wall
  {line: 'L8', id: 'p14', src: 'shorts/short324/short324_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a second side exit door on the opposite wall
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT324_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT324: ShortData = {
  shortId: 'short324',
  episodeId: 'PD-2026-081-station',
  durationSec: SHORT324_TOTAL_SEC,
  narrationSrc: 'shorts/short324/audio/short324_final_mix_v002_en_us.mp3',
  captions: SHORT324_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short324/short324_ctathumb.jpg',
  ctaLongTitle: "Two Doors on the Outside.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short324.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short324/short324_kin_about_doors.webm', atSec: 15.77, durSec: 2.2, phrase: "A STATEMENT ABOUT DOORS"},
    {src: 'shorts/short324/short324_kin_the_door.webm', atSec: 51.05, durSec: 2.4, phrase: "NOT THE FLOOR. THE DOOR."},
  ],
};
