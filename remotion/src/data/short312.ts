import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT312_CAPTIONS, SHORT312_TOTAL_SEC} from './short312_timing';

/**
 * short312 — The Board's quieter finding: the bridge had no countermeasures against a modern ship, the owner was likely unaware the risk applied to them, and sixty-eight other American bridges have never had the sum done.
 * Episode PD-2026-077-keybridge.  Type ?.
 *
 * AVOIDS: Does not say Maryland knew and ignored the guidance - it states the Board's own finding that they were likely unaware. Does not say the sixty-eight bridges are unsafe; the script's own qualification is spoken. Names no defendant, states no allegation, shows no collapse.
 * LEAVES FOR THE LONG-FORM: Who is supposed to do that arithmetic for the bridge you drove over this morning?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-077-keybridge.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short312/short312_01.png', kind: 'image', motion: 'parallax', fast: true},   // a harbour channel at dusk seen from far off, a bridge span
  {line: 'L1', id: 'p02', src: 'shorts/short312/short312_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a bridge pier rising out of black water, floodlit
  {line: 'L2', id: 'p03', src: 'shorts/short312/short312_03.png', kind: 'image', motion: 'pushin', fast: true},   // a sheet-pile and concrete dolphin structure beside a bridg
  {line: 'L2', id: 'p04', src: 'shorts/short312/short312_04.png', kind: 'image', motion: 'parallax'},   // a highway bridge seen from beneath, girders receding
  {line: 'L3', id: 'p05', src: 'shorts/short312/short312_05.png', kind: 'image', motion: 'kenburns'},   // an empty state transport authority office at night
  {line: 'L3', id: 'p06', src: 'shorts/short312/short312_06.png', kind: 'image', motion: 'pushin'},   // a single filing drawer pulled open in a dark records room
  {line: 'L4', id: 'p07', src: 'shorts/short312/short312_07.png', kind: 'image', motion: 'parallax'},   // a stack of engineering binders on a shelf, spines blank
  {line: 'L4', id: 'p08', src: 'shorts/short312/short312_08.png', kind: 'image', motion: 'kenburns'},   // a calculator and a mechanical pencil on a drawing of struc
  {line: 'L5', id: 'p09', src: 'shorts/short312/short312_09.png', kind: 'image', motion: 'pushin'},   // a wall-mounted map of a harbour with shipping channels mar
  {line: 'L5', id: 'p10', src: 'shorts/short312/short312_10.png', kind: 'image', motion: 'parallax'},   // an engineer's scale model of a bridge pier on a desk under
  {line: 'L6', id: 'p11', src: 'shorts/short312/short312_11.png', kind: 'image', motion: 'kenburns'},   // a row of identical container ships at anchor outside a por
  {line: 'L7', id: 'p12', src: 'shorts/short312/short312_12.png', kind: 'image', motion: 'pushin'},   // a long line of bridges receding into haze along a coastlin
  {line: 'L8', id: 'p13', src: 'shorts/short312/short312_13.png', kind: 'image', motion: 'parallax'},   // an empty lit carriageway running away across a long bridge
  {line: 'L8', id: 'p14', src: 'shorts/short312/short312_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a car's headlights crossing an empty bridge deck at dawn
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT312_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT312: ShortData = {
  shortId: 'short312',
  episodeId: 'PD-2026-077-keybridge',
  durationSec: SHORT312_TOTAL_SEC,
  narrationSrc: 'shorts/short312/audio/short312_final_mix_v002_en_us.mp3',
  captions: SHORT312_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short312/short312_ctathumb.jpg',
  ctaLongTitle: "They Got the Lights Back.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short312.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short312/short312_kin_out_of_date.webm', atSec: 33.7, durSec: 2.2, phrase: "ARITHMETIC OUT OF DATE"},
    {src: 'shorts/short312/short312_kin_sixtyeight.webm', atSec: 38.71, durSec: 2.4, phrase: "SIXTY-EIGHT BRIDGES"},
  ],
};
