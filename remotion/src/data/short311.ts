import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT311_CAPTIONS, SHORT311_TOTAL_SEC} from './short311_timing';

/**
 * short311 — The National Transportation Safety Board took twenty months to name the probable cause, and it is a label band that held one wire a fraction of a millimetre short of its clamp.
 * Episode PD-2026-077-keybridge.  Type ?.
 *
 * AVOIDS: States the Board's probable cause and nothing beyond it. Does not touch the indictment, the flushing pump, the companies or Nair - the contested second half belongs to the long-form. No allegation, no blame, no victim, no collision.
 * LEAVES FOR THE LONG-FORM: The wire explains the first blackout. Why did a ship built to restart itself not come back?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-077-keybridge.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short311/short311_01.png', kind: 'image', motion: 'parallax', fast: true},   // extreme macro of a terminal block with a spring-clamp gate
  {line: 'L1', id: 'p02', src: 'shorts/short311/short311_02.png', kind: 'image', motion: 'kenburns', fast: true},   // a terminal rail with dozens of identical blocks receding, 
  {line: 'L2', id: 'p03', src: 'shorts/short311/short311_03.png', kind: 'image', motion: 'pushin', fast: true},   // a public hearing room with a raised dais and empty seats
  {line: 'L2', id: 'p04', src: 'shorts/short311/short311_04.png', kind: 'image', motion: 'parallax'},   // a microphone on a hearing-room table, water glass beside i
  {line: 'L3', id: 'p05', src: 'shorts/short311/short311_05.png', kind: 'image', motion: 'kenburns'},   // a high-voltage switchboard cabinet, one green lamp lit
  {line: 'L3', id: 'p06', src: 'shorts/short311/short311_06.png', kind: 'image', motion: 'pushin'},   // a silent propeller shaft tunnel, the shaft stationary
  {line: 'L4', id: 'p07', src: 'shorts/short311/short311_07.png', kind: 'image', motion: 'parallax'},   // a technician's hands with a small screwdriver at a termina
  {line: 'L5', id: 'p08', src: 'shorts/short311/short311_08.png', kind: 'image', motion: 'kenburns'},   // extreme macro of a plastic labelling band wrapped around a
  {line: 'L5', id: 'p09', src: 'shorts/short311/short311_09.png', kind: 'image', motion: 'pushin'},   // extreme macro of the wire held a hair short of the clamp g
  {line: 'L6', id: 'p10', src: 'shorts/short311/short311_10.png', kind: 'image', motion: 'parallax'},   // an electrical test meter's probe tips touching a terminal
  {line: 'L7', id: 'p11', src: 'shorts/short311/short311_11.png', kind: 'image', motion: 'kenburns'},   // a ship's machinery space under investigators' portable lig
  {line: 'L7', id: 'p12', src: 'shorts/short311/short311_12.png', kind: 'image', motion: 'pushin'},   // an evidence table with anonymous machine components on bro
  {line: 'L8', id: 'p13', src: 'shorts/short311/short311_13.png', kind: 'image', motion: 'parallax'},   // a row of four marine diesel generators receding down a mac
  {line: 'L8', id: 'p14', src: 'shorts/short311/short311_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // an emergency generator unit starting in a dark compartment
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT311_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT311: ShortData = {
  shortId: 'short311',
  episodeId: 'PD-2026-077-keybridge',
  durationSec: SHORT311_TOTAL_SEC,
  narrationSrc: 'shorts/short311/audio/short311_final_mix_v002_en_us.mp3',
  captions: SHORT311_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short311/short311_ctathumb.jpg',
  ctaLongTitle: "They Got the Lights Back.",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short311.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short311/short311_kin_a_wire.webm', atSec: 2.74, durSec: 2.2, phrase: "IT WAS A WIRE"},
    {src: 'shorts/short311/short311_kin_whole_thing.webm', atSec: 37.09, durSec: 1.38, phrase: "THAT IS THE WHOLE THING"},
  ],
};
