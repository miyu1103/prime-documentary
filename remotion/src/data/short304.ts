import type {ShortBeat, ShortData} from '../compositions/Short';
import {LINE_WINDOWS, SHORT304_CAPTIONS, SHORT304_TOTAL_SEC} from './short304_timing';

/**
 * short304 — The securement check was done, and done as understood - but it was performed with the independent brakes still active, so the thing holding the train during the test was not the thing being tested. A check that cannot fail protects nobody.
 * Episode PD-2026-072-lacmegantic.  Type ?.
 *
 * AVOIDS: Names none of the three acquitted men and blames no individual - the hook line is the script's own negation, this was not a man skipping a check. The seven hand brakes are given against both the railway's own nine and the investigation's seventeen-to-twenty-six range, never alone. Does not mention single-person operation. The forty-seven dead appear only in the film's own summary sentence.
 * LEAVES FOR THE LONG-FORM: Who was standing in that yard at the end of the shift, and what happened to the engine two hours later?
 *
 * GENERATED BY scripts/assemble_short.py from the design at
 * episodes/_planning/short_designs/PD-2026-072-lacmegantic.design.v001.json — edit the design, not this file.
 *
 * The funnel card is the last thing on screen and holds to the final frame: Shorts loops on its
 * own, and conversion is the measured bottleneck (0.77 subs/1000 views on Shorts against 3.67 on
 * long-form), so the destination is worth more than a seamless loop seam.
 */

type Cut = {line: string; id: string; src: string; kind: 'image' | 'video';
  motion: ShortBeat['motion']; fast?: boolean; isCta?: boolean; rewind?: boolean};

const r3 = (n: number) => Math.round(n * 1000) / 1000;

const CUTS: Cut[] = [
  {line: 'L1', id: 'p01', src: 'shorts/short304/short304_01.png', kind: 'image', motion: 'parallax', fast: true},   // a hand-brake wheel on a tank car end platform, three-quart
  {line: 'L1', id: 'p02', src: 'shorts/short304/short304_02.png', kind: 'image', motion: 'kenburns', fast: true},   // the standing train from the side, still, at night, absolut
  {line: 'L2', id: 'p03', src: 'shorts/short304/short304_03.png', kind: 'image', motion: 'pushin', fast: true},   // the brake stand in a locomotive cab: two handles, gauges a
  {line: 'L2', id: 'p04', src: 'shorts/short304/short304_04.png', kind: 'image', motion: 'parallax'},   // a round brass-rimmed air pressure gauge on a locomotive co
  {line: 'L3', id: 'p05', src: 'shorts/short304/short304_05.png', kind: 'image', motion: 'kenburns'},   // two hands gripping a hand-brake wheel and turning it
  {line: 'L3', id: 'p06', src: 'shorts/short304/short304_06.png', kind: 'image', motion: 'pushin'},   // the same wheel from directly above, the chain and rod visi
  {line: 'L4', id: 'p07', src: 'shorts/short304/short304_07.png', kind: 'image', motion: 'parallax'},   // a hand-brake wheel in the extreme foreground, the line of 
  {line: 'L5', id: 'p08', src: 'shorts/short304/short304_08.png', kind: 'image', motion: 'kenburns'},   // a plain ring-bound operating manual lying open on a desk
  {line: 'L6', id: 'p09', src: 'shorts/short304/short304_09.png', kind: 'image', motion: 'pushin'},   // close on couplers between two cars with the slack run out
  {line: 'L6', id: 'p10', src: 'shorts/short304/short304_10.png', kind: 'image', motion: 'parallax'},   // close on a locomotive control stand: throttle lever, rever
  {line: 'L7', id: 'p11', src: 'shorts/short304/short304_11.png', kind: 'image', motion: 'kenburns'},   // a locomotive consist standing still on the main track at n
  {line: 'L7', id: 'p12', src: 'shorts/short304/short304_12.png', kind: 'image', motion: 'pushin'},   // a figure walking away along the ballast beside a line of t
  {line: 'L8', id: 'p13', src: 'shorts/short304/short304_13.png', kind: 'image', motion: 'parallax'},   // a line of black tank cars receding into darkness
  {line: 'L8', id: 'p14', src: 'shorts/short304/short304_14.png', kind: 'image', motion: 'kenburns', isCta: true, rewind: true, fast: true},   // a small town at night seen from a low hill above it
];

const buildBeats = (): ShortBeat[] => {
  const beats: ShortBeat[] = [];
  LINE_WINDOWS.forEach((win, i) => {
    const start = win.start;
    const end = i + 1 < LINE_WINDOWS.length ? LINE_WINDOWS[i + 1].start : SHORT304_TOTAL_SEC;
    const cuts = CUTS.filter((c) => c.line === win.id);
    // The card needs ~2.5 s to be read, but METHOD rule 6 caps any held frame at 2 s and a
    // weight-based share breaks that whenever a line carries few cuts: at weight 1.9 the card
    // ran 3.20 s on short94 and 2.63 s on short90, while short93 sat at 0.83 s. Give it a fixed
    // 1.9 s and let the other cuts of that line share what is left, so the ceiling holds
    // whatever the cut count is.
    // Capping the card at a fixed 1.9 s left 7.8 s of L5 with NO beat at all on short87 - the
    // screen went black under the captions. A line window must always be covered end to end.
    // So: the card takes at most 1.9 s of MOTION, then holds the same beat to the end of the
    // line rather than handing the remainder to nobody. Every second of every window is owned.
    const span = end - start;
    const nCta = cuts.filter((c) => c.isCta).length;
    const others = cuts.length - nCta;
    // when the card is the only cut in its line it simply takes the whole window
    const ctaDur = others === 0 ? span : Math.min(1.9, span / cuts.length * 2);
    const rest = others > 0 ? (span - ctaDur * nCta) / others : 0;
    let cursor = start;
    cuts.forEach((c) => {
      const dur = c.isCta ? ctaDur : rest;
      const {line: _l, isCta, ...keep} = c;
      beats.push({...keep, id: isCta ? 'cta' : c.id, startSec: r3(cursor), durSec: r3(dur)});
      cursor += dur;
    });
  });
  return beats;
};

export const SHORT304: ShortData = {
  shortId: 'short304',
  episodeId: 'PD-2026-072-lacmegantic',
  durationSec: SHORT304_TOTAL_SEC,
  narrationSrc: 'shorts/short304/audio/short304_final_mix_v002_en_us.mp3',
  captions: SHORT304_CAPTIONS,
  bgmSrc: null,
  ctaTextYT: 'Watch the full case on the channel',
  ctaTextTT: 'Full case on our profile',
  ctaLongThumbSrc: 'shorts/short304/short304_ctathumb.jpg',
  ctaLongTitle: "A Town Burned Because Seven Hand",
  ctaHeadline: 'FULL CASE',
  captionTop: 1210,
  ctaFadeOutSec: 0.8,
  beats: buildBeats(),
  // Mid-roll kinetic type, built in After Effects (runs/ae_jobs/short304.json) and installed
  // into this Short's public directory by scripts/ae/render_beats.sh. Words are taken
  // verbatim from the narration line each beat sits on.
  kineticBeats: [
    {src: 'shorts/short304/short304_kin_not_tested.webm', atSec: 37.2, durSec: 2.2, phrase: "NOT THE THING BEING TESTED"},
    {src: 'shorts/short304/short304_kin_seven_brakes.webm', atSec: 45.64, durSec: 2.4, phrase: "SEVEN HAND BRAKES"},
  ],
};
