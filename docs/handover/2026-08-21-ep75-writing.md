# 2026-08-21 — EP75 lahaina: the whole left process

**Scope of this session was the FILM BIBLE and the script. The owner then said continue, so the
image order, the paste files, the thumbnails, the scene plan and the fact_recheck packet were built
as well. All six items of spec v3 §5 now exist (§8).** Two things happened that
were not on the plan and that changed the film, and they are the first two sections here.

Everything below is measured. Where a number appears, the command that produced it is next to it.

---

## 1. Phase One was read, and it is why this film has an afternoon

The previous session marked `SRC-0009` — the FSRI *Lahaina Fire Comprehensive Timeline Report*
(Phase One, 17 April 2024) — **NOT YET READ**, and named it in `AB-05` as the largest open hole in
the record. It was the right call to name it. It was also the difference between a film and a lecture:
Phase Two gives you 84 analytical findings, and **none of them is a clock**. Phase One is nothing but
a clock.

It has now been read in full text, and `EP75_lahaina_FACTS_LEDGER.v002.md` §11 carries 55 new rows
from it.

**How, so nobody spends that hour again.** `ag.hawaii.gov` 403s. UL Research Institutes also deposits
the report on figshare, which does not:

```
curl -sSL "https://api.figshare.com/v2/articles/28062371" -o fig.json    # -> download_url
curl -sSL -A "Mozilla/5.0 (... Chrome/126.0)" \
     "https://ndownloader.figshare.com/files/51306806" -o phase1.pdf     # 132 MB, 199 pages
```

**The trap, and it is a good one.** Most of the body text uses an embedded font encoding that makes
pypdf return every character **shifted down by 29 code points**. `ACKNOWLEDGMENTS` extracts as
`$&.12:/('*0(176`. A space extracts as `\x03`. And **every digit extracts as a control character**, so
a naive read of a 199-page timeline report shows `"leading to  fatalities"` and `"reported at  p.m."` —
a document that appears to contain no numbers at all. It would be very easy to skim that, conclude the
extraction failed, and go back to secondary sources. The decoder is eight lines (`p1_decode.py`, in
this session's scratchpad; the shift is +29, applied per line, leaving already-plain lines alone).

**The five things in it that the film is now built on**, all `VERBATIM`:

| row | fact |
|---|---|
| LH-250 | "High winds crippled the communications infrastructure, **destroying cellular phone communication within the Lahaina region in the late morning of August 8, 2023**" |
| LH-255 | The **first** evacuation order in MEMA's own public-alert log naming a Lahaina subdivision is timestamped **16:16** — an hour and twenty-one minutes after the fire was reported, and hours after the phones died |
| LH-220/221 | The morning crews "reported that the fire was extinguished and returned to quarters at **14:17**." The afternoon fire was reported at **14:55**, at the same place. **Thirty-eight minutes** |
| LH-290/292 | "The only missing EOC sign-in sheet is the one for MEMA personnel for August 8, 2023. Maui County has not produced this document after multiple requests." … "Because of missing data, it is difficult to make a complete and accurate accounting of activities within the EOC" |
| LH-263 | ~16:45: a locked gate, 30–50 cars trapped behind it, a resident's reciprocating saw, and an officer who "rammed his body into the cyclone gate to open the other side." 30–50 fully loaded cars went through |

`LH-205` also retires half of `AB-06`: the film may now say **"forecast gusts of up to sixty miles an
hour,"** because that is primary and dated. It still may not say what the wind actually *did* — no
measured wind speed exists in this ledger.

---

## 2. ⛔-11 fired, and section 9 of the ledger was already wrong

The ledger required section 9 to be re-verified on the day of writing. It was, and **it had gone
stale in the eight days since it was written.**

v001 said the first of four annual settlement payments "was expected to begin flowing in July or
August 2026." Measured 2026-08-21 against two fetched sources:

- **No payment has been made.** Three attorneys and one fire survivor are challenging the ruling on
  attorneys' fees; filed 2 July 2026 with the Intermediate Court of Appeals, elevated to the **Hawaiʻi
  Supreme Court on 5 August 2026**. *"While the appeal is pending, no settlement payments can be
  made."* (Honolulu Star-Advertiser, 16 August 2026 — `SRC-0013`, fetched and read.)
- ~$1.1 billion is in trust for the first instalment. 94,816 claims by 21,750 claimants.
- v001's framing that the February 2026 ruling removed "the last legal obstacle" is **not correct** and
  was never correct — counsel said at the time that two other appeals were already pending.

**`LH-101…LH-103` of v001 may not be narrated.** v002 §10 replaces them with `LH-110…LH-117`, and the
script states the status **procedurally and with its date attached** ("As of August 2026, no payment
has been made"), so that it ages into a true sentence rather than a false one.

**This is not discharged.** ⛔-11 is satisfied for 2026-08-21 only. The court could rule in September.
**Re-verify §10 again before the packaging is written, and again before the video is scheduled.**

---

## 3. What was written

| file | what it is |
|---|---|
| `episodes/_planning/EP75_lahaina_FACTS_LEDGER.v002.md` | 55 Phase One rows, the corrected §10, revised `AB-05`/`AB-06`, new `AB-08`/`AB-09`, and four new quarantine rules `⛔-14…⛔-17`. **Binding together with v001**, which is not withdrawn |
| `episodes/_planning/EP75_lahaina_FILM_BIBLE.v001.md` | 17 sections, EP72 structure. Controlling idea, five hero objects, substitution table, five-act arc, beat map (77 beats), retention map, the hook written first, motion budget in numbers, producibility ceilings, 24 people plates, kinetic beats, caption breaks, packaging direction, and nine things a human must confirm |
| `episodes/_planning/EP75_lahaina_script.en.v001.md` | The script. 220 spoken lines, **every one of them carrying a ledger row id** |
| `scripts/gen_narration_case.py` | EP75 registered, then `design_speech_seconds` rewritten from the measurement |

**The controlling idea**, which is not the one the brief started with:

> A warning system is not the equipment you own. It is the thing you are in the habit of using — and
> this one's habit was the sea.

The brief's line was "the sirens did not sound." The film does not say that, because two different
sources say two different things and merging them produces a claim neither one makes (⛔-02, ⛔-03).
What the film says instead is the contradiction the record actually supports: the largest outdoor
warning network in the world, tested on the first business day of every month, with **wildfires on its
own published hazard list** — one operable siren inside the burn perimeter, never once used for a
fire in the whole life of the network, and the warning that *was* sent going out as a text message at
16:16 to a region whose phones had died that morning.

---

## 4. Measured — the length, and the pace the model got wrong

`--measure-section ACT_1` was run (54 chunks, ~$1.40, ElevenLabs is standing-approved):

```
MEASURED section=ACT_1 chunks=54 words=808
  speech    259.133s   raw 187.1 wpm
  finished  275.033s   176.3 words per finished minute
```

**187.1 raw wpm.** The registry's 171.79 model is **8.9 % slow** for this register; EP72 measured
191.1. A script sized on the model would have finished about three minutes short.

The script was then written to that number, not to the model:

| | |
|---|---|
| dry run | **349 chunks / 5,321 words** (band `[4900, 5400]`) |
| speech | 1,706.4 s |
| gaps | 117.0 s (348 beat @ 0.30 s, 7 section boundaries @ 1.8 s, no scripted silence) |
| **film with 9.0 s endcard** | **1,832.4 s = 30:32** |
| band | `[1740, 1920]` — **92 s of headroom low, 88 s high** |

Cross-checked against the finished-minute rate: 5,321 / 176.3 + 12.6 + 9.0 = 1,832.5 s. **The two
models agree to 0.1 s.** `design_speech_seconds` is now 1706.4 and carries the whole derivation in its
own comment. **Do not re-measure and do not re-size.**

The target to beat was EP72's delivered 29:44.

---

## 5. Gates run, honestly

| check | result |
|---|---|
| `check_episode_spec.py --slug lahaina` | **exit 0** — valid |
| `check_script_craft.py` (against ledger v002, `--wpm 187.1`) | **PASS, every mechanical gate green.** 0 emotion commands, 0 AI-smell phrases, 0 spoken CTA, 0 questions, 3.20 you/1000w (limit 8), 20.9 % short-punch (band 20–35), longest bare stretch 20.8 s (limit 90), 10.93 specific sentences/min (floor 5), **0 quarantined claims used** |
| citation integrity (ad-hoc) | **220 of 220 spoken lines carry a citation comment. 106 distinct row ids cited, all 106 exist in v001 or v002.** Zero orphans |
| banned-phrase sweep | 0 hits on *some say / many believe / questions remain / we may never know / the official story / paradise / tragedy / disaster*. The three `failed` hits are all about **water pipes**, quoting Finding 24 — the sirens are never described as having failed (⛔-03) |
| `verify_script_lint.py` / `verify_script_structure.py` | **SKIP — and they are right to skip.** §6 |

The craft gate first came back with one WARN (short-punch 19.4 % against a 20 % floor). Three long
sentences were split rather than padded; it is 20.9 % now, and the register is better for it.

---

## 6. A real defect found, not fixed, and deliberately not papered over

`verify_script_lint.py` and `verify_script_structure.py` both returned **SKIP** on this episode.
Spec v3 §2.4 already calls `script_structure` decoration — 0 pass, 0 fail, 8 skips, **never once
executed**. Measured here, the cause is concrete and it is not a path bug:

- both look for `episodes/<EPID>/03_script/script.en.v*.md`;
- **no episode from PD-2026-062 onward has a `03_script` directory at all** — the authoring
  convention since then is `_planning/EPnn_<slug>_script.en.vNNN.md`;
- and the lint extracts spoken text from `[VO:]` lines, which this convention does not use.

So the two gates are looking for an artefact shape the channel stopped producing about thirteen
episodes ago. **They were not made to pass by copying the script into `03_script`** — that would
create a second copy of the script that drifts, and it would convert an honest SKIP into a green that
means nothing. `check_script_craft.py` reads the file where it actually lives, and it is the gate that
ran.

**The fix belongs to whoever next touches the acceptance code**: point both at
`_planning/EP*_<slug>_script.en.v*.md`, and teach the lint to strip `<!-- … -->` and `【…】` instead of
looking for `[VO:]`. That is the same defect as spec v3 §6.6 (`check_script_length` counting citation
comments as spoken words), and both are one function.

---

## 7. The image order — written, and both mandated checks are green

**`EP75_lahaina_CODEX_BATCH_A.v001.md`: 132 plates, `H001`–`H132`.** Built to the EP72 order format,
against FILM BIBLE §10 (register ceilings) and §11 (people).

| measured | |
|---|---|
| plate rows | **132**, ids contiguous `H001`–`H132` |
| people plates carrying a figure **in the table** | **24** — meets `people_plates_min` **without depending on the five variants** |
| §5 id list vs the `P` flags | **identical**, zero mismatch |
| plates carrying the RECONSTRUCTION label | **122 of 132** — `R` is the default here: a real event three years old, invariant 11 |
| `check_image_order_neg.py` | **ok — all five required token families present** |
| `check_prompt_diversity.py` | **PASS** — 0 dup-pairs, 0 raw-dup-pairs, 0 generic |
| spec `forbidden_subjects` scanned against all 132 prompt cells | **zero hits** |

Four light states rather than EP72's two, because this film is one day: `W` wind-day, `S` smoke,
`N` night, `O` overcast-after. **The single most important instruction in the order is that the
afternoon is GREY, not orange** — fire photography defaults to a warm hero light, and that light would
falsify the most important visual fact in the film.

`[NEG]` keeps *human face*, *facial features*, *eye contact* and *headshot* **out**, deliberately.
Faces are wanted; identifiability is barred. Seven plates whose roles map to real named individuals
stay as backs, hands and silhouettes: `H055 H082 H083 H103 H104 H105 H121`.

### 7.5 The diversity gate was decoration, and now it is not

Running the check the manual mandates found that **`check_prompt_diversity.py` could not read a modern
order at all.** It parses the EP53-era code-fence format, and **EP72's own order returns "found 0 rows"
on it too.** EP72 §7 tells the next agent to run it; it would have printed a format error and been
read as noise.

Two fixes, both demonstrated:

1. **A markdown-table parser was added as a fallback** — the fence parser is untouched and still wins
   where a file carries both. EP75 now extracts 132 prompts and EP72 extracts 120. **EP53 norfolk, in
   the old format, still parses at 232 and still passes** — no regression. (EP39 frazier is a *third*
   format and still fails to extract. That is pre-existing: the change only adds a fallback, so it
   cannot reduce any file's row count.)
2. **Then the gate itself was caught lying.** CLAUDE.md §4.6 requires a check to be shown rejecting a
   deliberately bad input before it is relied on, so one was built: **30 identical prompts.** It
   returned **`RESULT: PASS`, 0 dup-pairs.** The cause is that `--boilerplate-df 0.30` drops every
   token appearing in more than 30 % of prompts — so when the corpus is homogeneous, *every shared
   token is discarded as boilerplate* and the duplicate detector has nothing left to compare. The same
   file with `--boilerplate-df 1.1` returns **435 pairs at Jaccard 1.00**, which proves the detector
   was sound and the filter was blinding it.
   **Fix: a second comparison over the unfiltered tokens at a deliberately high bar (raw Jaccard
   ≥ 0.85), which now also fails the run.** The bad input now returns
   `FAIL (0 dup-pairs, 435 raw-dup-pairs)`. EP75 and EP53 still pass; EP72 still fails on the one real
   pair it always had.

**Why the EP75 PASS is not an artefact of the same hole:** the run prints
`boilerplate tokens dropped: 0 (df>30%)` — no token in this order crossed the threshold, so the filter
removed nothing and the Jaccard ran on full content.

**One finding handed on rather than acted on:** EP72's order has a real near-duplicate pair,
`L025 ~ L043`, both "locomotive cab brake stand, no person". That episode's plates may already exist,
so its order was not edited from this session.

## 8. The rest of the left process — all six items of spec v3 §5 now exist

| spec v3 §5 | file | state |
|---|---|---|
| 1. FILM BIBLE + script | `EP75_lahaina_FILM_BIBLE.v001.md`, `EP75_lahaina_script.en.v001.md` | ✅ §3–§5 |
| 2. SCENE_PLAN | `EP75_lahaina_SCENE_PLAN.v001.md` | ✅ §8.2 |
| 3. image order | `EP75_lahaina_CODEX_BATCH_A.v001.md` + `EP75_lahaina_CODEX_PASTE/` | ✅ §7, §8.1 |
| 4. thumb prompts | `EP75_lahaina_thumb_prompts.v001.md` | ✅ §8.3 |
| 5. fact_recheck | `PD-2026-075-lahaina/01_research/fact_recheck.v001.md` | ✅ §8.4 |
| 6. valid episode_spec | `episode_spec.v002.json` | ✅ §8.5 |

### 8.1 Paste files — `EP75_lahaina_CODEX_PASTE/`

**17 batch files of 8 plates each, plus `plates.v001.jsonl`.** Round-trip verified: 132 records,
ids contiguous, **every plate appears in exactly one batch file, exactly once**, 24 people plates in
the jsonl.

They were produced by a new **generic** script, `scripts/export_codex_batch_paste.py`, not by hand
and not by a per-episode one-off. It reads any order in the EP72-onward table shape and emits the
house paste format. **Proof that it is generic rather than EP75-shaped: EP72's order runs through it
unchanged** — 120 plates, 15 batches, 16 people plates, matching that episode's own §5.

It is **not** a second implementation of `export_codex_prompts.py`. That one serves the SHORTS
pipeline: different inputs (`short_designs/`, `SHORTS_MOTIF_VOCABULARY`), different outputs
(`short<NN>_<nn>.png`), no notion of `[STYLE]`/`[NEG]` macros or plate flags. The docstring says so.

**One thing measured and then deliberately not changed:** the plate prefix `H` collides with EP69
hyatt, which already uses `H001.png`. It was checked rather than assumed —
`check_cross_episode_reuse.py` states in its own docstring that **identity is by CONTENT, not by
name**, and prefix reuse is already the norm (`P` across 11 episodes, `S` across 23). The images live
in different directories. **No rename.**

### 8.2 Scene plan — and the one way it differs from EP72's

EP72's scene plan was written *after* its master existed, off `narration_index.v001.json`.
**EP75 has no master, so every timestamp in its plan is a projection** off the one measured section,
and the plan says so in a warning at the top of the file rather than quietly.

The arithmetic it proves: **132 plates ≤ 154 stills ceiling**, and **265 distinct video assets cover
327 video cuts at 1.23× reuse**. Figure beats fall 14 / 16 / 17 / 15 / 16 across the acts — inside
`figure_beats_per_act` [13, 17] at both edges, with ACT_3 **at the ceiling**, so nothing may be added
there.

**Item 1 of that plan's §5 is "re-derive §2 from the narration index."** It is item 1 for a reason.

### 8.3 Packaging — titles measured, not chosen

Four titles and four thumbnail headlines were each run through `check_packaging_claims.py --slug
lahaina` against the script and both ledgers (**652 sentences from 2 files**). **Two titles and two
headlines came back with zero unsupported claims and zero soft notes:**

- `Hawaii Built the World's Largest Warning Siren Network. It Had Never Been Used for a Fire.` (90)
- `Only One Siren Was Operable Inside the Burn Perimeter at Lahaina on August 8, 2023.` (83)
- headlines `NEVER USED FOR FIRE` and `ONE SIREN`

`ONE SIREN` carries a **binding condition** recorded in the file: it is an incomplete claim on its
own and may only ship paired with the title that carries *inside the burn perimeter*.

### 8.4 fact_recheck — six items before the render, two that will change on their own

The two that are not like the others:

- **`LH-115` will be wrong before this film publishes.** No settlement payment had been made as of
  2026-08-21 and the fee appeal reached the Hawaiʻi Supreme Court on 5 August; counsel expected a
  ruling as early as September. The script says *"As of August twenty twenty-six"* **out loud**, so a
  later ruling makes the film **dated rather than false** — but that is a mitigation, not the check.
- **`LH-38` is the only quotation of a living identifiable individual in the film, and its source was
  never read in a primary record** (`SRC-0006` is search summaries). Before publish it is either
  upgraded to a read source **or cut** — and the packet states plainly that **the film's argument
  survives the cut**, because Finding 38 carries that beat and Finding 38 is primary.

### 8.5 The gate found a real gap in the spec, and then a real bug behind it

`check_episode_inputs.py` reported *"0 P### people stills against the 24 this episode declares"*.
Chasing it rather than dismissing it produced two findings:

1. **`people_plates` is a first-class schema field and EP75's spec did not declare it.** With no list,
   the checker falls back to counting filenames beginning with `P` — and this episode's people plates
   are `H004`, `H005`, `H008`… The code's own comment says the prefix rule was wrong ("a filename is
   not a face, and it is not a people plate either"). **`episode_spec.v002.json`** adds the list.
   **Every declared number is byte-for-byte v001's** — nothing was re-tuned — and the 24 ids are
   **generated from the order's `P` flags**, not typed, so the two cannot drift.
2. **`build_motion_from_plates.py` hard-codes `episode_spec.v001.json` and reads `people_plates`.**
   Every other tool that reads that field goes through `check_episode_spec.spec_path()`, which takes
   the highest revision. This one did not, so a correctly-written v002 would have been **ignored
   silently** and every people plate animated as an ordinary cut. Fixed to use the resolver;
   verified it now sees 24, and verified EP72 (v001-only) and EP62 still resolve unchanged.
   **Honest scope: the only live victim would have been EP75.** EP69 hyatt is the one other episode
   with a v002, and its two revisions carry the same list, so nothing already shipped was affected.

### 8.6 What is left, and it is all downstream of an image

`check_episode_inputs.py --slug lahaina` → **NOT READY, 7 problems**, and all seven are things that
cannot exist yet:

```
no filmconfig            EP75_lahaina_filmconfig.v001.json
no narration index       06_audio/narration_index.v001.json
no narration audio       remotion/public/lahaina/narration.mp3
0 stills                 (need >= 40)
0 people stills          (need 24)
0 factory clips          (need >= 40)
no Remotion composition  id starting with Ep75 in Root.tsx
```

In order: **Codex generates from the 17 paste files → upscale every plate to 3840 BEFORE it enters
the pool → a person opens a labelled contact sheet → narration master → filmconfig and composition →
stage footage → `preflight_render_gate.py` GREEN → render.**

**Before staging a single clip**, run `check_cross_episode_reuse.py` and eyeball that contact sheet.
`footage_review_required` is `true`, the factory shelf's labels are known to be wrong, and every query
in this episode will return holiday footage of Maui. FILM BIBLE §0 item four and §10.

---

## 8.7 Done while the images were generating

The owner started the Codex batch and asked for whatever could run alongside it. In the order that
unblocked the most:

**The narration master is delivered.** `made=311 skipped=46 failed=0`, master **1,857.403 s**, film
**31:06** with the endcard — inside `runtime_seconds` [1740, 1920] with 126 s of headroom low and
54 s high. **The hook landed at 20.266 s**; the bible asked for a trim in the edit to reach 0:20 and
none was needed.

**The scene plan's own first instruction was carried out**: §2 is now read from
`narration_index.v001.json` instead of projected, along with every kinetic-beat time and the whole
arithmetic. Everything still holds — 132 plates ≤ a 156 ceiling, 265 distinct assets over 330 video
cuts at 1.25× — so nothing had to be re-scoped.

**§2.2 keeps what the projection got wrong, and it is the useful part.** One measured section, one
dollar of ElevenLabs on ACT_1, predicted a thirty-one-minute film to **within 1.4 %** (30:40 projected
against 31:06 delivered; 187.1 wpm modelled against 184.1 delivered). The registry's 171.79 model
would have sized this script four minutes short. **That is the argument for `--measure-section`, in
numbers, for the next episode that is tempted to skip it.**

**`LH-38` was upgraded before the master was generated, not after.** It was the only quotation of a
living identifiable individual in the film and `fact_recheck` §3.2 said upgrade it or cut it. CBS
News, 18 August 2023, was fetched and read; ledger v002 §12 replaces the row with four read rows and
the script now carries two verbatim quotations instead of a paraphrase. Doing it *before* the run
meant the sha sidecars regenerated only the changed chunks.

**The archive was probed before a single query was written**, which is what
`episode_footage_queries.v001.json` demands of itself. **The finding is negative and it is the
point**: this shelf cannot carry one of this film's subject registers. `power line` 0 rows,
`utility pole` 0, `dry grass` 1 and it is a sunset, `smoke` 215 rows of factory stacks and welding
sparks, `fence`/`chain` prison footage with prisoners in it, `siren` five rows all police and one of
them labelled *ai generated*, `theme:weather_disasters` fourteen items in the whole library. So the
subject registers are removed and carried by plates, and the 18 kept queries are the registers where
place does not matter. The substring traps are recorded with counts so nobody re-adds them.

**83 figure cards were authored** into `EP75_lahaina_filmconfig.v001.json` from the bible's beat map —
14/16/17/15/16 across the acts, inside `figure_beats_per_act` at both edges, every card carrying a
ledger row id that was checked against both ledgers before the file was written.

**The description was drafted and gated**: 3,567 characters, `unsupported=0`. It also produced a
second instrument false positive, recorded in the packaging file: the department's full legal title
*County of Maui Department of Fire and Public Safety* collides with Finding 30's *"did not have a
Public Safety Power Shut-Off program"* and returned `CONTRADICTED`. Resolved with the abbreviation
the record itself uses.

**The Remotion composition was deliberately not written.** It imports `lahaina_film.json`, which does
not exist, so registering it now would break the build for every other episode.

### 8.8 Three live findings the next person needs

**1. The media root moved from `H:` to `E:` and 238 scripts have not.** `H:\pd-media` does not exist;
the shelf, the plates and the narration master are all under `E:\pd-media`. `check_episode_inputs`
still reports plate paths under `H:`, and `select_factory_assets` cannot find the factory ledger,
which means **label verification is unavailable and every clip picked is unverified**. The cheapest
fix is one reversible command — `subst H: E:\` — and it is an environment change, so it is the
owner's call rather than something done here. Rewriting 238 scripts was not attempted.

**2. Plates are arriving at the wrong aspect ratio, at a rate.** Measured on the live delivery:
`H002` 1881×836 (2.25:1) and `H014` 1832×859 (2.13:1) against everything else at 1672×941 (16:9).
Two in sixteen is 12 %, which projects to about sixteen of the 132. The `[STYLE]` macro already said
16:9 and that was not enough, so **the instruction is now in the batch header** where a person reads
it, with the two measured failures quoted, and all 17 paste files were regenerated.
`scripts/check_plate_delivery.py` reports missing / wrong-aspect / under-floor / unexpected in one
pass and is meant to be run repeatedly while a batch generates.

**3. The captions came out of `gen_captions_case.py` failing the break gate at 15.4 % against a 5 %
cap** — which is the owner's long-standing "字幕が変な所で切れる" complaint, and `check_caption_breaks`
names the cause in its own docstring: that generator cuts at `MAX_WORDS, MAX_CHARS = 7, 42`, a
mechanical count with no syntax. **A second generator, `gen_captions_forced.py`, already contains the
grammar-aware `_smart_split`.** Both were run against the same master so the two could be compared on
the gate rather than on opinion; see the result recorded alongside `captions.smartsplit.v001.srt`.
**Whichever wins, the loser should stop being the default** — two caption generators where only one
has the fix is how this defect keeps shipping.

## 9. The nine things a human has to confirm, because no gate measures them

They are FILM BIBLE §17 and they are not decoration. The three that will actually decide whether this
film is right:

1. **That no frame, line, caption, card, title or thumbnail ever completes the sentence "if the sirens
   had sounded…".** No source establishes it. The film's shape is: the system existed, it was for
   this among other things, one siren inside the perimeter was operable, it had never been used for a
   fire, and it was not used — **then stop**.
2. **That the film never uses the shape *raise a question, leave it open*.** That shape, not any false
   claim, is what this topic's conspiracy audience is made of. Every question the script raises is
   answered inside ninety seconds with a date and a source.
3. **That the 14:17 → 14:55 gap never sits next to a word implying fault**, and that the County's
   statement about its firefighters' diligence and Finding 67 on mopup are in the same passage every
   time it is spoken.

---

## 10. Cost and side effects

- One paid ElevenLabs call: `--measure-section ACT_1`, 54 chunks, ~$1.40. No master and no index were
  written — `--measure-section` stops before both, and the sha-256 sidecars mean the later full run
  skips those chunks rather than paying for them twice.
- Two web fetches and two searches for the ⛔-11 re-verification.
- One 132 MB PDF downloaded to the session scratchpad. **Nothing was written to `H:` or to any media
  root.**
- `scripts/gen_narration_case.py` gained one registry entry and had it rewritten once. Both edits went
  through `pd_edit.py` with an `ast.parse` smoke command.
- The full narration run for this episode will cost about **$9.22** (349 chunks) when it is made.
