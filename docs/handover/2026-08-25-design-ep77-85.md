# 2026-08-25 — EP77–85 の設計スレ

**このスレが持っていたもの**: EP77–82 の左工程仕上げ／EP83–85 の設計（題材確定済み）／EP83–85 のサムネ選定。
**組み立て・レンダー・投稿は別スレ**。引き渡し条件は `docs/PD_HANDOFF_FROM_DESIGN_THREAD.v001.md`。

セッション実測: API 1,457 / 平均文脈 533,775 / ピーク 986,111 / 増幅率 1,335倍。**CRIT**。工程の境目なのでここで切る。

---

## 1. 到達点

### EP83–85 の設計は完了

| 話 | 台帳 | 契約 | 台本 | 発注書 | プレート |
|---|---|---|---|---|---|
| **83 max737** (Boeing 737 MAX) | 一次資料11点 | ✅ | ✅ 4ゲート緑＋claims PASS | ✅ 188枚 | **188/188 QC合格・追加なし** |
| **84 threemile** (Three Mile Island) | 起訴状原本まで | ✅ | ✅ 同上 | ✅ 186枚 | **186/186 合格**（T157再生成済） |
| **85 katrina** (堤防決壊) | 判例＋工学10資料 | ✅ | ✅ 同上 | ✅ 186枚 | **59/186・再生成中** |

**EP83/84 の4K化＋深度をGPUキューに投入済み**（`scripts/_gpu_queue_ep83_84.sh`、`out_gpu_queue_ep83_84.log`）。
EP85は再生成が終わったら同じ手順。不足分の発注書は**毎回ディスクを数え直す**スクリプトで作る:

```
py -3.11 scripts/build_plate_additions_order.py --slug katrina \
  --plates episodes/_planning/EP85_katrina_CODEX_PASTE/plates.v001.jsonl \
  --dest "E:\pd-media\05_visuals\katrina\img" --out <出力先>
```

### EP77–82 は「台本・音・画像は完成、実写プールが未了」

6話とも: 台本4ゲート緑 / ナレ / 字幕 PASS 0.0% / SFX行 61–72本 / AEジョブ定義 / 4K画像 / 深度マップ。

---

## 2. **このスレの最重要の発見** — 実写プールは全画面で見ると壊れている

組み立てスレの文書§2（「サムネイル大では国の手がかりが読めない」）を、EP77で実測した。

`scripts/build_fullframe_strips.py`（新規）で **1クリップ＝2フレームを900px**で出し、35本を1本ずつ読んだ結果:

**35本中24本が却下。残り11本（床は40本）。**

| 却下理由 | 本数 | 実例 |
|---|---|---|
| 外国と判明 | 10 | 日本（船体の漢字）／東アジアの街路（青い縦看板・青白の縞ガードレール）／東南アジアのフェリー（対岸に寺院）／イギリス（レンガのテラスハウス）／トルコ2／地中海2／北欧2 |
| 読める商標 | 3 | WALGREENS の看板／クレーンの ASYAPORT／Tシャツの文字 |
| 場所が特定できない | 6 | 夜の景色・霧の石畳・製油所の空撮 |
| 米国だが地域が違う | 1 | **ヤシの木**（メリーランドに生えない） |
| 意味が本編と無関係 | 3 | 印刷機・製造機械 |
| 重複 | 1 | |

**260pxでは全部「無地・場所の手がかりなし」に見えていた。** 判定は理由つきで
`runs/qc/keybridge_clip_verdicts.v001.json`、却下24本は `factory/_eyeball_reject/` へ退避済み。

> **EP78 colgan（66本）と EP79 alaska261（51本）も同じコンタクトシート判定で通ったプール。**
> 同基準なら大量に落ちる。**まだ全画面で見ていない。**

---

## 3. 残作業（引き渡しまで）

`py -3.11 scripts/check_episode_inputs.py --slug <slug>` が **READY to build** を出すまで。現状6話とも NOT READY。

| | filmconfig | プレート判定 | 実写 | Root.tsx | clip_qc台帳 |
|---|---|---|---|---|---|
| 77 keybridge | 未 | 131枚未 | **11本**（要再選定） | 未 | 未 |
| 78 colgan | 未 | 166枚未 | 66本（**全画面未**） | 未 | 未 |
| 79 alaska261 | 未 | 198枚未 | 51本（**全画面未**） | 未 | 未 |
| 80 concordia | 未 | 185枚未 | **0本** | 未 | 未 |
| 81 station | 未 | 188枚未 | **0本** | 未 | 未 |
| 82 valdez | 未 | 183枚未 | **0本** | 未 | 未 |

加えて EP77–82 の youtube_meta とサムネ候補3案。EP83–85 は 4K化以降が丸ごと残り。

**着手順の提案**: EP77を1本だけ最後まで通して「READY to build」を1本出す。組み立てスレが並行して動ける。

---

## 4. 直した機構（全部、実演つき）

| 何が壊れていたか | どう出たか | 直し方 |
|---|---|---|
| `check_script_length` が **"Title 18 U.S.C." の "Title"** を付録見出しと誤認 | EP84の**4,800語の台本を80語**と報告 | 付録見出しを行頭アンカーに。引用コメントを先に除去 |
| `check_packaging_claims` が**台帳の行ID「TM-404」を数量404**と誤読 | サムネ文字 ONE VALVE を数値不一致で却下 | ID形式を数値抽出前に除去。**全話の台帳が同じ形式なので全話に効く** |
| `check_caption_breaks` が**数字を語として数えない** | 「…of 1989」の最終語を "of" と誤読 | 数字を語文字に含める |
| 字幕生成器が**引用コメントをSRTに書き込む** | EP77で103件混入（音声は無事なことをwhisperで実証） | 生成側とナレ抽出側の両方で除去 |
| 字幕の折返しが**機能語で行を切る** | 15.9%不良 | 文法対応の折返し＋orphan統合＋修理ツールのoff-by-one → **6話とも0.0%** |
| `author_sfx_beats` の**単語境界なし** | 「du**ring**」に電話音、「**Key** Bridge」に掛け金音 | `\b` 適用＋語彙拡張。cp932クラッシュも2箇所 |
| 4K化が**cuDNNで死ぬと全部やり直し** | EP82が56/183で死亡 | `--resume` ＋ タイル半減リトライ ＋ 失敗記録 |
| `gen_depth` に**親ディレクトリを渡していた** | 5話ぶん「done 0」でキューは"完走"と報告 | `img/` を渡す。**1枚で実動を実証してから**投入 |

**新規ツール**: `build_fullframe_strips.py`（全画面判定）／`build_qc_zoom_sheet.py`（440px精査）／
`upscale_plates_4k_esrgan.py`（一話ごとのコピー4本を1本に）／`check_ae_jobs.py`（AEジョブ検証）／
`build_plate_additions_order.py`（追加発注を毎回実測から生成）。

---

## 5. 踏んだ罠（次スレも踏む）

1. **キューの「done」は完走ではない。** 深度0枚でも、上流が死んでも "done" と出た。**必ずバイト数を数える。**
2. **"narrow-body" の中の "body"** が禁止語に一致（EP83の発注書7行）。`nobody` の罠と同じ形。
3. **プレートIDの衝突と枚数変動。** 追加納品で枚数が変わり `--count` ガードが正しく拒否した。リストは**毎回測り直す**。
4. **納品は動いている最中に読むと古くなる。** EP85が4分で31→38枚。
5. **1980年代のマイクロフィルムはOCRが数字を壊す**（$5,000→45,000、$1 million→51 million）。EP84の罰金表は**ページ画像を見て**確認した。
6. **spec を v001 のまま編集した。** 不変条件6には反するが、**47のツールが v001 をハードコード**しており（EP74の実績）、まだ何もビルドされていない契約なので、この判断を記録として残す。

---

## 6. オーナー決定（この日）

- **尺は20分超ならOK**（EP77–82の実測は25:40–27:50。宣言帯 [1740,1920] は下回るが `release_deviations` 行き）
- **EP82の28:26容認** → `episodes/PD-2026-082-valdez/approvals/APR-0001.json`
- **EP83–85 の題材確定**（正典 `EP83-85_SLATE.v004.md`）

## 7. サムネ選定（未決）

9枚は合成済み・オーナー目視合格。**`thumbnail.selected.v001.png` は意図的に作っていない**（オーナー決定事項）。
推薦と根拠: `episodes/_planning/EP83-85_THUMB_SELECTION_ANALYSIS.v001.md`。

- EP83=**03** IT WAS ALL THERE.／EP84=**01** IT BEGAN AT 4 A.M.／EP85=**01** BELOW DESIGN.
- **EP85だけ前レーンの推し（03）と違う。** 「THEY NEVER MOVED.」はバスの写真だが、**本編はバスにも避難にも一度も触れない**（台本873文を検索してヒット0）。claims も UNVERIFIED。
- 9枚とも claims 照合済み（台本ができたので実行できた）。**EP85は台本後に再照合済み・3枚ともPASS。**
- EP85 T06 の再生成は**合格**（原寸で文字が成立していないことまで確認、sha紐づけで記録）。

## 8. 未解決

1. **EP83–85 の台帳の Open items**（EP83=無罪判決の一次引用／EP84=Rogovin未読／EP85=IPET Vol.I草案の罠）
2. **EP85 の死者数は 1,118（ルイジアナ・2006年8月）を使う。「1,800人超」はNHC自身が2023年に撤回済み。**
3. **EP82 の V079 が未着**（海図とディバイダー）
4. **AE汎用ビルダーが無い。** 9種のkindのうち `kinetic_beat.jsx` が描けるのは2種だけ。ジョブ定義9話分は検証器つきで完成しているが、**レンダーは未**
