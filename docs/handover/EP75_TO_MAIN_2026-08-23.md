# EP75 lahaina スレ → メイン（2026-08-23 / セッション終了）

**このスレは担当分を終えて閉じます。**
詳細は `episodes/_planning/EP75_lahaina_STATUS.v001.md`（実測値＋数え直すコマンド付き）。

---

## 終わったこと

| | 実測 | 場所 |
|---|---|---|
| 素材の目視QC | **135/135シート**（全クリップ6コマ以上） | `episodes/PD-2026-075-lahaina/05_visuals/pool_frame_review.v001.json` |
| ナレーション | 1,857.4秒 / 357チャンク | `remotion/public/lahaina/narration.mp3` |
| 字幕 | 907キュー・切れ位置不良3.5%・dangling 0 | `episodes/PD-2026-075-lahaina/08_edit/captions.final.v001.srt` |
| 絵 | 135枚 4K ＋ 深度135枚 | `remotion/public/lahaina/img/` |
| 映像素材 | 374本 | `remotion/public/lahaina/factory/` |
| 音響 | 76キュー・2.453/分 PASS | 台本内 `(SFX:)` ／ `config/sound_rules/lahaina.json` |
| 汎用音響ツール | 納品済み（EP70/71 移行済み・一発物削除済み） | `scripts/write_sound_plan.py` |

### 目視QCの結果

```
プール          374本（シートは375本ぶん＝1本は既に factory_rejected 済み）
  forbidden      25本  forbidden_subjects 該当 or 顔が識別できる
  off_register   89本  規則違反ではない・編集判断（理由を全件記載）
  ------------------
  却下          114本
  使える        261本  うち conditional 33 / standout 84
```

**261本で足ります。追加ダウンロードは不要。** factory スロット274に対し
再利用 平均1.05回・distinct比0.95 で `footage_diversity` の床に余裕。

---

## 次に触る人へ（3点だけ）

1. **`runs/` は `.gitignore:17` で除外されています。**
   目視結果の正本は追跡側の `05_visuals/pool_frame_review.v001.json` です。
   `runs/qc/lahaina_clip_verdicts.v001.json` は同じ内容のローカル控え。**消えても正本は残ります。**

2. **`conditional` の33本には1本ずつ条件が書いてあります**（トリミング / 開始秒 / 尺）。
   条件を無視して使うと、そのまま欠陥になります。特に注意する4件:
   - `38957773` 消防隊員 … 顔が6コマ中5コマで解像。ホースと装備だけに切るか、落とす。
   - `38779095`ほか3本 交通管制室 … 緊急対策本部の画はこれしかない。人と読める画面あり。
   - `34535507`/`34535510` 窓辺の男性 … 顔は完全な黒。**火事や死者の話の近くに置かない。**
   - 農地の野焼き4本 … 一直線の着火線が「人が管理して燃やした」と読め、
     `forbidden_claims` の放火示唆に触れる。狭く切ること。

3. **これは出荷判定ではありません。** メインスレの整理どおり、出荷は
   **実レンダーのコンタクトシートを人が原寸で読む**ゲートが決めます。
   判定ファイルにもその旨を明記済み。

---

## 残っている作業（全部このスレの外）

| 作業 | 担当 |
|---|---|
| `remotion/src/lahaina_film.json` を作る | 組み立てスレ |
| `Root.tsx` / `compositions/Ep75LahainaFilm.tsx` 登録 | **メインスレ**（各スレは触らない・film.json 完成時に slug/composition id/パスを渡す） |
| 権利がゲートから見えない件の修正 | EP72/73スレ（2026-08-22 移管） |
| 実レンダーのコンタクトシート目視 ＝ **出荷判定** | レンダー完了後 |
| 台帳 §10 和解の状況を一次ソースで再確認 | **公開当日（8/29）**。生きている案件 |

予約 8/25 昼 ／ 公開 8/29。

---

## この回で分かった罠（次の人が同じ穴に落ちないように）

- **リポジトリ直下からの再帰検索で `remotion/public` 配下が返ってきません。**
  深度マップを一度「0枚」と誤読しかけました。パス直指定なら135枚あります。
  **「0件」を見たら、まずパスを疑うこと。**
- **代表1コマの目視は、40秒のクリップを承認できません。** 実例:
  無地の白い布が8コマ中7コマで顔になる／完璧な書類の画の机の隅に「正義の女神像」
  （`scales of justice` は禁止事項）／167秒の平凡な舗装が中盤からドリフト走行になる。
- **1つのクエリが1か所を8回返すことがあります。** 廃墟8本が全部同じ建物でした。
  被り率は目視前に測ったほうが早い。
- **「窓」のクエリは人物を連れてきます。** 顔が識別できる却下5本のうち3本が窓由来。

---

## トークン実測（rule 20・セッション終了時の義務）

```
billed total ....... 721,060,938
AMPLIFICATION ...... 1,031x
判定 ............... CRIT（文脈が肥大。工程の切れ目でセッションを分ける）
内訳 ............... Read 48.2% (207回) / Bash 16.0% (432回)
画像 ............... 193枚 ≒ 289,500トークン
```

**画像289,500トークンは削るべき浪費ではありません。** 135枚の目視で114本の欠陥を外した対価で、
うち25本は規則違反、1本は顔でした。rule 20 も「画像の目視QCは品質の生命線。減らすのではなく
サブエージェントに出して親に残さない」と書いています。**次回に効く改善はそちら**で、
今回は目視をこのスレ自身がやるようメインスレから明示指示があったため親に残しました。

削るべきは **Bash 432回**のほうです。同じ確認を繰り返す短いコマンドが多すぎました。
