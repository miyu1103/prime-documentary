# Handover — 2026-08-25 (written mid-session ~01:20, will be updated at session end)

Continues `2026-08-24.md`. The sheet-reading session. Everything below was found by reading
sheets, and none of it by a gate.

## 1. The two big finds

**EP75 lahaina's 19:32 master ships 180 of 497 cuts — 678 seconds — of raw grayscale depth
maps as picture.** Proof: sheet 16 shows the color plate H077.png as cut0076 and the SAME
SCENE as a white cutout (H077_depth.png) as cut0077. Cause: `build_asset_manifest_motionfirst.py`
and `i2v_episode_batch.py` both glob `H*` and swept the `_depth` companions in; 62 of the 71
"i2v motion clips" that fixed the stills-32% complaint were animated depth maps. Both globs
now exclude `_depth`; `check_spec_satisfied` has a `depth_as_picture` check demonstrated
failing (180 cuts) on the defective film; the 62 depth mp4s and 63 frame dirs are quarantined;
a real i2v of 83 color hero plates is running (chain pid detached, log `out_i2v_lahaina.log`,
progress [N/83]; a persistent Monitor watches for COMPLETE).

**EP74 itaewon's 00:12 re-render still ships the wrong-country class at scale.** The five
named clips are gone, but 75-sheet reading found Kyoto, Paris ×3, Venice ×3, London ×4,
Vilnius, Warsaw, NYC ×2, Dublin, Brno, Mexico City, Australia, Kobe, Moscow (a direct
survivor), Singapore, Brooklyn, Texas Capitol, Taipei, Hong Kong, Ho Chi Minh, Netherlands…
The pool was keyword-staged ("asian street", "night market") and country never screened.
Fix executed tonight: full pool country-eyeball (84 sheets, 4 readers) → **116 of 248 clips
rejected**; 76 Korea-labeled candidates pulled from the shelf, 70 presented, 66 staged after
review → pool now 198 clean clips; episode_spec gained 33 place terms (the gate now catches
32 cuts of the old film by label alone); film rebuilt — 291 cuts, forbidden hits 0,
distinct_ratio 1.0. Only deviation: distinct_video_assets 198 vs declared 265 (morandi
shipped at 109/265; record in release_deviations, not a blocker).

## 2. Component fixes that affect every future render (committed 7b111d1a)

* `ComparisonBars.formatVal`: was `toFixed` — "1300000" shipped at 100 px (EP76 sheet 22),
  and 0.58 landed as "0.6" beside its own 0.58 label. Now `toLocaleString` with the target's
  own decimals.
* `QuoteCard` attribution: `nowrap` clipped long attributions mid-word on EP75
  ("…ASKED WHETHER HE R") and EP76 ("…MOST SIGNIFICANT IMMEDIA"). Now wraps at maxWidth 1360.

## 3. Verdicts recorded tonight (all runs/qc, not committed by rule)

* `lahaina_shipped_frames_review.v001.json` — REJECT, master 18ae3041. Do not ship.
* `itaewon_shipped_frames_review.v001.json` — REJECT, master 6a8d17d8. Do not ship.
* `itaewon_clip_verdicts.v001.json` — supersedes the PASS that had approved Kyoto/Paris
  (old file renamed .SUPERSEDED_20260825). 116 rejects moved to factory/_eyeball_reject.
* `uri_clip_verdicts.v001.json` — 49/49 sheets read, 26 rejects pruned (UK estates ×2,
  faces ×2, S-Asian plant ×3, Cyrillic substation, German ICE town, CGI archviz ×3,
  PORSCHE brand, frozen-lake/alpine forbidden, ...). Pool 105, film built, READY.
* `lacmegantic_plate_verdicts.v001.json` — L169 accepted (people plate 20/20; v001 order's
  "L168" was a numbering collision with an existing registered asset — corrected order is
  `EP72_lacmegantic_CODEX_ADD_L169.v002.txt`). Film built, READY.
* morandi 47/47 sheets read: NaN fix HOLDS everywhere, BLOCKING 0; one ungrouped bar label
  ("1300000") → component fixed → re-render queued.
* EP74 + EP76 descriptions written and checker-PASS (0 hard); EP75's existing draft PASS.

## 4. The stale-sheet trap fired twice more tonight

uri's sheet dir mixed 49 current sheets with 15 from 8/22 (quarantined to
`_stale_20260822_pre_current_pool/`); itaewon's pool dir mixed 84 current with 165 from 8/22
(quarantined to `_stale_20260822/`). Always reconcile the receipt's sheet list against the
dir before reading.

## 5. GPU plan (deadline order; one job at a time)

i2v (ends ~08:30) → **itaewon render** (8/26 16:05 slot) → lahaina manifest+film rebuild
(CPU, during itaewon render; spec gate must show depth_as_picture=0) → **lahaina render**
(8/27) → **morandi re-render** (8/28) → **lacmegantic** (8/29) → **uri** (8/30). After each
render: check_shipped_frames --sheets-only → READ the sheets → write the review JSON →
acceptance → booking. The three re-rendered episodes currently have UNREVIEWED shipped-frames
verdicts, so nothing can upload prematurely; oroville (8/25 16:05) is untouched and green.

## 6. Standing warnings

* The i2v chain is a DETACHED process (the harness lost it when its wrapper was killed).
  It holds the single-instance lock; do not start a second. `out_i2v_lahaina.log` is truth.
* remotion/public/lahaina/_motion_depth_rejected_20260825/ and
  ae-demo/_rejected_depth_frames_lahaina/ hold the quarantined depth artifacts — do not
  restore them; the manifest builder would exclude them anyway.
* EP74 thumbnail still has 1 variant vs the 3 thumbnail_ready wants — owner decision open.
* EP70 live-thumbnail refresh: still waiting on EP35's measured swap (owner decision open).
* LH-115 (lahaina settlement status) must be re-verified before packaging final and again
  before scheduling — the ledger says so explicitly.
