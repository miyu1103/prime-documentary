# Handover — 2026-08-11, 23:10 JST

Read this before touching anything. It is the part of tonight that the repository cannot tell you.

The previous session ended deliberately, to pick up a raised subagent cap
(`CLAUDE_CODE_MAX_SUBAGENTS_PER_SESSION=10000`, already written to
`C:\Users\aab15\.claude\settings.json`; it only takes effect on a fresh start).

---

## 0. First command

```
py -3.11 scripts/check_queue_will_stall.py
```

It reports, for all seven queued episodes plus the machine itself, every hard reason the unattended
render queue would stall. It was written tonight because nothing else looked at the machine. Trust
it over this document where they disagree, and over any receipt older than a few hours — stale
receipts sent two agents down blind alleys tonight.

## 1. What is running right now, unattended, and must not be killed

| | state |
|---|---|
| **EP65 marmet render** | started 22:46, ~3 h, launched by the queue. `out/marmet_preprobe.mp4` exists; `out/marmet.mp4` does not yet. |
| **`scripts/queue_unattended.sh`** | one instance, snapshot `/tmp/pdq.WhHBPr`. Order: marmet, greene, correa, openfields, ramirez, pinto, hyatt. Heartbeats every 10 min into `runs/logs/queue_unattended.*.log`. |
| **EP62 greene i2v** | **COMPLETE** 193/193 at 22:43. |

These are OS processes. They survived the session ending and will keep going. **Do not restart the
queue while a render is in flight** — killing the queue with `/T` takes the render with it.

Three subagents were staging footage for EP67/68/69 and died with the session. Their output is on
disk and is not lost:

| slug | img | factory | motion |
|---|---|---|---|
| ramirez | 146 | 107 | 0 |
| pinto | 123 | 42 | 0 |
| hyatt | 127 | 146 | 0 |

They need restarting. EP68 pinto is furthest behind.

## 2. Six ways the machine stopped tonight, and what was done

All are fixed; they are recorded because the fixes are only obvious once you know the failure.

1. **A hung `remotion render --help` idled the machine for 545 minutes.** The queue's busy detector
   matched on the command line, saw `remotion` and `render`, and waited nine hours for a process
   that was never rendering. The detector now excludes `--help`/`--version` and anything older than
   8 h. Demonstrated: old detector counted a synthetic ghost as 1, new one as 0.
2. **The queue exited on its wait timeout.** `wait_idle || exit 1` meant one stuck reading killed
   the whole queue. It now logs and continues.
3. **Two queue instances ran at once**, 37 minutes apart, each with its own snapshot of
   `_finish_episode.sh` — and the older snapshot predated the 4-layer-audio change. Whichever won
   the race would have decided the audio of the next film. `check_queue_will_stall.py` now counts
   snapshot directories, which is the reliable signal (process count reads 1 or 2 for one queue).
4. **C: was at 22 GB free, 99% full**, with ~29 GB of renders and public dirs still to write. 52 old
   renders (47 GB) were **moved, not deleted**, to `H:/pd-media/renders/archive_20260811/`, which has
   a README naming every file and how to restore it. C: is now ~60 GB.
5. **The `[4a]` spec gate could never fire.** It searched `grep "^\[satisfied\]" | tail -4`, but
   `check_spec_satisfied` prints failures as detail lines starting with two spaces and a dash, so the
   inner grep threw away exactly the lines being searched for. EP66 openfields is missing 96 of 185
   purpose-made plates and the guard waved it through. Fixed and demonstrated: openfields now DIES,
   marmet still proceeds.
6. **The API session limit killed four agents simultaneously** at ~21:35. Renders and i2v were
   untouched. When it happens, resume agents from their transcript rather than spawning fresh ones —
   a fresh agent loses everything the dead one measured.

## 3. Two burn-in defects caught before they shipped

Burned captions cannot be fixed after a render, so these were the most expensive near-misses.

- **Six of eight episodes would have burned captions at the wrong offset.** The lead was stored in a
  sidecar and trusted, but `build_case_film_generic.write_srt` regenerates the srt at raw timings on
  every film build without updating the sidecar. `polish_captions_srt.py` now **measures** the lead
  present in the file. Proven: a deliberate 0.4 s shift injected into a copy is detected; a second
  run shifts 0.0; marmet, pinto and memphis are byte-identical before and after. `verify_caption_sync`
  moved memphis and pinto from FAIL to PASS.
- **`_finish_episode.sh` hardcoded `captions.final.v001.srt`** while step [4/7] writes wherever the
  filmconfig's `captions` key points. EP67 ramirez's captions were re-cut as **v002** after tonight's
  script extension (master 1448.020 s → 1600.809 s), so the finisher would have burned the
  pre-extension captions **152.8 s out of sync**. Now reads the declared key. Verified: ramirez
  resolves v002, the other six resolve v001.

## 4. Facts measured tonight that contradict older notes

- **Narration pace 167.5 wpm** end-to-end, pooled over 50,582 words / 18,118.4 s across EP60-69
  delivered masters. `gen_narration_case.py`'s printed "measured wpm" excludes inter-chunk silence and
  overstates by 6.7-8.9% — divide yourself (PD_CANON rule 25).
- **Long-form CTR 1.61%** impression-weighted (median 1.26%), not the 2.31% on record.
- **Audience 77% aged 55+**, not 91%. 92.5% male.
- **"27-37 min beats 9-15 min" is confounded** and must not be used as a length rule. The band's
  advantage is carried by OceanGate and D.B. Cooper, the only two topics with a large external feed.
  Length is not the variable; **a famous, adjacent premise is.**
- **The feeders are FRONTLINE's Madoff film and LEMMiNO's D.B. Cooper.** Search terms that find us
  are named events — `oceangate`, `aaron swartz`, `ruja ignatova`, `anthony ray hinton`. **No doctrine
  term appears anywhere.**
- **Register abundance decides whether an episode can be dressed.** EP66 openfields is a field-and-
  woods story and the shelf holds 3,572 nature clips, so it took 191 using 5% of its register. EP63
  correa is a 1995 San Juan emergency room; the shelf holds 258 hospital clips, it would have needed
  74%, and it got 58. Same shelf, same week. This is not a search-quality problem, and more querying
  will not fix it.
- **The channel's normal visual shape is ~68% archive footage and 0% AI motion** (openfields, burge,
  lejeune, postoffice, surfside). EP62-65 drifted to 44-57% AI motion — a regression, not a standard.
  Generating more plates makes it worse, not better: `solve_totals` splits video proportionally to
  pool capacity, so a bigger AI pool pushes archive cuts off the screen (measured on correa:
  52 → 46 → 41 archive cuts at +0/+20/+40 plates).
- Subscribers **21** (from 17), 55 long-forms, 50 public, 5 private awaiting their dates.

## 5. Owner directives issued tonight — both now canonical

- **`docs/PD_EDITORIAL_STRATEGY.v001.md`** — the channel's promise, the pillars, KPIs, the separation
  of ¥1M/month from 2M subscribers.
- **`config/pd_planning_os.v001.json` + `scripts/score_premise.py`** — the planning OS as data and a
  command: the 100-point rubric, 12 series, 8 title archetypes, the funnel, the learning-engine
  fields. Where the two documents conflicted the newer wins, and this is already reconciled in the
  JSON: the premise gate is **85**, not 70, and the slate is 30/20/15/10/10/5/5/5, not 35/30/25/10.

Run `py -3.11 scripts/score_premise.py --show` to see the whole thing.

## 6. EP70 — chosen, then failed its own gate

Topic: **Oroville Dam, 2017**, ordered by the owner at **45 minutes**. Chosen from measured demand
(median 117,477 across 15 results; 7 channels above 100k; only 2 results over 30 min, best 182,912)
and from the register test (8,289 clips, 398 video cuts needed at 45 min = **4.8% utilisation**,
matching openfields' successful 5%). Ban risk **LOW, not R3** — zero deaths, defendant is a state
agency, no living private individual accused.

Sources verified: FERC Project No. 2100-052 filing of 17 Oct 2005 quoting the operator's own 2002
memo (*"is not armored"*); Independent Forensic Team Report, 5 Jan 2018; *Oroville Dam Cases*, No.
C093600 (Cal. Ct. App. 3d, 5 Oct 2023), CourtListener 9430451. **A class-certification claim in
secondary coverage is NOT what C093600 holds — do not build on it.**

Then it was scored against the owner's own new rubric and came back **77/100 — IMPROVE, not GO**,
8 points short. Record: `episodes/_planning/EP70_oroville_premise.v001.scored.json`.

- `personal_threat` **12/20** — most people do not live below a dam.
- `story_arc` **7/10** — the protagonist is currently an institution, which breaks the brand rule that
  the protagonist is always an ordinary person.

The route to 85 is written into the premise file: centre a **named evacuee family** from the
12 February 2017 order, and move the promise from "a dam nearly failed" to "the government knew, in
writing, for twelve years, and nobody told the people living below it". Open on the evacuation order
— not the document, not the water. **If `personal_threat` still cannot reach ~16 after the reframe,
say honestly that this is a strong 30-minute film and not a 45-minute one. Do not pad.**

45-minute arithmetic, already derived: 2,691 s of narration → **~7,513 words** (1.2× the longest ever
delivered, surfside at 6,347), ~45,000 characters, **~$13.50** of ElevenLabs. At `target_cut_sec` 4.6:
**583 cuts, 398 video / 185 still**. Codex plates needed **281-327, call it ~300** — 185 stills plus
96-142 motion sources, the range set by how much archive survives review. A plate driven to i2v
LEAVES `img/` and lands in `motion/`; the pools are disjoint, do not double-count.

## 6b. THE STRUCTURAL BLOCKER FOR THE BACK HALF OF THE SLATE

Found at 23:20 by measuring, after EP66 and EP67 failed the same gate for what looked like two
different reasons. It is one reason.

`check_spec_satisfied` counts a mandatory plate as satisfied if it reaches a cut **either as a still
or as an i2v motion clip** -- it matches on the stem, so `R045.png` and `R045.mp4` are the same plate.
The video-share floor (`MIN_VIDEO_SHARE 0.68`) caps still cuts at roughly 0.47 x the video cuts, so an
episode with no motion can only ever place a fraction of its plates as stills. Measured:

```
slug         plates  motion  mandatory_stills
marmet           75     149  OK -- plates reach the film as motion
greene          117     193  OK
correa           99     128  OK
openfields      191       0  WILL FAIL -- [4a] blocks the render
ramirez         146       0  WILL FAIL
pinto           123       0  WILL FAIL
hyatt           127       0  WILL FAIL
```

**Every episode without an i2v pass fails, and every episode with one passes.** EP62-65 shipped
because they got i2v; EP66-69 have had none. This is not four separate staging problems and no amount
of further archive review fixes it -- EP66 needs ~21 conversions, EP67 needs enough to cover 81
unplaced plates. The `[4a]` guard fixed tonight now catches all four **before** the render, so the
cost is minutes rather than three hours each, but the queue will keep refusing them until the GPU
runs i2v for each.

Sequencing this is the single highest-value decision for whoever picks this up: i2v and a render
cannot overlap, and there are three renders queued ahead (marmet, greene, correa).

**Two staging agents reported at 23:2x that the `[4a]` guard still cannot fire and that their episode
"will render, red gate and all". That report is wrong** -- they reconstructed the old grep from an
earlier read of the file. The fix landed at ~23:00 and is present in both `scripts/_finish_episode.sh`
and the snapshot the live queue is executing (`grep -c "LOG}.satisfied"` returns non-zero in both).
Verified empirically against each episode`s real `check_spec_satisfied` output at 23:2x:

```
hyatt       exit=1  live [4a] guard: DIES  <- render blocked
openfields  exit=1  live [4a] guard: DIES  <- render blocked
ramirez     exit=1  live [4a] guard: DIES  <- render blocked
marmet      exit=0  live [4a] guard: proceeds
```

Do not re-fix it. Do check it the same way before believing any claim about it, including this one.

**Also disclosed by the EP69 agent, and worth knowing:** it ran `build_case_film_generic.py` (19 s)
while marmet`s render was live, which PD_CANON forbids. It measured marmet immediately before and
after -- 2,328 then 9,974 of 57,066 frames, progressing normally -- so no harm resulted. The
`guard_destructive.py` hook did not refuse the command, which means the rule is documented but not
enforced. Enforcing it is a real open item.

A correction worth recording, because I got it wrong first: I initially computed the still-cut cap and
concluded that five of seven specs declare arithmetically impossible values. That was wrong -- it
assumed a mandatory plate must appear as a still. It need not. The specs are fine; the missing i2v is
the whole story.

## 6c. EP68 pinto — READY, but the review is a third done and the film json is a placeholder

`check_queue_will_stall.py` reports `pinto  READY  every prerequisite present`. That is true and it
is also the smallest true thing that can be said. Measured 2026-08-12 00:20:

```
img 123 (plate gate PASS, binding=exact, 104 declared / 123 on disk)
factory 42     factory_rejected 1     motion 0
runs/qc/pinto_clip_verdicts.v001.json: reviewed 216, accepted 42, rejected 174
pool binding   subset (honest: 174 clips removed since the hash, none added)
film.json      EXISTS but is a PLACEHOLDER -- 1,160 bytes, `_placeholder` key, cuts/hook/
               captions/graphics/figures all empty arrays
Ep68Pinto      registered in remotion/src/Root.tsx (line ~774), wrapper
               remotion/src/compositions/PintoFilm.tsx
filmconfig     assets now points at asset_manifest.v003.json -- it shipped pointing at
               .v001.json, which nothing in this pipeline ever writes, and it was corrected
               through pd_edit.py. ramirez and hyatt had the same trap; EP69's generator
               (scripts/build_ep69_hyatt_filmconfig.py line 402) still emits v001
footage queries  465 distinct queries under episodes.pinto in
               config/episode_footage_queries.v001.json, lifted verbatim from
               EP68_pinto_FOOTAGE_PLAN.v001.md section 4 (the 470 table rows with at least one
               unused >=1920x1080 hit)
```

**Two things the next session must know before touching it.**

**1. The film json is a stub, not a film.** `_finish_episode.sh` rebuilds it at step [4/7] before
the render at [6/7], so the unattended queue is safe. Anything that renders `Ep68Pinto` *without*
running [4/7] first -- a hand-typed `npx remotion render Ep68Pinto`, a studio preview -- draws zero
cuts. Treat a green `check_queue_will_stall` for pinto as "the queue will not abort", not as
"the film exists".

**2. The review is 61 of 178 sheets, and 42 is the true accepted figure, already applied.** The
verdicts are recorded *and* enforced: 42 clips are in `remotion/public/pinto/factory`, one
withdrawn clip is in `factory_rejected/`, `05_visuals/factory_clip_qc.v001.json` is written with
no `unreviewed` row, and `apply_clip_verdicts.py` has nothing left to undo. Nothing is pending.

Sheets read: **1-36, 78-83, 100, 110-115, 130, 136-160, 166-178.**
Sheets never opened: **37-77, 84-99, 101-109, 116-129, 131-135, 161-165** — 117 of 178.

The 117 unread sheets carry ~303 candidates that were deliberately **not** given a verdict and were
removed from the candidate list instead. That was a choice, and the reason matters: a rejection is
written into `rejected` / `prestage_dropped`, and `stage_footage_by_title.py` globs **every**
`runs/qc/*verdicts.v001.json` and excludes those ids from **every future episode**. Marking 303
clips "nobody looked yet" would have deleted them from the whole channel's supply for a reason that
is not about the clips. So they are simply absent from the record. Re-presenting them costs nothing:
`prestage_footage_review.py --slug pinto --candidates <list>` re-runs in about a minute because the
frames are cached by content hash under `runs/qc/prestage_frames/_bycontent`.

**What the yield actually looks like, so the next session sizes it correctly.** This is not the
ramirez/hyatt curve. The shelf is a contemporary stock library and this film is USA 1968-1981, and
that mismatch, not the reviewer, is what rejects material:

| register | sheets | outcome |
|---|---|---|
| R1 car / R2 road | 1-26 | ~3 accepts in 26 sheets. Every car is modern; the two survivors are an empty cracked blacktop and a road cresting a rise, both with no vehicle in them |
| R9 paper | 78-86 | ~0. The register the film most needs, and almost every clip carries legible text, which this episode bars outright |
| R12 stone / R13 farmland | 104-122 | ~0. `arch` and `temple` return European cathedrals and Sicilian ruins; `street`/`town` return world cities |
| R16 hands / R17 clock / R18 salvage / R20 dusk | 136-178 | where nearly all 42 came from — weather, cloud, water, silhouettes, derelict interiors, anonymous hands |

So sheets 37-77 (R3 fuel, R4 factory, R5 drafting, R6 lab) and 84-99 (R10 press) are worth reading;
101-109 and 116-135 are the registers that already produced nothing.

**Do not lower the bar to close the gap.** `asset_reuse` will warn that 42 distinct video assets
against ~265 footage cuts forces ~223 repeats. That warning is correct and it is not fixed by
accepting modern cars. The episode's own FOOTAGE_PLAN section 3 already concluded the archive cannot
supply four of its registers and assigned them to generated plates R001-R080; 123 plates are staged
and signed for. If the pool cannot be grown past ~80 by reading the remaining sheets, the honest
move is an APR for the `asset_reuse` deviation, not a looser rubric.

**Things done here that the filesystem does not show:**

- `config/episode_footage_queries.v001.json` gained an `episodes.pinto` entry (465 queries + a note
  recording that the 124 `forbidden_subjects` are deliberately **not** encoded there).
- The forbidden-subject screen was applied to candidate **titles before staging**, because
  `check_spec_satisfied` matches those words against the staged filename and a title carrying
  `fire`/`crash`/`child`/`police` is an automatic build failure. 101 of 807 candidates were removed
  that way; the record is `runs/qc/pinto_forbidden_title_screen.v001.json`, a filename chosen so it
  does **not** match the `*verdicts.v001.json` glob and cannot poison other episodes.
- `check_cross_episode_reuse.py --build` was re-run (6,165 staged clips, 4,813 distinct sources) and
  `--check-list` over the final pool returned **0/43 already used** — no collision with the EP66/67/69
  threads staging from the same shelf at the same time.
- One clip was staged and then withdrawn by hand: `AR-v_244023__yellow_leaves_branch_maple_autumn_japan.mp4`.
  It looks placeless on the sheet — backlit maple foliage — but its ledger title ends in `japan` and
  `era_setting.country` is USA. The constraint comes from the spec, not from how the picture reads.

**Next two lines of work:** read sheets 37-77 and 84-99, re-present those candidates with
`--candidates`, and decide them; then let the queue rebuild the real film json at [4/7]. Nothing
else about pinto is blocked.

## 7. Open items, in priority order

1. Restart the three footage-staging agents (EP67, EP68, EP69). Their remaining blockers are
   `Ep67Ramirez` / `Ep68Pinto` / `Ep69Hyatt` missing from `remotion/src/Root.tsx`, plus finishing the
   archive pools.
2. **EP66 openfields needs at least 21 plates driven to i2v.** Without them `mandatory_stills` can
   never pass at any pool size — the video-share floor caps still cuts at 164 against 185 declared.
   The `[4a]` guard will now correctly refuse to render it. Needs GPU, which the render queue holds.
3. Rework EP70's framing to clear 85, or conclude honestly that it is a 30-minute film.
4. EP63 correa declares `distinct_video_assets: 234` against a pool ceiling of 186; it cannot pass.
   Either an owner approval against 179 delivered, or ~55 more i2v conversions. The previous session's
   judgement was to accept it, since `asset_reuse` passes at mean 1.36× and the viewer sees adequate
   variety.
5. **EP64 memphis is scheduled 2026-08-18 with captions ~0.5 s late** (p90 lag +0.509 s against a
   +0.35 s gate). Fixing it costs a 3 h re-render, a 1.7 GB re-upload, and touching a scheduled video.
   The previous session judged the GPU hours better spent on EP66-69. Reversible if the owner disagrees.
6. YouTube quota: **5,188 of 10,000 spent** today (Pacific), 3 uploads' headroom. `topic_demand_probe.py`
   does not write to the ledger — 3,500 units were recorded by hand tonight. Make it self-record.
7. ElevenLabs this slate: **$24.51** — hyatt $8.63 / pinto $8.17 / ramirez $6.97 + $0.74 top-up.
   Ledger: `runs/logs/elevenlabs_cost_ledger.tsv`.
8. 2,229 uncommitted files. Do not `git add -A` blindly — `runs/` and SSD media must never be
   committed.

## 8. The role this session was holding

**Publishing-calendar operations — uploads, scheduling, rearranging — are done by exactly one
session at a time, and this was that session.** Whoever picks this up inherits it. Do not run a
second thread that also schedules.

## 9. Standing rules worth restating because they were nearly broken tonight

- Never edit a shell script while bash is executing it; bash reads by byte offset. This destroyed one
  episode's step 7 previously. The queue runs a snapshot for exactly this reason.
- A checker that disagrees with a working pipeline is wrong until proven otherwise. Mine lied three
  times tonight: it reported "no narration" for all seven episodes (globbed `*.wav`; the real file is
  `narration_index.v001.json`), "0 snapshots" with a queue running (Python read `/tmp` as `C:\tmp`
  while Git Bash maps it to `%TEMP%`), and a quota `record()` call multiplied 3,500 by 35 because
  `units` is per-call. Measure the instrument before believing it.
- Never weaken a threshold, gate, schema or declared spec value to make something pass.
- Reply to the owner in Japanese. Artifacts in English.
