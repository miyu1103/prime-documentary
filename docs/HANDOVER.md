# HANDOVER — read this first

This is the standing entry point for any session picking up Prime Documentary. It has two halves and
they are trusted differently.

- **LIVE STATE** below is generated by `py -3.11 scripts/handover_snapshot.py`. It is measurement.
  Regenerate it whenever you doubt it. If it contradicts anything written in prose anywhere in this
  repository, including the narrative below, **the measurement wins.**
- **The narrative** is the judgement, the traps and the reasons — the part a machine cannot produce.
  The current one is linked at the bottom; older ones stay in `docs/handover/`.

Two commands to run before doing anything:

```
py -3.11 scripts/handover_snapshot.py        # refresh the block below, then read it
py -3.11 scripts/check_queue_will_stall.py   # every reason the render queue would stall
```

**Session-ending protocol.** Before a session ends, run `handover_snapshot.py`, then write that
session's narrative to `docs/handover/YYYY-MM-DD.md` and point the link at the bottom of this file
at it. A handover that only exists in a chat log does not exist.

<!-- LIVE-STATE:BEGIN -->

## LIVE STATE — measured 2026-08-19 00:47

Generated by `py -3.11 scripts/handover_snapshot.py`. Everything in this section is a
measurement, not a recollection. If it disagrees with the narrative below, believe this.

### Running now

```
  queue     2 process(es): pid 20744 (2,117 min), pid 19592 (1,843 min)
```

### Queue snapshots

The queue copies `_finish_episode.sh` at startup and runs the copy for its whole life, so a
queue started before a fix silently keeps the old behaviour.

```
  pdq.6yjDBu  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  pdq.erAxnF  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  pdq.mJAFc8  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  pdq.nN7cKM  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  pdq.WhHBPr  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  pdq.xBx9tU  4-layer-mux=yes  spec-guard-fixed=yes  srt-from-filmconfig=yes
  MORE THAN ONE SNAPSHOT -- two queues are racing the GPU. Kill all, restart one.
```

### Episodes

```
  slug          img  factory  motion   film.json  master
  marmet         75       91     149  08-13 15:54  08-12 03:48
  greene        117       33     193  08-13 14:31  08-12 05:44
  correa         99       42     128  08-12 07:15  08-12 11:40  <-- QUEUE SKIPS THIS (film older than master)
  openfields    146      191      53  08-17 14:06  08-17 18:04  <-- QUEUE SKIPS THIS (film older than master)
  ramirez       104      106      50  08-17 19:50  08-17 21:48  <-- QUEUE SKIPS THIS (film older than master)
  pinto          77       41      54  08-17 21:54  08-17 23:17  <-- QUEUE SKIPS THIS (film older than master)
  hyatt         101      131      34  08-17 23:23  08-18 00:45  <-- QUEUE SKIPS THIS (film older than master)
```

### Disk

```
  repo (C:)     42.1 GB free
  scratch      722.1 GB free
```

### Spend

```
  ElevenLabs ledger: 4 entr(ies), $24.51 recorded
  YouTube quota: Pacific date 2026-08-18 | spent 9503 of 10000 | remaining 497
```

### Git

```
  branch claude/vibrant-archimedes-2mmr5h  head 44fe4603 Quarantine rules fire under their own conditions; hyatt/pinto packaging corrected
  2567 uncommitted path(s) -- never `git add -A`: runs/ and SSD media must not be committed
```

### Will the queue stall?

```
  slug        verdict  why
  ----------------------------------------------------------------------------------------------------
  marmet      READY    every prerequisite present
  greene      STALLS   inputs: [inputs] greene: stills=117 (faces 23) factory=33 motion=193 | narration 383 chunks, 30.7 min; asset_reuse will FAIL: 55+193 distinct video assets vs 
  correa      STALLS   SILENTLY SKIPPED: film json older than master -- queue thinks it is done
  openfields  STALLS   SILENTLY SKIPPED: film json older than master -- queue thinks it is done
                       inputs: [inputs] openfields: stills=146 (faces 20) factory=191 motion=53 | narration 277 chunks, 26.7 min; asset_reuse will FAIL: 203+53 distinct video assets
  ramirez     STALLS   SILENTLY SKIPPED: film json older than master -- queue thinks it is done
  pinto       STALLS   SILENTLY SKIPPED: film json older than master -- queue thinks it is done
  hyatt       STALLS   SILENTLY SKIPPED: film json older than master -- queue thinks it is done
  MACHINE
    READY    42.1 GB free on the repo drive (need about 30)
    STALLS   6 queue snapshots exist -- more than one queue has been started
             and each runs its own copy of _finish_episode.sh. Kill all, restart one:
               E:\UserTemp\aab15\pdq.6yjDBu
               E:\UserTemp\aab15\pdq.erAxnF
               E:\UserTemp\aab15\pdq.mJAFc8
               E:\UserTemp\aab15\pdq.nN7cKM
               E:\UserTemp\aab15\pdq.WhHBPr
               E:\UserTemp\aab15\pdq.xBx9tU
  READY here means the queue can start it. It does not mean the film is good --
  check_spec_satisfied, the plate gate and the pool gate run inside the finisher.
```

<!-- LIVE-STATE:END -->

---

## Permanent operating rules that keep getting rediscovered

These are not tonight's news. They are the things that cost days when a fresh session did not know
them, so they live here rather than in a dated file.

1. **Exactly one session operates the publishing calendar** — uploads, scheduling, rearranging.
   Whoever holds this handover holds that role. Never run a second thread that also schedules. A
   `publishAt` in the past publishes immediately.
2. **Never edit a shell script while bash is executing it.** Bash reads a running script by byte
   offset; an edit mid-flight corrupts execution. This already destroyed one episode's step 7. The
   unattended queue copies `_finish_episode.sh` to a snapshot at startup for exactly this reason —
   which also means **a fix to the original does not reach a queue that is already running.**
3. **A checker that disagrees with a working pipeline is wrong until proven otherwise.** Measure the
   instrument before believing it. Real examples, all from one night: a check reported "no narration"
   for all seven episodes because it globbed `*.wav` when the real artifact is
   `narration_index.v001.json`; a duplicate-queue detector reported "0 snapshots" with a queue plainly
   running, because Python reads a literal `/tmp` as `C:\tmp` while Git Bash maps it to `%TEMP%`; and
   a quota `record()` call multiplied 3,500 by 35 because its `units` argument is per-call.
4. **Never weaken a threshold, gate, schema or declared spec value to make something pass.** If it
   cannot pass, say so with the numbers and let it be an owner decision.
5. **A gate that has never been shown to fail is decoration.** When you add or fix one, demonstrate it
   rejecting a deliberately bad input before relying on it.
6. **Fix everything in one batch before re-rendering.** A render is ~3 hours. Run
   `py -3.11 scripts/predict_acceptance.py --slug <slug>` first — it forecasts acceptance failures
   without spending the GPU. Several of its predictions self-clear inside the finisher; read
   `_finish_episode.sh` to see which, rather than hand-fixing them.
7. **The GPU takes exactly one job.** i2v and a render cannot overlap. i2v is ~206 s per clip; a
   30-minute render is ~3 h.
8. **Stale receipts lie.** Before acting on any receipt, check which file it measured and when. Two
   agents in one night went down blind alleys on two-day-old receipts.
9. **Resume a dead agent, do not respawn it.** When the API session limit kills agents — it kills all
   of them at once — resuming from the transcript keeps everything they measured. A fresh agent
   starts from nothing.
10. Reply to the owner in Japanese. Artifacts in English.

## The documents that bind

| what | where |
|---|---|
| constitution | `CLAUDE.md`, `.claude/rules/` |
| current truth, traps, one-thread-only list | `docs/PD_CANON.md` |
| how a film must be made | `docs/PD_ONE_PASS_PRODUCTION_SPEC.v2.md` |
| what gets made at all | `docs/PD_EDITORIAL_DIRECTION.v002.md` (2026-08-11 evening; supersedes `PD_EDITORIAL_STRATEGY.v001.md`) |
| the planning OS — rubric, series, gate | `config/pd_planning_os.v002.json` **is the binding gate** (owner switched 2026-08-12, 100-point reading). Build line 90, priority 100, flagship 110. `scripts/score_premise.py --show`; `--os v001` re-scores EP60-69 under the rubric they were actually judged by |
| premise inventory — ~200 owner hypotheses | `config/pd_premise_seeds.v001.json` (hypotheses, not facts — no figure from it enters a script unverified) |
| per-episode machine contract | `episodes/<EPID>/episode_spec.v001.json` |

## Current narrative

**[2026-08-12](handover/2026-08-12.md)** — the night the ship gate was rewritten. **Read §1 before
touching anything: only four classes may stop a ship now** (`config/ship_policy.v001.json`), everything
else is recorded and shipped. Five measuring instruments caught lying — the film was usually fine and the
measurement was wrong. Do not touch the title experiment before 2026-09-07 (§4). The number that should
decide the next month is in §8.

**[2026-08-19](handover/2026-08-19.md)** — three days: the archive drive H: disappeared and has not
come back, taking the whole audio library with it (rebuilt by synthesis, then replaced with seven
real Suno tracks, all registered with prompts and hashes this time). EP71's 118 plates recovered
from the Codex cache by exact record match; EP70's 160 turned out never to have been made and were
generated fresh. **EP62-64 published, EP65-68 booked, EP69 ready** — 253 shipped-frame sheets read
tile by tile, zero rejections. §5 lists eleven instruments that lied, in order; §6 is what to do
next. **EP70 is waiting on two regenerated plates that have NOT arrived yet.**

**[2026-08-16](handover/2026-08-16.md)** — three bugs and a fabricated review, none of them about
the films: a cp932 decode that killed uploads between "uploaded" and "scheduled"; a detached archive
drive crashing a step that had nothing to do; a queue launching the same episode three times. correa's
two blocking `real_person_likeness` rejections were read against the pixels and are not true — all 78
sheets re-read, PASS. EP62 and EP63 booked; EP66-69 i2v running because without it half their
purpose-made plates never reach the film. **§6: every private long-form on the channel was deleted
between 19:10 and 19:35 and nobody knows why — read it before uploading anything.**

**[2026-08-11](handover/2026-08-11.md)** — the night the queue was fixed. Six ways the machine
stalled and what was done about each; two caption burn-in defects caught before they shipped; the
measurements that overturned older canon (narration pace, CTR, audience age, and the finding that
the apparent long-form advantage is confounded by two outlier videos); EP70 chosen and then failed
its own new gate at 77/100.
