# PD ONE-PASS PRODUCTION SPEC & ACCEPTANCE CONTRACT (v2)

> **SUPERSEDED BY v3 FROM EP72 (noted 2026-08-23).** This file is the record EP19-EP71 were
> built to and its text below is unchanged, but do not build a new episode from it.
> **It contradicts itself on the hook**: row 9 says the hook is written FIRST, section B.1 says
> it is written last. Both sentences are still below. `v3` settled it -- **written FIRST** --
> and also replaced the title length (`<=60` here, **59-100** there, measured).
> For anything from EP72 onward read `PD_ONE_PASS_PRODUCTION_SPEC.v3.md`.

**Status:** BINDING for every episode from **EP19 onward**. Supersedes v1 (v1 remains the record EP17/EP18 were built to). Consolidates decisions/0002, 0004–0007 + `scripts/check_final_acceptance.py`.
**What changed from v1 (owner steers 2026-06-28):** (a) hook shortened to **~8 s** highlight; (b) captions = **1 cue per breath group**; (c) **OP/ED canonical = `Bookends.tsx`** (new row 14); (d) script must be written at **Academy-screenplay level** (film-bible method, EP18); (e) reinforced **no hard-cut stutter** + **abundant-asset fast pace**; (f) duration math uses the **measured voice pace ~173 wpm**, not the old ~150 wpm estimate.
**Purpose:** Eliminate the recurring rework spiral. Every problem the owner keeps hitting is pre-specified here with an **exact spec** + a **measurable gate** + a **verification command**. Codex must build the FIRST render to satisfy this whole table.

## THE ONE RULE
> **"Done" ≠ validator PASS. "Done" = the independent acceptance script measures the real `final.mp4` (+ provenance) and ALL hard gates pass.** The producing agent may NOT hand-write its own quality gate. Self-reported QC is rejected (CLAUDE.md invariant 13, rule 17). A new revision re-runs the full gate.

Final gate command (must exit 0 before "package_ready"):
```
./.venv/Scripts/python.exe scripts/check_final_acceptance.py <NN> --json
```
The runtime band is read from the episode's duration profile (standard 11.5–12.5 / **mid 27–33** / feature 55–65), NOT hardcoded. **EP19/EP20 = mid (target 30:00).**

---

## A. THE FAILURE-MODE → SPEC → GATE TABLE (the contract)

Each row is a thing that has gone wrong. Build to the SPEC; the GATE is how it's proven; nothing ships until every hard GATE passes.

| # | Recurring problem | EXACT SPEC (build to this) | GATE (hard, measured on real file) | Verify |
|---|---|---|---|---|
| 1 | **BGM doesn't play** | Continuous **library music bed**, ducked under VO to ~ -22 LUFS (VO at front), one track per chapter from the 8-category set. **The bed must stay audible UNDER the VO — ducking floors at ~ -22 LUFS, never to silence.** No chapter without a bed except the deliberate silence beat. | `bgm_present`: no silent stretch > **25 s**; music energy present across the timeline AND **never drops below the audible floor during VO**. | check_final_acceptance |
| 2 | **Voice is "different"** | Narration master = **ElevenLabs**, `VOICE_ID=nPczCjzI2devNBz1zQrb`, `model_id=eleven_multilingual_v2`, `stability≈0.35`, `similarity_boost≈0.80`, style 0, speaker_boost on. SAPI/local = timing draft ONLY, never shipped. | `voice_is_master`: narration provider contains `eleven`, never `sapi/windows/zira/local`. | check_final_acceptance |
| 3 | **Narration ≠ captions** | Captions are **forced-aligned to the rendered ElevenLabs audio** (not pasted from the script). Text must match spoken words verbatim. | Caption text ↔ narration token match **≥ 99%**; `captions_final` sidecar `.srt` non-empty, covers **≥ 95%** of runtime. | check_final_acceptance + caption-diff |
| 4 | **Captions break badly / hard to read** | **1 cue = 1 breath group** (息継ぎ〜息継ぎ): split the line where the narrator actually breathes, so a cue appears/clears with the breath. Within that: ≤ **2 lines/cue**, ≤ **42 chars/line**, never split a word or a preposition off its noun, **1.0 s ≤ cue ≤ 6.0 s**, ≥ 2 frames gap, no orphan single-word cues, reading speed ≤ 17 cps. Burned-in style = brand font, bottom-safe, drop-shadow. | Caption QC json: 0 violations; cue boundaries align to narration pause/breath points. | caption QC script |
| 5 | **Images coarse / low quality** | **Hero stills are generated by Codex** (owner choice 2026-06-28) from the per-image prompt pack, **BEFORE edit**, at the highest quality the model allows, **long edge ≥ 3840 px** (upscale + denoise + brand LUT grade if the raw gen is smaller). Reject any still blurry, artifacted, bad-anatomy, with text, or a real-person likeness. | Image QC: every used still long-edge ≥ 3840, sharpness ≥ threshold, 0 NEG-violations. | image QC script |
| 6 | **Not "Max quality"** | Render **libx264 `-preset slow -crf 16` (≤17)**, `-pix_fmt yuv420p`, **bt709**, 1920×1080 (4K master if source allows), **never NVENC**. Audio `aac 320k`. Integrated loudness **-16…-12 LUFS**. Driven by `remotion.config.ts` canonical values (png/crf16/yuv420p/bt709/aac320k/all-core/angle). Per-chapter render → concat. | `runtime_band` + `loudness` pass; crf/preset/pix_fmt asserted from render log. | check_final_acceptance |
| 7 | **Downloaded factory assets barely used** | **Use the 221 GB / 65k shelf abundantly.** Every span carries ≥ 1 layer; across the episode use **≥ 1 distinct factory clip per ~30 s** (denser than v1's 45 s — owner wants abundant, varied material); no single clip reused > 3×; b-roll must match the span's `search_keywords`. | Asset-usage audit: distinct_factory_used ≥ runtime/30; max-reuse ≤ 3; 0 empty spans. | asset-usage script |
| 8 | **Animation is weak / cuts "stutter" (かくっ) / image sits still and bores** | **No static image, ever; no held frame > 2 s; no naked hard cut.** Each still gets MovingImage (Ken Burns ≥ 6% zoom or parallax), b-roll = MovingVideo, hero beats get organic loops (SVD) + ink/particle overlays. **Transitions are designed: 0.3–0.5 s crossfade/dissolve (Sequences overlap by the transition length so there is no 1-frame black/jump), motion direction carries through the cut (no velocity reset = the "かくっ"), motion blur (Trail) on fast moves.** **Fast pace: average shot ≤ ~6 s; the picture is always changing.** **Premium-motion tier available (real 3D depth via `@remotion/three`, Blender EEVEE/Cycles hero render, animated figures) — see `docs/PD_MOTION3D_HERO_AND_FIGURES_SPEC.md`; additive, does not replace existing motion components.** | Motion audit: 0 zero-motion spans; 0 hard cuts without a transition; 0 freeze > 2 s; mean shot length ≤ ~6 s. | motion audit script |
| 9 | **Hook — VOICED FROM 0:00, written FIRST — BINDS FROM EP66 ONWARD** (owner decision 2026-08-10: "66話から"). **EP62-65 are mid-flight and are NOT changed** — they keep the silent 11.5s opening and must render bit-identical; applying this row to them means re-rendering four finished films. | **The narrator speaks from the first second.** The old rule asked for a 0:00–0:08 montage written LAST; it did not say silent, but the engine put the narration inside Body at `hookSeconds + OPENING_SEC`, so every episode opened with **11.5 seconds in which nobody speaks**. Measured across 30 episodes in seconds: 87.6% at 10s → **76.9% at 15s** → 71.4% at 20s → 60.4% at 30s. **The steepest loss in the entire film is 10→15s at 2.13 points per second**, six times the rate after 60s — and it falls inside that silence. The six best openings hold **91.1% at 20s**; the six worst fall to 56.3%. **Hook = 0:00–~0:20, voiced throughout, written FIRST** (before the body, with the title and thumbnail, so the promise and its payment are designed together). Shape taken from the channel's own best opening (katz, 0.818): a time, a place, one person doing one thing, ending on something the subject does not know. **Do not summarise the outcome** — tyler summarised inside ten seconds and measured 0.447. The strongest single beat lands in the **10–15s window**. At 0:20 an unanswered question is standing. The brand OPENING moves off the 10–15s window and becomes an overlay over continuing footage and narration. Engine: `leadSeconds` is opt-in and defaults to the old value, so EP62–65 render bit-identical. | `hook_added`: hook present at head; **narration audio starts at 0:00 ± 0.5s**; promise-payoff = the teased reveal appears later; no outcome stated in the first 10s. | check_final_acceptance + payoff QC |
| 10 | **Not Hook/Opening/Body/Ending** | Mandatory 4-part spine: **hook** (~0:08) → **opening** (thesis + "what you'll learn", channel ID; gold `BrandOpening` lands here, after the hook) → **body** → **ending** (emotional payoff + single clear CTA; `BrandEndcard` at tail). **The ending must EARN the Like**: deliver the promised reveal, land the emotional/thematic punch, then a warm, specific ask ("if this changed how you see X, hit like") — not a generic beg. | Structure gate: all four roles present and ordered; CTA in last 30 s. | structure QC |
| 11 | **No thumbnail prepared** | **≥ 3 thumbnail variants** rendered as Remotion `<Still>` at **1280×720** (`BRAND.thumb`) BEFORE package_ready, **pre-generated by Codex** (background art + headline); files exist; a `selected` is chosen. Upload manual (PD-001). | thumbnail_present: ≥ 3 PNG 1280×720 + 1 selected. | thumbnail QC |
| 12 | **Thumbnail not striking ("派手")** | UPPERCASE headline **≤ 3–4 words** (auto-split), one emotional/curiosity idea, **huge subject, very high contrast**, black/navy bg + **gold `#E5B53A` or electric `#1F6BFF` accent** + white/silver text, **bold/loud composition**, readable at 320 px. No real-person likeness. No clutter. | thumbnail QC: word-count ≤4, contrast ≥ threshold, legible at 320px. | thumbnail QC |
| 13 | **Thumb/title not CTR-max** | **Title follows the measured winner's shape, not a length cap.** Pattern: `[dramatic present-tense clause] — [what the powerful party does NOT know] \| The Case of [Name]`. **Length 59–100 chars (median ~82)** — the old “≤60” was invented, and PD's own CTR under it is **1.38%** against a 4–6% target. Measured 2026-08-10 from the Data API on **Unseen** (`UCVtHlqfvXn-6m6A2IWUB8MQ`, 1.41M subs, 116 videos, 328M views, narrated case documentaries = PD's exact product): all 25 recent titles 59–100 chars, every top-ten title >78, and the **shortest title (29 chars) is the worst performer**. Real name as a searchable suffix; no case citation, no doctrine, no question form (PD's three question-form titles are its three worst videos). Third person — PD's second-person titles median 4 views vs 17 for third. Ship **A/B title×thumb variants** and, where available, run Studio Test &amp; Compare so the next decision is made on PD's own numbers. **Honest limit:** Unseen is true crime and PD's traffic data files this channel next to money-and-institutions explainers — transfer is unproven, adopted because it is the only measured winner available. | Packaging gate: title 59–100 chars, pattern present (clause + name suffix), no question form, no citation/doctrine, ≥2 A/B variants, promise-payoff = true. | packaging QC |
| 14 | **OP/ED inconsistent / off-brand — and motion written as adjectives** | **Anything that moves is specified in numbers, not adjectives** (transplanted 2026-08-10 from the owner's opening design-doc rules, `C:/Users/aab15/CLAUDE.md` — half of which PD had no equivalent for): every motion **eased, constant-linear forbidden** (`spring()` or `Easing.out(Easing.cubic)`); **opacity alone forbidden** — always with `translateY`/`scale`; multiple elements **stagger** a few frames apart; **≥3 layers** behind the subject; seconds derived from `fps`, **never a hard-coded frame count**, numbers as constants; and the design document states **start frame, end frame, displacement and easing type per motion** — "かっこよく" is not a specification. `eased_motion` + the linear ban are enforced by `leveled_animation`; the rest is reviewed at design time. **OP/ED canonical = `remotion/src/components/Bookends.tsx`** (`BrandOpening {seriesLabel,title,subtitle?}` / `BrandEndcard {channel?,ctaLine?,cadenceLine?}`; `OPENING_SEC=3.5` / `ENDCARD_SEC=9`; signatures + constants FIXED). Adopted "派手" look (sunrise bg + PD monogram + Trail motion + gold spark/streak/flash + ED CTA pulse). All Premium compositions + `CasePremiumFromRoughCut` import it = single source of truth; do not fork (invariant 14). Gold `BrandOpening` lands AFTER the hook, not at frame 0. **Optional premium 3D hero plate may sit behind `BrandOpening` / as the cold-open hero (Blender render as `OffthreadVideo`) — see `docs/PD_MOTION3D_HERO_AND_FIGURES_SPEC.md`; it is a backdrop, Bookends stays the OP/ED canonical (invariant 14).** | Bookends imported (not re-implemented); OPENING_SEC/ENDCARD_SEC unchanged; OP after hook, ED at tail. | code review + structure QC |
| 15 | **Script is merely "correct", not gripping** | **Script written at Academy-screenplay level**: a `EPnn_FILM_BIBLE` (EP18 method) — fact-locked but built as cinema: cold-open question, dramatic three-act arc, escalating stakes, a human throughline, scene/sequence beats, motif & theme, turn & payoff, restrained narration that lets images carry weight. **Drama is built on the facts, never by inventing them** (R2/R3 causation locks still bind). Sized to the duration: **mid 30:00 → narration ~27 min → ~4,600–4,700 words at the measured ~173 wpm** (NOT the old ~150 wpm estimate — that under-filled runtime). | Script QC: film-bible present; FK grade in band; word-count in the wpm-derived target; promise-payoff; no fabricated fact. | script QC + fact_recheck |
| 16 | **Watched-to-end + Like rate (retention/engagement)** | The script is engineered for a **flat-to-rising retention curve**, not just clarity: an unresolved question opened in the first 8 s and held until the end; **open loops** ("but that is not what happened…") closing late; **re-hooks every ~2–3 min** (a new question/turn before interest decays); no dead air, no flat exposition stretch; rising stakes into a payoff that resolves the cold-open. Goal: viewers watch to the end, and the emotional payoff makes the Like feel deserved (works with row 10's earned CTA). | Retention design check: cold-open loop closed at end; ≥1 re-hook per ~3 min; no >20 s low-tension flat stretch in the beat map. | script QC (retention map) |

> The **catch-all** is THE ONE RULE — the independent acceptance script is the final wall. New defect classes are added here as a row AND a check (no silent caps, rule 17).

---

## B. WHAT MUST EXIST BEFORE CODEX IS ASKED TO RENDER (left-process gate)
Claude does not hand off until these are locked, so Codex can hit rows 1–15 on the FIRST pass:
1. `EPnn_FILM_BIBLE.<v>` + `script.annotated.<v>` — Academy-level (row 15), **hook (~8 s) written last + promise-payoff verified**, explicit hook/opening/body/ending roles, word-count in the 173-wpm target band.
2. `shotlist.<v>` — every span has asset_type + motion + transition + factory `search_keywords` (drives rows 7–8); average shot ≤ ~6 s; designed crossfades.
3. `ai_prompts.<v>` — **per-image prompt pack** (one careful prompt per hero still) with upscale target ≥3840 noted (row 5), for Codex bulk pre-generation.
4. `thumb_prompts.<v>` + headline/kicker candidates (rows 11–13) — for Codex pre-generation of thumbnail art.
5. `fact_recheck.<v>` packet (R2/R3) — facts/quotes locked verbatim.
6. Duration profile in `manifest.target_duration_minutes` (EP19/20 = 30 → mid band).

## C. VERIFIER STATUS (infra to keep current)
`scripts/check_final_acceptance.py` machine-enforces (measured on the real file):
rows **1 (bgm incl. audible-floor under VO + `bgm_ending` resolve), 2 (voice master), 3 (caption coverage), 4 (caption format + `caption_narration_match` 100%-grade token match), 6 (runtime band + render res), 8 (`motion_present` no-freeze AND `animation_density`: near-still ≤10% of runtime / single hold ≤3s — kills the 紙芝居/slow-Ken-Burns a freeze check misses), 9 (hook headroom), 11 (≥3 thumbnails @1280×720 + selected)** + `images_present` (black), loudness.
**Now also coded (2026-07-04):** row 7 → **`footage_diversity`** (distinct/total ≥0.40, no clip reused >4×, generic symbols like 天秤/gavel ≤2×); row 12 → **`thumbnail_visibility`** (selected thumb luma mean ≥33 + contrast floor — the "しょぼい/暗い/CTR下がる" reject class); row 14 → **`op_ed_bookends`** (canonical BrandOpening+BrandEndcard imported, not forked); + `structure_4part` (row 9), `image_resolution` (row 5, ≥3840). **Still manual:** row 13 (title ≤60 / A-B), row 15 (film-bible craft review).

### C1. Ship Gate — the schedule is LOCKED to a green receipt (binding, all threads)
`docs/PD_SHIP_GATE.md` + `.claude/rules/19-ship-gate.md` are canonical. No long-form is scheduled until the gate has measured the exact render and issued a receipt:
1. `check_final_acceptance.py <ep> --render <final.mp4> --emit-receipt` → writes `09_package/acceptance_receipt.v001.json` (per-check results + `video_sha256`).
2. `upload_schedule_case_v001.py --ep <slug>` **hard-refuses** to upload unless a receipt exists whose `video_sha256` matches the file and whose only tolerated hard failure is `runtime_band` (the one owner-accepted deviation). Self-certified "done" is impossible; the gate is never weakened to pass (invariant 15).

### C2. Premium animation engine (row 8 implementation of record)
`remotion/src/compositions/CaseFilm.tsx` is the long-form engine: designed **motion-blurred cut transitions** (push-in/rise/slide, spring-eased, decaying blur), **mask-reveal kinetic typography** ("切り上がり") + gold kicker chip/underline, `@remotion/motion-blur` **Trail** on beats + hook, over the upgraded **Bookends** (row 14). The per-beat **`AmbientMotion`** overlay (drifting bokeh + orbiting glows) composites on top so no frame is static. Banned: left→right vertical sweep line, full-screen yellow/gold wash, plain zoom/pan-only.

## E. CODEX DETERMINISM (why the handoff keeps drifting, and the fix)
Codex builds from the design doc **standalone** (it does not read CLAUDE.md or memory). When the result "isn't what I wanted," the cause is an under-specified doc, not Codex. So the per-episode handoff MUST be written to leave **zero interpretation room**:
- **Numbers, not adjectives.** Never "fast / dramatic / subtle." Always the exact value: start/end frame, easing type (`spring{damping,stiffness}` or `Easing.out(Easing.cubic)`), zoom % per shot, transition length in seconds, BGM duck level in LUFS, caption cue boundaries, shot order with per-shot seconds.
- **Per-asset, per-shot tables.** The `shotlist.<v>` lists every shot: id, source file/prompt id, seconds, motion (Ken Burns vector or parallax), transition in/out, factory layer, caption span. No "etc." / no "and so on."
- **One image = one prompt.** `ai_prompts.<v>` has a numbered, self-contained prompt per hero still (subject, composition, lens, light, palette, mood, negative list, upscale target ≥3840). Same for each thumbnail variant.
- **State the value even when obvious** (resolution, fps, voice id, crf, color space, audio bitrate) so Codex never guesses a default.
- **The gate is the contract, not the chat.** Codex must run `check_final_acceptance.py <NN>` itself and not declare done until exit 0. The acceptance script is what makes "thought-through" survive contact with Codex — if a behavior matters, it must be BOTH written as an exact spec here AND enforced by a measurable gate (rows 1–16). A requirement with no gate WILL drift; add the gate.
- **Acceptance restated at the top of every Codex prompt**: the exact rows the render must satisfy + the verify command, so Codex self-checks before returning.

## D. DEFINITION OF DONE (per episode)
- `check_final_acceptance.py <NN>` → exit 0 (all hard gates).
- Rows 4,5,7,8,11,12,13,15 measured (script or manual) → 0 violations.
- first-cut / title-thumbnail / pre-publish fact re-check / scheduling = owner gates, in order.
- Then, and only then, `state = package_ready`.

*This spec is the contract. Building to "looks good" is the bug. Building to this table is the fix.*
