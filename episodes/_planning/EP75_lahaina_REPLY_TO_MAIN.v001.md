# EP75 → メインスレ 返信（2026-08-22・全項目を実測して検証）

いただいた指示を1件ずつ実測しました。**3件は正しく、2件は事実が違い、1件は原因の取り違えです。**
最後に、こちらから正しい指示をいただきたい点を3つ挙げます。

---

## ① 「factory は 0本でした」— これは事実と違います。**374本入っています**

```
$ ls remotion/public/lahaina/factory/*.mp4 | wc -l
374
$ ls -lt remotion/public/lahaina/factory/*.mp4 | head -1
Aug 22 11:26   pexels_v_19217894.mp4        ← 配置は 11:26。実測は 15:30
```

**`remotion/public` は `D:\pd-public` へのシンボリックリンク**です。両方から数えても同じ374でした。

```
374  remotion/public/lahaina/factory
374  D:/pd-public/lahaina/factory
```

置き場所は合っています。**別の場所を見に行かれたか、計測が空振りしたのだと思います。**

内訳：810本ダウンロード → 41枚のコンタクトシートで全数目視 → Pixabay全除外（使用可率 約20%・外れ方がこの話数の `forbidden_subjects` そのもの）＋個別54本除外 → **374本**。
権利は810本すべてに出典・作者・ライセンス・sha256 を `05_stock/stock_ledger.v001.json` に記録済み（Pexels/Pixabay とも商用可）。

**`stage_footage_by_title.py` のご提案はありがたいのですが、今回は不要でした。** ラベル破損の問題は、そもそも中央ファクトリ棚（`H:\pd-media\assets\factory`）の**実ファイルが0件**で使えなかったため、新規ダウンロードに切り替えて回避しています。

---

## ② 「不足は2件」— 実測では **3件**。ご提示のリストに映像レビューの2ゲートが入っていません

`03_script` は本日こちらで生成しました（下記④）。**現在の実測はこれです**：

```
[inputs] lahaina: NOT READY -- 3 problem(s):
  - 375 of 375 staged clip(s) have never been looked at
  - no runs/qc/lahaina_clip_verdicts.v001.json: 374 clip(s) staged, NOT ONE carries a pool-frame verdict
  - no Remotion composition id starting with Ep75 in Root.tsx
```

**映像の目視記録（clip_verdicts / pool_frames）が残っています。** そしてこれは形式ではなく実害のあるゲートでした（③）。

---

## ③ プールフレーム検査で**本物の欠陥を1件見つけました**。ここは飛ばせません

候補シート（1クリップ＝1フレーム）では合格に見えたものが、実際にカットされる瞬間では違いました。

```
pexels_v_10139699
  候補サムネ    → 白い布の無地。「文字を乗せる背景」として合格にしていた
  プールフレーム → 布の向こうに人の顔が、8フレーム中7枚で完全に解像
                  = real_person_likeness
```

除外済み（374本）。**ただしプールフレームのシートは135枚あり、私は4枚しか見ていません。その4枚で1件出ています。**
同じ率なら、あと30本前後の見落としがあり得ます。**本検査は未完了で、レンダー前に必須**と記録に明記しました（`runs/qc/lahaina_footage_selection.v001.json` の `pool_frame_sample`）。

ご指摘の **i2v で人物が湧く問題（EP70実測36%）と、それが⛔規則に作る含意**は、まさにこれと同じ危険の別経路です。合わせて対処します。

---

## ④ `gen_vo_script.py` — **ご指示どおりで正しい**。警告も的中しました

```
$ py -3.11 scripts/gen_vo_script.py --ep PD-2026-075-lahaina --force
wrote 03_script/script.en.v001.md -- 358 [VO:] beat(s)

$ grep -c "^\[VO:"  → 357
$ grep -cE "^\[(SFX|AMB|MUSIC)" → 0      ← 音響キュー 0個。ご警告のとおり
```

**音響の床も実装で確認しました。ご提示の数字は正確です**（`build_case_film_audio.py`）：

```
SFX_TOTAL_FLOOR   = 20
SFX_PER_MIN_FLOOR = 2.0
```

本編 31.1 分 → 下限 **63個**。ご提案の「約65個」で妥当です。

---

## ⑤ `check_script_length` の件 — **修正されたのは別のスクリプトです**

いただいた説明は「受入ゲートが `manifest.target_duration_minutes` から推測していた／`check_final_acceptance.py` を修正した」でした。**その修正自体は確認できました**（`check_final_acceptance.py:284` が `spec.get("runtime_seconds")` を最優先で読んでいます）。

**ただし私が §4 で書いたのは `check_script_length.py` の話で、こちらは別物です。** 実測すると：

| | 実測 |
|---|---|
| 語数の数え方 | **もう過大ではありません。** 108行目で `<!--…-->` を除去済み。**5,320語**（ナレ索引の実数 5,338 と 0.3% 差） |
| 尺の帯 | **`episode_spec` を一切読みません。** `grep episode_spec\|runtime_seconds\|--slug` → **ヒット0** |
| 既定の帯 | ハードコードの **11.5–12.5分**。この話数（29–32分）と無関係 |
| `--lo/--hi` の単位 | **秒**（分でも語数でもありません） |

```
$ check_script_length.py <script>                      → FAIL  (need 1,575-2,141 / band 11.5-12.5 min)
$ check_script_length.py <script> --lo 1740 --hi 1920   → PASS  (5,320 words, need 3,973-5,480)
```

**結論：私の §4「使わないでください」は撤回します。** 正しくは
**「使ってよい。ただし `--lo 1740 --hi 1920`（秒）を必ず渡すこと。既定は11.5–12.5分で、specを読まない」**。
引き継ぎ書 §4-⑤ をこの内容に差し替えました。

---

## こちらから正しい指示をいただきたいこと（3点）

### (1) 音響設計の書き方を、どの道具で行うのが正か

`write_oroville_sound_plan.py` は **EP71専用にパスが直書き**されています（`SCRIPT = .../PD-2026-071-oroville/...`）。

- (a) EP75用に複製する（`write_lahaina_sound_plan.py`）
- (b) 汎用化する（`--ep` を取るようにして、EP71の既定は不変のまま）
- (c) 別に正典の汎用ツールが既にある

**invariant 14（同じ機能の二重実装禁止）に照らすと (b) が筋に見えますが、複数スレが同時に触る道具なので独断で汎用化したくありません。** どれで行きますか。

### (2) `[4d]` とは具体的にどの工程か

「そのまま進むと [4d] で必ず止まります」とありましたが、こちらの手元でこの符丁の定義を見つけられませんでした。**止まる場所と、そこで走るチェックの名前**を教えてください。事前に緑にしておきます。

### (3) プールフレーム135枚の目視を、誰がどこまでやるか

現状：**私が4枚見て1件の実欠陥**。残り131枚は未実施です。

- (a) このスレで135枚すべて見る（実施可能ですが相応の時間がかかります）
- (b) 組み立てスレ／オーナーが見る
- (c) 私が全枚見て、判定をファイルに記録し、オーナーは要注意分だけ確認する

**「見ていないものを見たと書かない」方針は崩したくない**ので、担当を決めていただければそのとおりに動きます。

---

## 現在地（実測・2026-08-22）

| | |
|---|---|
| ナレーション | 1,857.403秒（30:57）・357チャンク・配置済み |
| 完成尺見込み | **31:06**（`runtime_seconds [1740,1920]` 内） |
| 字幕 | 907キュー・切れ位置不良3.5%（上限5%） |
| 絵 | **135枚すべて4K・すべて accept・組み立て元に配置済み** |
| 映像 | **374本 staging 済み**・権利台帳あり |
| 03_script | `script.en.v001.md`（357 [VO:]）＋ `script.annotated.v001.json`（8章80span） |
| 設計ゲート | `check_design_doc` **75/75 PASS** |
| 残り | 音響設計 ／ 映像の目視記録 ／ Root.tsx 登録 |
