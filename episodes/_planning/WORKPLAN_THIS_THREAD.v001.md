# このスレの作業計画 v001

**担当範囲：TikTokの作り直しと、両プラットフォームの配信カレンダー。**
（配信カレンダーは1スレだけが触る決まり。`docs/PD_CANON.md` §3）

基準時刻 **2026-08-10 03:34 JST**。数字は必ず測り直すこと。

---

## A. いま走っているもの

| | 状態 | 確認 |
|---|---|---|
| EP62-65 の TikTok用レンダー | **6/12** 完了 | `ls remotion/out/short2[5-7][0-9]_tt.mp4 \| wc -l` |

完了後に下の A-1 → A-2 を続けてやる。**これが終わるまで次の日の投入は始めない。**

### A-1. 12本ぶんのカバーを焼く

```bash
cd remotion
BUNDLE="${TEMP:-/tmp}/pd_cov"
npx remotion bundle --public-dir=./public_min --out-dir "$BUNDLE"
for n in 259 260 261 262 263 264 265 266 267 268 269 270; do
  npx remotion still "$BUNDLE" "ShortThumb-short${n}" "out/short${n}_ttcover.png" --props='{"layout":"tt"}'
done
ls out/short*_ttcover.png | wc -l          # 134 になるはず
```

背景画像が `public_min` に無いと 404 で落ちる。落ちたら
`py -3.11 scripts/preflight_short_render.py <番号> --fix-mirror` を先に。

### A-2. キューを作り直す

```bash
py -3.11 scripts/build_tiktok_queue.py --all --out C:/temp/studio_auto/tt_queue_full.json
cp C:/temp/studio_auto/tt_queue_full.json C:/temp/studio_auto/tt_queue.json
```

**134本**になる（現在122本＋EP62-65の12本）。予約済み3本は自動でスキップされる。

---

## B. TikTok：131本の投入（毎日）

### 現在地

- 投稿済み **3本**（8/10 10:00・14:00・18:00、すべてカバー付き）
- 残り **131本**
- **本日分は上限到達済み。** 8/11 から。

### 毎日やること

```bash
cd /c/temp/studio_auto
taskkill //F //IM chrome.exe ; sleep 5
node start_chrome.js ; sleep 18
node tt_batch_clean.js <開始日 YYYY-MM-DD> <その日に既に埋まっている枠数>
```

**1日15本を上限にする。** 上限に当たれば自動で止まるが、当たる前に止めるほうが安全。
上限に当たると `CHECK_LIMIT_REACHED` が出てバッチ全体が停止する。**回避しないこと。**

### 開始日の決め方

予約枠は **1日4本（10:00 / 14:00 / 18:00 / 22:00）**。
埋まっている最後の日の翌日から。8/10 は3枠埋まっているので、次は `2026-08-10 3`（=22:00から）。

### 見込み

| | |
|---|---|
| 投入 | 131本 ÷ 15本/日 ＝ **約9日**（8/11〜8/19） |
| 公開 | 1日4本 ＝ **9月中旬まで**埋まる |

### 毎回そのあと確認すること

```bash
# 予約が実際に入ったか（公開グリッドではなく Studio の一覧を見る）
node -e "..."   # tt_run.log の 'X/Y scheduled' でも可
```

カバーが乗らなければ `COVER_DID_NOT_STICK` で止まる。止まったら**押し通さない**。

---

## C. YouTube：ショート83本の投入（毎日）

### 毎日1回

```bash
cd /c/Users/aab15/Documents/prime-documentary
bash scripts/daily_shorts_push.sh          # 長尺が同じ日にあるなら引数 1650
```

索引キャッシュ削除 → 実チャンネル取得 → 台帳同期 → クォータ内アップロード →
再取得して衝突報告、まで通しでやる。

- **クォータは 16:00 JST に回復**（Pacific 深夜0時）。それ以前に叩いても0本
- 1日 **6本** が上限（`videos.insert` 1600 × 6 ＝ 9600 / 10000）
- **12:00 JST は長尺専用。**ショートは 6/9/18/21

### 見込み

83本 ÷ 6本/日 ＝ **約14日**（8/10〜8/23）。公開は1日4本。

### 長尺の予約

すでに入っているもの：8/10 norfolk / 8/13 willingham / 8/14 morton。
**12:00枠は 8/15 まで埋まり、8/16 以降が空き。** 新しい話が焼けたらここへ。

---

## D. 制作の穴：ショート9本

195本計画（65話 × 3本）に対して **186本**。

| 話 | 現状 | 不足 |
|---|---|---|
| EP038 | 2本 | 1本 |
| EP050 | 1本 | 2本 |
| EP060 | 0本 | 3本 |
| EP061 | 0本 | 3本 |

**投稿枠が尽きる前に埋める。** B と C の投入が終わる8月下旬には、この9本が
次の在庫になる。設計 → レンダー（YouTube用・TikTok用の両方）→ カバー、まで要る。

**TikTok用は Composition の登録が別途要る**（忘れると12本まとめて空振りする）：

```bash
py -3.11 scripts/register_tiktok_compositions.py --apply
```

---

## E. 終わったあと・様子を見るもの

| 項目 | いつ | 何を見るか |
|---|---|---|
| カバーの実物確認 | 8/10 10:00 以降 | 公開されたタイルにカバーが出ているか |
| ハッシュタグの妥当性 | 次の投入時 | TikTok自身の候補表示（投稿数付き）で4つを見直す |
| TikTokユーザー名 | 期間を置いて | `prime.documentary8` は自動生成風。いまは変更を受け付けない |
| プロフィールのリンク欄 | フォロワー1,000人到達時 | そこまでYouTubeへの導線はテキストのみ |

---

## F. やらないこと

- **著作権チェックを押し通さない。**「投稿に進みますか？」を押すとチェックが止まる
- **1日上限を回避しない。**「上げすぎ」という警告そのもの
- **配信カレンダーを他スレと同時に触らない**
- **視聴のある動画を削除しない**（`--force` なしでは消えない設計）
- **古いリビジョンのファイルを書き換えない**

---

## G. 罠と作業のやり方

`docs/PD_CANON.md` §7（踏み抜いた罠）と §8（作業のやり方）。
今回のスレの失敗全記録は `docs/PD_RETRO_20260810_TIKTOK_AND_CALENDAR.v001.md`。

作業中に自分へ問う4つ：

1. このログは「やったこと」か「変わったこと」か
2. この検出器を、失敗するはずの入力で試したか
3. 証拠が食い違うとき、権威あるのはどちらか
4. 破壊的操作の対象を、定義的な属性で選んだか。件数を突き合わせたか
