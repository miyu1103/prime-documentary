# EP75 lahaina — 現在地（2026-08-22 実測）

このスレ（素材・台本・音響・目視QC）の担当分の状態です。**すべて今日その場で数えた実数**で、
申告値は1つもありません。数え直すコマンドを各行に付けてあります。

公開 8/29 ／ 予約 8/25 昼。

---

## 1. できているもの（ファイルの場所つき）

| もの | 実測 | 置き場所 |
|---|---|---|
| 台本（読み上げ） | 357ビート | `episodes/PD-2026-075-lahaina/03_script/script.en.v001.md` |
| 台本（注釈つき） | 8章・80スパン | 同 `03_script/script.annotated.v001.json` |
| ナレーション音声 | **1,857.4秒（30分57秒）/ 357チャンク / 42.5MB** | `remotion/public/lahaina/narration.mp3` |
| 字幕 | **907キュー**・切れ位置不良3.5%・dangling 0 | `episodes/PD-2026-075-lahaina/08_edit/captions.final.v001.srt` |
| 絵（プレート） | **135枚**すべて4K・すべて accept | `remotion/public/lahaina/img/H001.png` … `H135.png` |
| 深度マップ | **135枚（135/135＝100%）** | 同 `img/H0xx_depth.png` |
| 除外した絵 | 7枚 | `remotion/public/lahaina/img_rejected/` |
| 映像素材 | **374本** staging 済み | `remotion/public/lahaina/factory/` |
| 素材の権利記録 | 810本ぶん（出典・作者・ライセンス・sha256） | `episodes/PD-2026-075-lahaina/05_stock/stock_ledger.v001.json` |
| 音響キュー | **76キュー・2.453/分・PASS** | 台本内の `(SFX:)` ／ ルール `config/sound_rules/lahaina.json` |
| 目視QCの判定 | 50/135シート読了 | `runs/qc/lahaina_clip_verdicts.v001.json` |

数え直す:

```bash
ls remotion/public/lahaina/img/*_depth.png | wc -l      # 135
ls remotion/public/lahaina/factory/*.mp4  | wc -l      # 374
grep -c '^[0-9]*$' episodes/PD-2026-075-lahaina/08_edit/captions.final.v001.srt   # 907
py -3.11 scripts/build_case_film_audio.py --ep PD-2026-075-lahaina --out out_qc/_x.json --dry-run
```

**測るときの注意（今日踏んだ）**: リポジトリ直下からの再帰検索では `remotion/public` 配下が
返ってきませんでした。**パスを直接指定すれば出ます。** 「0本」を見ても、まずパスを疑ってください。

---

## 2. 音響ツールは汎用化して納品済み

```
scripts/write_sound_plan.py --slug <slug> [--rules <json>] [--dry-run]
config/sound_rules/<slug>.json
```

ご指示の4点はすべて実装済みです。slug からパス解決／kind は `build_case_film_audio.ONESHOT_MAP` を
**実行時 import**（コード側に定数なし・解決不能な kind は書き込み前に拒否）／anchor はビートが実際に
話す語のみ・不一致はスキップして件数報告／既存 `(SFX:` があれば二重書きを拒否。

追加した安全弁は `--dry-run` と床（2.0/分・20個）に対する不足数表示です。密度不足のまま書いて
[4d] で気づく往復を、書き込み前に潰せます。

ご指示どおりミキサーを単独で回して実出力を読みました（[4d] のメッセージは信用していません）:

```
SFX cues: 76 (before dedup 78, dropped 2); word-triggered=68; unmapped=0
density: sfx/min=2.453 (floor 2.0), distinct_beds=7 (floor 4), coverage=1.0 (floor 0.85) -> PASS
```

`unmapped=0` なので綴り違いによる無音落ちはありません。
**EP70/EP71 を寄せていただけます。使い捨て2本は削除して構いません。**

---

## 3. 目視QC：**135/135シート 完了。却下114本・使える261本**

```
プール             374本（シートは375本ぶん＝1本は既に factory_rejected 済み）
  規則違反          25本   forbidden_subjects に該当、または顔が識別できる
  別の映画の画      89本   規則違反ではない。私の編集判断（理由は全件記載）
  ---------------------
  却下             114本
  使える           261本   うち 条件つき33本 / 特に良い84本
```

**261本は足りています。** 組み立て側の factory スロットは274。261本の別素材で274カットなら
再利用は平均1.05回、distinct比 0.95 で、`footage_diversity`（distinct≥0.40・再利用≤4）に対して
大きく余裕があります。**追加ダウンロードは不要です。**

判定は `runs/qc/lahaina_clip_verdicts.v001.json`。カテゴリは4つに分けてあります。

- `forbidden` … 機械が読める規則違反。議論の余地なし。
- `off_register` … 規則には当たらないが別の映画の画。**私の判断**なので理由を全件書いてあり、誰でも覆せます。
- `conditional` … 条件つきで使える（トリミング / この秒数から / 短く）。33本すべてに条件を明記。
- `standout` … 特に良い84本。組み立て時に優先して当ててほしいもの。

### 6コマ以上のご指示が効いた実例

**却下の多くは代表1コマでは絶対に見えません。**

```
10139699  無地の白い布      → 8コマ中7コマで布の向こうに顔が完全解像
8135728   完璧な書類の画    → 机の隅に「正義の女神像」（forbidden_subjects）
11180689  平凡な舗装(167秒) → 中盤からドリフト走行の連続
10603138  夜景空撮(206秒)   → 12コマ中9コマが off_label、実体は大都市
3234833   良質な枯れ草(58秒)→ 最後の1/3で霜と水辺に変わる
7710456   書類を繰る手      → 最初の1コマだけ顔が写り、残り5コマは画面外
```

### 見つけた構造的な問題

- **廃墟8本が全部同じ1か所**（`6694066`〜`6694074`＋`6703178`）。しかも苔とツタに覆われた
  欧州の廃屋で、焼け跡ではありません。1つのクエリが1か所を8回返していました。
- **ラリー競技3本が同一イベント**（`32791820` / `33085611` / `33513221`）。
- **ドラレコ視点が8本**。どれも車のボンネットかワイパーが写り込みます。
- **技術的欠陥2本**（`4849151` 左右黒帯 / `9721842` 上下黒帯）＝16:9でない素材。
- **窓のクエリが人物を連れてきました。** 顔が識別できる却下5本のうち3本が「窓」由来です。

### 判断を残したもの（組み立て側で決めてください）

- `38957773` **消防隊員** … 求めている画そのもので、**顔が6コマ中5コマで解像**します。
  切り抜いてホースと装備だけにするなら可。迷うなら落として構いません。
- `38779095`ほか3本 **交通管制室** … 緊急対策本部の画はプールにこれしかありません。
  オペレーターが写り、モニターの中身も読めます。顔が解像しない構図に限る。
- `34535507` / `34535510` **窓辺の男性のシルエット** … 顔は完全な黒で問題なし。
  ただしこの映画は犠牲者を名指しも描写もしません。**火事や死者の話の近くに置かないでください。**
- 農地の野焼き4本 … 一直線の着火線が「人が管理して燃やした」と読めます。
  `forbidden_claims` は放火の示唆を禁じています。狭く切って使うこと。

---

## 3.1 （記録）中間報告時点：50/135シート、却下41本

**6コマ以上のご指示は正しかったです。** 全375本が最小6・中央6・最大12コマになり、
**却下の大半は代表1コマでは絶対に見えないもの**でした。

```
10139699  無地の白い布 → 8コマ中7コマで布の向こうに顔が完全解像
11180689  167秒       → 序盤は平凡な舗装、中盤からドリフト走行の連続
11331025  金網フェンス → 途中からスケートボーダーが横切る
10603138  夜景空撮206秒 → 12コマ中9コマが off_label、実体は大都市
```

判定は**2列に分けて**記録しています。混ぜると後で誰も覆せなくなるからです。

- **`forbidden`（12本）** = 仕様 `episode_spec.v003` の `forbidden_subjects` に当たる、または人物が特定できる。
  機械が読める規則違反。議論の余地なし。
- **`off_register`（29本）** = 規則には当たらないが別の映画の画。**私の編集判断**なので、
  理由を全部書いてあります。誰でも覆せます。
- **`conditional`（6本）** = 条件つきで使える（トリミング / 短く / この秒数を避ける）。
- **`standout`（31本）** = 特に良い。組み立て時に優先して当ててほしいもの。

規則違反で拾った例2つ:

- `26167683` = **針葉樹林の火災**。`conifer forest` と `pine forest fire` は両方 forbidden で、
  理由は事実の問題です。この話の燃料は休耕地に生えた外来牧草で、松林の火事は**燃料を誤って伝えます**。
- `30960958` = **人が積んで火をつけた枝の山**。`forbidden_claims` の「放火をほのめかさない」に
  直接触ります。

**却下は前半に偏っています。シート27以降（2回目取得・ID 2900万〜3100万台）はほぼ全部が当たり**です。
斜面を這い上がる煙／家並みの真下から立つ煙／ヘリの写る煙柱／町を覆う煙のかさ／消火栓の放水2本／
変電所の空撮／道路を覆う砂塵／傾いた電柱／**画面が真っ黒なスマホを持つ手**。
1回目のコンタクトシートを見てから2回目のクエリを書いた効果が、そのまま数字に出ています。

**残り85シート。見終えるまで最終数は出しません。**
「見ていないものを見たと書かない」ため、判定ファイルの `status` は `IN PROGRESS -- NOT A PASS` のままです。

---

## 4. 権利情報がゲートから見えていない件 — **EP72/73 スレに移管済み（2026-08-22）**

> メインスレの割り当てにより、この修正は EP72/73 スレの担当になりました。**このスレは降ります。**
> 以下は移管先が読むための調査結果としてそのまま残します。



`check_pool_frames` の受領書は、**374本すべてが**こうなっています。

```
"provenance": "FILENAME ONLY -- no ledger row"   ×374
"look_closer": []     "era_prompts": []          ← 全件が空
```

**権利情報は実際には全部あります。** 810本すべての出典・作者・ライセンス・sha256 が
`05_stock/stock_ledger.v001.json` にあり、全件 `commercial_use: allowed` です
（Pexels License 215 / Pixabay Content License 198 …）。

**原因は、`check_pool_frames` が読むのが「ファクトリ台帳」で、ダウンロード素材は
`05_stock/stock_ledger` に入るため、両者が繋がっていないことです。**

影響が2つ:

1. 出荷判断の **`rights_and_licence` クラスから見て374本が「出所不明」に見えます**。
   実態はクリアなのに、機械には証明できません。
2. `look_closer` と `era_prompts` が全件空になり、ツールの分類支援が丸ごと効きません
   （私は目で見ているので進みますが、優先順位づけは使えていません）。

**ファクトリ棚が空になった以上、今後どのスレも「ダウンロードして staging」になります。**
これは EP75 固有ではなく全話数に効きます。**どちらのスレで直すか判断をください。**
私が直すなら、`check_pool_frames` が `05_stock/stock_ledger.v001.json` も参照する形が最小と見ています。

---

## 5. 残っていること

**このスレ … 残りゼロ。完了しました。**

~~1. プールフレーム目視~~ → **135/135 完了**（2026-08-22）
~~2. 判定ファイルの確定~~ → **`runs/qc/lahaina_clip_verdicts.v001.json` 確定済み**

**組み立てスレ ← 次はここ**

3. `remotion/src/lahaina_film.json` を作る（**現在ありません**）。
   素材は `remotion/public/lahaina/factory/` の374本から、**判定ファイルの
   `forbidden` と `off_register` の114本を除いた261本**を使ってください。
   `conditional` の33本には1本ずつ条件が書いてあります（トリミング / 開始秒 / 尺）。

**メインスレ（2026-08-22 に方針確定・各スレは触らない）**

4. `Root.tsx` と `compositions/Ep75LahainaFilm.tsx` の登録。
   **film.json ができた時点で「slug・composition id・film.json のパス」だけ渡すこと。**
   競合するのでこのスレでは登録しません。

**公開日**

5. 台帳 §10 の和解の状況を、公開当日にもう一度一次ソースで確認（生きている案件のため）

---

## 5.1 出荷可否の判定はプール段階ではない（メインスレ確定・2026-08-22）

**出荷の可否を決めるのは「実レンダーのコンタクトシートを人が原寸で読む」ゲートだけです。**
EP70 は58枚を読んで REJECT 0 = PASS を取りました。

**このスレがやっているプール段階の目視は、品質のための作業であって出荷判定ではありません。**
`lahaina_clip_verdicts.v001.json` が緑になっても、それは「悪い素材を組み立て前に外した」証拠に
すぎず、出荷許可ではない — レンダー後にもう一度、実フレームを人が読む必要があります。

---

## 6. 決まっていること（再確認不要）

- **i2v は使いません。** ご提示の実測（i2vなし 404カット/4.60秒per cut、marmet水準）に同意します。
  GPU 7.2時間が浮くこと以上に、374本の半分が使われなくなるのが決め手でした。
- 台本・字幕・絵・深度・音声・音響キューは**完了**。作り直す予定はありません。
