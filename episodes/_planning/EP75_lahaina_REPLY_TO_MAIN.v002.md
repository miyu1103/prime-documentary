# EP75 → メインスレ 返信 v002（2026-08-22）

## ■ ご指示の「2. 音響キュー」は**完了しています**（行き違いでした）

汎用版を書き、EP75 に適用し、ミキサー単独で検証まで済んでいます。**EP70/EP71 を寄せていただけます。**

```
scripts/write_sound_plan.py --slug <slug> [--rules <json>] [--dry-run]
config/sound_rules/<slug>.json          ← 作品ごとのルール
```

ご提示の設計要点は4つとも満たしています：

| ご指示 | 実装 |
|---|---|
| SCRIPT パスを slug から解決 | `episodes/PD-*-<slug>/03_script/script.en.v001.md` |
| kind は `ONESHOT_MAP` から取る（コード側に定数を置かない） | **実行時 import**。解決できない kind は**書き込み前に拒否** |
| anchor はそのビートが実際に話す語 | 一致しないものはスキップし、件数を報告 |
| 既存キューがある台本に二重書きしない | 冒頭で `(SFX:` を検出して拒否 |

**追加した安全弁が1つ**あります。`--dry-run` と、床（2.0/分・20個）に対する不足数の表示です。密度不足のまま書き込んで [4d] で気づく往復を、書き込み前に潰せます。

**ご指示どおりミキサーを単独で回し、実出力を読みました**（[4d] のメッセージは信用していません）：

```
$ py -3.11 scripts/build_case_film_audio.py --ep PD-2026-075-lahaina --out out_qc/_x.json --dry-run
SFX cues: 76 (before dedup 78, dropped 2); word-triggered=68; unmapped=0
density: sfx/min=2.453 (floor 2.0), distinct_beds=7 (floor 4), coverage=1.0 (floor 0.85) -> PASS
```

`unmapped=0` なので、kind の綴り違いによる無音落ちはありません。**使い捨て2本は削除して構いません。**

---

## ■ i2v不要の判定、ありがとうございます。同意します

`i2vなし 404カット / 4.60秒per cut`、marmet 水準。**374本を絞った作業がそのまま最良、という結論は納得です。**
GPU 7.2時間が浮くこと以上に、**374本の半分が使われなくなる**というのが決め手だと思います。

---

## ■ 新しく見つけたこと：**素材の権利情報が、ゲートから見えていません**

`check_pool_frames` の受領書を読んだところ、**374本すべてがこうなっています**：

```
"provenance": "FILENAME ONLY -- no ledger row"   ×374
"look_closer": []     "era_prompts": []          ← 全件が空
```

**権利情報は実際には全部あります。** 810本すべてについて出典・作者・ライセンス・sha256 が
`episodes/PD-2026-075-lahaina/05_stock/stock_ledger.v001.json` に記録済みです（Pexels License 215 / Pixabay Content License 198 …、全件 `commercial_use: allowed`）。

**問題は、`check_pool_frames` が読むのが「ファクトリ台帳」で、ダウンロード素材は `05_stock/stock_ledger` に入るため、両者が繋がっていないことです。**

影響が2つあります。

1. **出荷判断の `rights_and_licence` クラスから見て、374本が「出所不明」に見えます。** 実態はクリアなのに、機械には証明できません。
2. **`look_closer` と `era_prompts` が全件空になり、ツールの分類支援が丸ごと効きません。** 私は135枚を目で見ていますが、ツール側の優先順位付けは使えていません。

**ファクトリ棚が空になった以上、今後どのスレも「ダウンロードして staging」になります。** この繋ぎ込みは EP75 固有ではなく、全話数に効くと思います。**どちらのスレで直すか判断をいただけますか。**（私が直すなら、`check_pool_frames` が `05_stock/stock_ledger.v001.json` も参照する形が最小だと見ています。）

---

## ■ プールフレーム目視の進捗と、ご警告の裏づけ

**135枚中50枚を見て、却下40本**です（内訳: 仕様の禁止事項に当たるもの13本、規則違反ではないが別の映画の画 27本）。

**6コマ以上のご指示は正しかったです。** 生成し直した結果、全375本が最小6・中央6・最大12コマになりました。
そして**却下の大半は、代表1コマでは見えないもの**でした：

```
10139699  白い布の無地 → 8コマ中7コマで布の向こうに顔が完全解像
10603138  夜景空撮206秒 → 12コマ中9コマが off_label、実体は大都市
11180689  167秒 → 序盤は平凡な舗装、中盤からドリフト走行の連続
11331025  金網フェンス → 途中からスケートボーダーが横切る
11331037  同上
```

**ツールの `off_label` オレンジ表示が実用的な信号です。** 却下の約6割で、問題のあるフレームにこれが付いていました。EP70 の目視でも使えると思います。

**却下率は前半に偏っています。** ID順に並んでおり、1回目取得（1000万台〜2000万台）は却下が続きましたが、**シート27以降（2回目取得・2900万〜3100万台）はほぼ全部が当たり**です。斜面を這い上がる煙、家並みの真下から立つ煙、ヘリの写る煙柱、町を覆う煙のかさ、消火栓の放水2本、変電所の空撮、道路を覆う砂塵、傾いた電柱、そして**画面が真っ黒なスマホを持つ手**。**1回目のコンタクトシートを見てから2回目のクエリを書いた効果がそのまま数字に出ています。**

**なお、判定は2列に分けて記録しています。** 仕様の `forbidden_subjects` に当たるもの（機械が読める規則違反）と、規則には当たらないが別の映画の画（私の編集判断）です。後者は後から誰でも覆せるように、理由を全部書いています。

**規則違反で拾った例**: `26167683` は**針葉樹林の火災**でした。`conifer forest` と `pine forest fire` は両方 forbidden で、理由は事実の問題です（この話の燃料は休耕地に生えた外来牧草で、松林の火事は燃料を誤って伝える）。`30960958` は**人が積んで火をつけた枝の山**で、これは forbidden_claims の「放火をほのめかさない」に直接触ります。

**135枚を見終えるまで最終数は出しません。**

---

## ■ こちらの現在地（2026-08-22 実測）

| | |
|---|---|
| ナレーション | 1,857.403秒・357チャンク・配置済み |
| 字幕 | 907キュー・切れ位置不良3.5% |
| 絵 | 135枚すべて4K・すべて accept・**深度マップ135/135 生成済み**（設計の床40%に対し100%） |
| 映像 | 374本 staging 済み |
| 音響 | **76キュー・2.453/分・PASS** |
| 03_script | `[VO:]` 357行＋`(SFX:)` 71行 ／ `script.annotated.v001.json` |
| 残り | **プールフレーム 85枚の目視** ／ **Root.tsx 登録** |

**8/25 昼の予約に向けて、こちらの律速はプールフレームの目視だけです。** 終わり次第、組み立てへ渡せます。
